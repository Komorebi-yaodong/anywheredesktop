name: Build Desktop Packages

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  ELECTRON_BUILDER_DISABLE_AUTO_PUBLISH: true
  ELECTRON_BUILDER_BINARIES_MIRROR: https://github.com/electron-userland/electron-builder-binaries/releases/download/
  npm_config_electron_builder_binaries_mirror: https://github.com/electron-userland/electron-builder-binaries/releases/download/

on:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: win
            artifact_name: anywhere-desktop-windows
            artifact_glob: |
              dist/*-setup.exe
          - os: ubuntu-latest
            target: linux
            artifact_name: anywhere-desktop-linux
            artifact_glob: |
              dist/*.AppImage
          - os: macos-latest
            target: mac
            artifact_name: anywhere-desktop-macos
            artifact_glob: |
              dist/*-mac.zip

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify build icon assets
        shell: bash
        run: |
          test -f build/icon.png
          test -f build/icon.ico
          if [ "${{ runner.os }}" = "macOS" ]; then
            test -f build/icon.icns
          fi
          echo "Using checked-in icon assets from build/:"
          ls -R build

      - name: Print builder mirror envs
        shell: bash
        run: |
          echo "ELECTRON_BUILDER_BINARIES_MIRROR=$ELECTRON_BUILDER_BINARIES_MIRROR"
          echo "npm_config_electron_builder_binaries_mirror=$npm_config_electron_builder_binaries_mirror"

      - name: Build package
        run: pnpm build:${{ matrix.target }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          ELECTRON_BUILDER_DISABLE_AUTO_PUBLISH: true
          ELECTRON_BUILDER_BINARIES_MIRROR: https://github.com/electron-userland/electron-builder-binaries/releases/download/
          npm_config_electron_builder_binaries_mirror: https://github.com/electron-userland/electron-builder-binaries/releases/download/

      - name: Upload packaged artifacts
        uses: actions/upload-artifact@v5
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_glob }}
          if-no-files-found: error
