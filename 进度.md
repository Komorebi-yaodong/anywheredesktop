# 🚀 AI Anywhere Desktop 重构进度表

> **核心原则重申：**
>
> 1. **拒绝展位代码**：严格按顺序开发，底层能力（如数据库、窗口管理）未实现前，绝不进行前端 UI 的硬绑定。
> 2. **彻底脱离 uTools**：将所有 `utools.xxx` API 替换为 Electron 原生或 Node.js 实现。
> 3. **前后端数据转换**：严格管控 IPC 通信前后的数据序列化与反序列化（如 `File` 对象、`Buffer`、`Error` 等）。

原项目地址：E:\Programming\Anywhere
软件项目地址：E:\Programming\Anywhere_desktop

---

## 阶段 0：项目初始化与基础设施 (Phase 0)

```
PS E:\Programming> pnpm create @quick-start/electron Anywhere_desktop
.../19cdd49b4ca-5abc                     |   +8 +
.../19cdd49b4ca-5abc                     | Progress: resolved 8, reused 8, downloaded 0, added 8, done
√ Package name: ... anywhere-desktop
√ Select a framework: » vue
√ Add TypeScript? ... No / Yes
√ Add Electron updater plugin? ... No / Yes
√ Enable Electron download mirror proxy? ... No / Yes

Scaffolding project in E:\Programming\Anywhere_desktop...

Done. Now run:

  cd Anywhere_desktop
  pnpm install
  pnpm dev

```

- [X] **0.1 基础环境搭建**
  - [X] 初始化 `pnpm` 工作区，安装 `electron`、`vite`、`vue` 等核心依赖。
  - [X] 划分目录结构：`main/` (主进程), `preload/` (预加载), `render/` (渲染进程)。
  - [X] 配置 `vite.config.js`，支持多入口（`main/index.html`, `window/index.html`, `fast_window/index.html`）。
  - [X] 配置 Electron Builder / Electron Packager 的基础打包脚本。

### 会话切换快照（2026-03-11）
- ✅ 你已确认：当前无报错，主/窗/快窗口均可打开；界面仅骨架按钮（符合 Phase 0.1 预期）。
- ✅ 本地 Git 存档已完成（未 push）：
  - `9e463c9 fix(phase0.1): 修复空白窗口渲染报错并稳定三窗口骨架`
- 🔜 下一会话首要任务（Phase 0.2）：
  1. 新建 `main/dataConverter.js`
  2. 实现 `File -> Path/Buffer` 序列化转换
  3. 实现 `Error` 标准化序列化
  4. 实现 `safeClone`（JSON 安全深拷贝）
  5. 在主进程 IPC 层接入 converter 并完成 `pnpm build` 验证

### 验收修复记录（2026-03-11）
- 🐞 问题：主窗口空白，DevTools 报错：`Property "window" was accessed during render but is not defined on instance`。
- 根因：在 Vue 模板中直接访问 `window.api`，模板渲染上下文中不允许直接访问全局 `window`。
- 修复：
  - `render/main|window|fast_window/App.vue` 改为在 `<script setup>` 中声明 `appWindowType`，模板只使用响应式变量。
  - `preload/*_preload.js` 的 `openWindow` 改为 `electronAPI.ipcRenderer.invoke(...)`，避免依赖 `window.electron`。
- ✅ 再验证：`pnpm build` 通过。

### 验证记录（2026-03-11）
- ✅ `pnpm build` 通过（2026-03-12）：dataConverter 初始版本构建成功。
- 📝 2026-03-12：用户授权 AI 执行 Git 存档；受当前系统 Git 只读策略限制，仍需人工提交。


### 会话启动对齐（2026-03-12 / 本轮继续开发）
- [x] 已完成双重对齐：读取 `Code 专属记忆`（ID: `mmmcnu8w`）与项目 `README.md`、`进度.md`。
- [x] 已完成基线验证：`pnpm build` 通过（本轮）。
- [x] 已完成迁移差距复核：`render/main` 当前仍为联调壳，`Setting.vue` 尚未在桌面端落地；原项目 `Setting.vue` 依赖 `vuedraggable` 与 `webdav`，桌面端尚未安装。
- [/] 下一步执行计划：优先推进 `Phase B.2 设置与状态流转适配`，先迁移 `Setting.vue` 最小可用版（配置读写 + 主题 + 本地导入导出 + memory 同步），每步 `pnpm build` 验证并本地 Git 提交（不 push）。


### 会话启动对齐（2026-03-12）
- [x] 已读取 `README.md` 与 `进度.md`，完成任务上下文对齐。
- [x] 已执行基线检测：`pnpm build` 通过（本次会话）。
- [/] 下一步拟执行：优先完成 `A.2 窗口管理器重构` 的未完子项（主窗口显隐、窗口间投递/EventBus、Fast Window 交互完善），每个子项完成后单独构建验证。
- ⚠️ 说明：当前运行环境对 Git 为只读策略，AI 无法直接执行 commit/push；我会在每个小步骤通过验证后提供可直接粘贴执行的 Git 存档命令与建议提交信息。
- ✅ 授权更新（2026-03-12）：用户已明确授予本项目本地 Git 写权限；后续默认在每个小步骤构建验证通过后由 AI 直接执行 `git add` / `git commit`，无需重复申请（仍**绝不执行 `git push`**）。
- [x] 本轮基线复核（2026-03-12）：再次执行 `pnpm build` 通过，确认当前主干可继续迭代。
- [x] 本轮差距识别：`Phase A.4` 关键缺口已确认（`main/core/data.js`、`main/core/file.js` 缺失；preload 尚未暴露 `getConfig/saveSetting/updateConfig/exportMemoryData/importMemoryData`）。




- ✅ `pnpm build` 已通过（E:\\Programming\\Anywhere_desktop）
- 输出产物包含：
  - `out/main/index.js`
  - `out/preload/main_preload.js` / `window_preload.js` / `fast_preload.js`
  - `out/renderer/main/index.html` / `window/index.html` / `fast_window/index.html`
- 结论：Phase 0.1 多入口基础设施可用。

- [x] **0.2 核心数据转换器 (`main/dataConverter.js`)**
  - [x] 实现 `File` 到 `Path` / `Buffer` 的序列化拦截处理。
  - [x] 实现 Error 对象的标准化提取与传递封装。
  - [x] 实现 JSON 安全的深拷贝验证工具。

---

## 阶段 A：后端架构与 Preload 桥接 (Phase A)

*复刻原后端逻辑，用 Electron 原生能力完全替代 uTools 底座功能。*

- [x] **A.1 本地数据库重构 (`main/core/db.js`)**

  - [x] 选型并接入本地 JSON 数据库（如 `lowdb` 或 `sqlite`）以替代 `utools.db`。
  - [x] 实现文档 CRUD 操作：`put`, `get`, `remove`, `allDocs`, `bulkDocs`。
  - [x] 实现附件存储机制：`postAttachment`, `getAttachment`（将 Base64/Buffer 写入本地特定缓存目录，维护元数据映射）。
  - [x] 实现类 `localStorage` 接口：`dbStorage.setItem / getItem`。


### ✅ A.1 子任务记录（2026-03-12 / Step A.1.1）
- 已接入 `lowdb` 并落地 `main/core/db.js`。
- 已实现：
  - 文档 CRUD：`put/get/remove/allDocs/bulkDocs`
  - 附件：`postAttachment/getAttachment`（落盘到 `userData/attachments`）
  - 存储：`dbStorageSetItem/dbStorageGetItem/dbStorageRemoveItem/dbStorageListKeys`
  - 诊断：`isDbReady/getDbStats`
- IPC 已接入：`db:*` 与 `dbStorage:*` 通道。
- preload 已暴露 db 接口（main/window/fast 三窗口一致）。
- ✅ 验证：`pnpm build` 通过。

- [x] **A.2 窗口管理器重构 (`main/windowManager.js`)**

### ✅ A.2 实施记录（2026-03-12 08:01）


### ✅ A.2 子任务记录（2026-03-12 / Step A.2.2）
- 已新增主窗口显隐能力：`showMainWindow()` / `hideMainWindow()`（`main/windowManager.js`）。
- 已新增 IPC 通道：`window:showMain` / `window:hideMain`（`main/ipcHandler.js`）。
- 已完成主进程注入：`registerIpcHandlers({ openWindow, showMainWindow, hideMainWindow })`（`main/index.js`）。
- 已在三套 preload 暴露 API：`showMainWindow` / `hideMainWindow`。
- 已在骨架页面加入联调入口按钮（main/window/fast）。
- ✅ 验证：`pnpm build` 通过。


### ✅ A.2 子任务记录（2026-03-12 / Step A.2.5）
- 已新增 `main/eventBus.js`：实现进程内窗口事件总线 `dispatchWindowEvent`。
- 新增事件投递模式：
  - `target: 'main' | 'fast' | '<windowId>'`（定向投递）
  - `target: 'type:window'`（按窗口类型投递）
  - `target: 'broadcast' | '*'`（全量广播）
- 已新增 IPC：
  - `window:list`：列出当前存活窗口
  - `window:event:emit`：发送窗口事件
- 已在 `windowManager` 增加 `getWindowByRef / listWindows / getWindowRefByWebContentsId`，并为每个窗口绑定 `webContents.id -> windowRef`。
- 已在三套 preload 暴露：`listWindows` / `emitWindowEvent` / `onWindowEvent`。
- 已在 main/window/fast 骨架页面增加联调按钮与“最近收到事件”展示，用于验证“追加消息式”投递链路。
- ✅ 验证：`pnpm build` 通过。


### ✅ A.2 子任务记录（2026-03-12 / Step A.2.3）
- 已为窗口注入初始化信号 `window:init`（`windowManager` 在 `did-finish-load` 后发送）。
- 已为独立对话窗口提供稳定 `senderId`（即多实例 `windowRef/id`），并在 preload 侧通过 `getWindowContext()` 暴露。
- 已在三套 preload 增加：`onWindowInit` + `getWindowContext`。
- 已在三套骨架页面展示 `senderId`，用于后续“定向追问/追加消息”链路联调。
- ✅ 验证：`pnpm build` 通过。


### 🛠️ A.2 热修复记录（2026-03-12 / Close Crash）
- 用户反馈：关闭窗口时主进程报错 `TypeError: Object has been destroyed at unbindWindowRef`。
- 根因：`BrowserWindow` 进入 `closed` 阶段后，访问 `win.webContents` 可能抛出对象已销毁异常。
- 修复：
  - 新增 `unbindWindowRefByWebContentsId`，在窗口创建时预捕获 `webContentsId`。
  - `closed` 回调改为按预捕获 `webContentsId` 解绑映射，不再触碰已销毁对象。
- 兼容策略：主窗口显隐逻辑保留为“可选能力”，不将“打开对话前必须隐藏主窗口”作为桌面版强制策略。

- 体验修正（2026-03-12 / 用户反馈）：`hideMainWindow` 语义从“直接隐藏”改为“最小化主窗口”，避免主窗口彻底不可见难以找回。





- 补充说明：A.2 核心子项均已达成（窗口创建复用、主窗口显隐、Fast 失焦隐藏、EventBus、senderId 注入）；后续将把窗口控制能力（最小化/最大化/置顶切换）在 A.5 里以稳定 API 形式补齐。

- 目标：把 `main/index.js` 中的窗口创建逻辑抽离为独立模块，形成可复用的 `windowManager`；并在 IPC 层形成统一的 invoke-handler 包装，保证返回值可序列化、异常可结构化回传。
- 产出文件：
  - `main/windowManager.js`：
    - `openWindow(type)`：`main/fast` 走单例复用；`window` 走多实例（用 `randomUUID()` 生成 id 并维护索引）。
    - 提供 `getWindowIds(type)` / `getWindowById(id)` 作为后续多窗口通信/投递的基础能力。
  - `main/ipcHandler.js`：
    - `handleInvoke(channel, handler)`：统一 try/catch；成功路径走 `serializeIpcPayload`；失败路径回 `{ok:false, error: serializeError(err)}`。
    - `registerIpcHandlers(...)`：已从单一 `window:open` 扩展到主窗口显隐、窗口列表与事件投递。
  - `main/index.js`：精简为应用生命周期 + 注册 IPC + `openWindow('main')`。
- 验证：`pnpm build` 通过；`pnpm dev` 可正常启动（renderer dev server 5173 + Electron app）。
- 约束说明：当前 `window:open` 仍返回基础信息（ok/type/id），后续做“消息投递/窗口间通信”时再引入 senderId 与更精细的路由。


  - [x] 替代 `utools.createBrowserWindow`，封装带状态的 `BrowserWindow` 创建逻辑。
  - [x] 实现主控台（Main Window）的单例管理与显隐控制 (`hideMainWindow`, `showMainWindow`)。
  - [x] 实现独立对话窗口（Agent Window）的多实例管理，维护 `windowMap`，注入唯一 `senderId`。
  - [x] 实现快捷输入窗口（Fast Window）的无边框、透明、失去焦点自动隐藏处理。
  - [x] 替代 `utools.redirect`：实现进程内事件总线（EventBus），完成窗口间的指令投递（如“追加消息”）。
- [x] **A.3 系统级 API 重写 (`main/core/system.js`)**

  - [x] 剪贴板：基于 `electron.clipboard` 实现 `copyText`、`copyImage` 等。
  - [x] 文件弹窗：基于 `electron.dialog` 实现 `showOpenDialog`、`showSaveDialog`。
  - [x] 文件管理器：基于 `electron.shell` 实现 `shellOpenPath`、`shellShowItemInFolder`。
  - [x] 屏幕拾取：基于 `desktopCapturer` 实现音视频/窗口源获取。


### ✅ A.3 子任务记录（2026-03-12 / Step A.3.1）
- 新增 `main/core/system.js`：
  - 剪贴板：`copyText / copyImage / readClipboardText`
  - 对话框：`showOpenDialog / showSaveDialog`
  - Shell：`shellOpenPath / shellShowItemInFolder / shellOpenExternal`
  - 桌面源：`getDesktopSources`（返回可序列化缩略图与图标）
- IPC 新增：
  - `system:clipboard:*`
  - `system:dialog:*`
  - `system:shell:*`
  - `system:desktop:getSources`
- preload 已同步暴露 system API（main/window/fast 三窗口一致）。
- ✅ 验证：`pnpm build` 通过。


### 🧾 会话压缩总结（2026-03-12，上下文续航）

- 当前已稳定完成并通过构建：`Phase 0.2`、`A.1`、`A.2`、`A.3`。
- 关键修复：
  - 关闭窗口崩溃（`Object has been destroyed at unbindWindowRef`）已修复。
  - 主窗口“隐藏”行为已改为“最小化”，避免窗口不可找回。
- 本地 Git 存档（未 push）关键提交：
  - `4406c10`、`27971e9`、`f50df0a`、`5784454`、`b329a43`、`9c40c31`、`7372b68`。
- 本轮新增外部变更同步：
  - 原项目 `Anywhere` 已拉到 `92fb056`（删除`显示通知`，新增 memory 同步）。
  - 影响文件：`backend/src/data.js`、`backend/src/preload.js`、`backend/src/file.js`、`Anywhere_main/src/components/Setting.vue`。
  - 已完成阅读确认：
    - `preload.js` 新增 `exportMemoryData/importMemoryData` 暴露。
    - `Setting.vue` 在配置导入导出中新增 memory 同步逻辑。
    - `data.js` 迁移时需同步移除 `showNotification` 残留，并补 memory 导入导出接口。
- 图标更新状态：
  - `E:\Programming\Anywhere_desktop\resources\icon.png` 已检测存在；当前代码已引用资源图标。
- 下一步（保持小步+验证+本地存档节奏）：
  1. 落地 `main/core/data.js`（基于现有 `dbApi`，对齐 `92fb056` 的 memory 同步能力）
  2. 落地 `main/core/file.js`（替换 uTools 文件/弹窗能力为 system IPC + Node fs）
  3. 回填 preload API 以先对齐 `Setting.vue` 依赖链路（`getConfig/saveSetting/updateConfig/exportMemoryData/importMemoryData`）


- [/] **A.4 原后端业务代码迁移与适配**

  - [x] 重构 `main/core/data.js`：接入新版 DB API，替换所有 `utools.db` 调用。
    - 补充说明：已完成配置分块存储（`config/prompts/providers/mcpServers/tasks/config_local_desktop`）与 memory 导入导出；后续在迁移 `chat/mcp/skill` 时按需补充扩展能力。
    - [x] Step A.4.1（已完成）：先落地配置与 memory 同步最小闭环（`getConfig/saveSetting/updateConfig/updateConfigWithoutFeatures/exportMemoryData/importMemoryData`），并完成 IPC + preload 暴露。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

    - [x] Step A.4.3.3（已完成）：迁移 `skill.js` -> `main/core/skill.js`，并打通 skill IPC / preload（list/get/save/delete/export/extract/definition/resolve/pathJoin）。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [x] 重构 `main/core/file.js`：将原 uTools 文件对象逻辑改写为基于 Node.js `fs/promises` 的绝对路径读取。
    - [x] Step A.4.2（已完成）：新增 `main/core/file.js` 并接入 IPC（`file:*`）；三套 preload 已暴露文件相关 API（`handleFilePath/sendfileDirect/saveFile/selectDirectory/listJsonFiles/readLocalFile/renameLocalFile/deleteLocalFile/writeLocalFile/setFileMtime/isFileTypeSupported/parseFileObject/copyLocalPath`）。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [x] 迁移 `chat.js`、`mcp.js`、`mcp_builtin.js`、`skill.js`：审查并替换其中可能存在的 uTools API。

    - [x] 说明：`mcp_builtin.js` 已在桌面端落地最小可用骨架（内置工具注册与调用入口），后续在 C/D 阶段按 UI/Agent 联调逐步填充具体内置工具实现。
    - ✅ 本轮本地提交：
      - `a70218e` feat(phase-a4.3.1): 迁移 chat 核心并接入 Electron IPC
      - `db4bbb8` feat(phase-a4.3.2): 迁移 mcp 管理核心并打通 IPC/preload
      - `d347989` feat(phase-a4.3.3): 迁移 skill 核心并接入 IPC/preload


  - [/] Step A.4.3（进行中，2026-03-12）：按“小步迁移 + 每步 build + 本地 commit”推进。
    - 本轮范围：先迁移 `chat.js` 到 `main/core/chat.js`（优先复用原实现，仅做 ESM/工程化小改）。
    - Git 策略：用户已确认 **ICON 改动纳入**后续提交。

    - [x] Step A.4.3.1（已完成）：迁移 `chat.js` -> `main/core/chat.js`（ESM 化），并接入 `chat:createCompletion` IPC 与三套 preload `createChatCompletion` API。

    - [x] Step A.4.3.2（已完成）：迁移 `mcp.js` -> `main/core/mcp.js`，先构建 MCP 服务管理最小闭环（初始化/工具列表/调用/关闭 + IPC）。

    - [x] Step A.4.4（已完成，2026-03-12）：同步上游 `5ccb192` 的文件类型判定增强（`file.js`），并评估 `data.js` 差异在桌面端是否存在对应实现。
      - 结论：桌面端 `main/core/data.js` 当前未实现上游 `expectedMatchFeature/cmds` 那套分支，故本次 `data.js` 无需迁移该 diff。
      - 已同步到 `main/core/file.js`：`isFileTypeSupported` 改为“白名单支持 + 二进制黑名单拒绝 + 未知后缀放行待探针”；`parseFileObject` 新增 Base64 内容探针（\0 检测），未知后缀且非二进制按文本处理。
      - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。


    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

- [x] **A.5 Preload 层隔离实现**
  - [x] Step A.5.1（已完成）：补齐窗口控制相关 IPC 与 preload API（`window:minimize/maximizeOrRestore/close/toggleAlwaysOnTop`），为后续 `TitleBar.vue` 迁移铺路。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

  - [x] 编写 `preload/main_preload.js`：暴露给主设置界面的 API。
  - [x] 编写 `preload/window_preload.js`：暴露给对话界面的 API（包含窗口控制、置顶切换等 IPC）。
  - 说明：已统一暴露数据层、文件层、窗口事件总线与窗口控制能力，并保留 `windowControl(action)` 兼容接口以承接旧代码迁移。
  - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮，见 Step A.5.1）。
  - [x] 编写 `preload/fast_preload.js`：暴露给快捷指令窗的轻量级 API。

---


### 会话启动对齐（2026-03-12 / 本轮）
- [x] 已完成双重对齐：读取 `Code 专属记忆`（ID: `mmmcnu8w`）与项目 `README.md`、`进度.md`。
- [x] 已完成当前基线验证：`pnpm build` 通过（2026-03-12，本轮）。
- [x] 已完成迁移差距复核：
  - `render/main` 当前仅落地 `App.vue + Setting.vue`，`Providers/Mcp/Skills/Prompts/Tasks/Chats` 尚未迁移。
  - 桌面端已具备大部分基础 API（config/file/mcp/skill/webdav/window 控制），但仍缺少 `coderedirect`、`runTaskNow`、`getRandomItem` 兼容能力，属于 B.3 关键阻塞项。
- [/] 下一步执行计划（小步快跑）：
  1. **Step B.3.0**：先补齐阻塞 API（`coderedirect` / `runTaskNow` / `getRandomItem`）到 `main + ipc + preload`，并用最小联调按钮验证。
  2. **Step B.3.1**：迁移 `Providers.vue`（低风险，主要是配置读写）。
  3. **Step B.3.2**：迁移 `Mcp.vue`（复用现有 mcp IPC，补足文案与交互）。
  4. **Step B.3.3**：迁移 `Skills.vue`（复用现有 file/skill IPC）。
  5. **Step B.3.4**：迁移 `Prompts.vue`（依赖 `coderedirect`，在 B.3.0 后执行）。
  6. **Step B.3.5**：迁移 `Tasks.vue` 与 `Chats.vue`（依赖 `runTaskNow` / `coderedirect` / file API）。
- ✅ Git 策略：继续执行“每个小步骤 `pnpm build` 通过后立即本地提交（不 push）”。

## 阶段 B：Anywhere_main 主控制台复刻 (Phase B)

*直接复用 Vue 代码，修改接口对接方式，确保界面与设置数据跑通。*

- [x] **B.1 前端环境与公共资源挂载**

  - [x] Step B.1.0（已完成）：清理脚手架残留目录 `src/`（确认当前构建不引用），为 `Anywhere_main` 迁移消除歧义入口。

    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

  - [ ] 在 `render/main` 中配置 Vue + Element Plus，引入原项目的 `base.css`, `dark.css` 等样式文件。

  - [x] Step B.1.2（已完成，2026-03-12）：同步 `Anywhere_main/src/assets/base.css` 与 `main.css` 到 `render/main/assets/`，统一主控台基础样式基线。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。


  - [x] Step B.1.1（已完成，2026-03-12）：迁移 `Anywhere_main/src/main.js` 的插件与 i18n 初始化到 `render/main/main.js`（Element Plus + icons + i18n + locales）。
    - 已新增 `render/main/locales/en.json` 与 `render/main/locales/zh.json`（先落地最小语言包，后续随组件迁移扩充）。
    - 已安装依赖：`element-plus`、`@element-plus/icons-vue`、`vue-i18n`。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。


  - [x] Step B.2.1（已完成）：`render/main/App.vue` 已升级为配置与 Memory 联调控制台，已接入 `getConfig/saveSetting/updateConfig/exportMemoryData/importMemoryData` 的可视化验证入口。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [x] Step B.2.1（已完成）：在 `render/main` 引入 `Setting.vue` 最小容器，先跑通 `getConfig/saveSetting/updateConfig/exportMemoryData/importMemoryData` 联调链路。
  - [ ] 确保原主题切换逻辑（监听媒体查询及 `html.dark` 类名）正常工作。

  - [x] Step B.2.2（已完成，2026-03-12）：在 `render/main/App.vue` 建立最小主题闭环（`themeMode` + `isDarkMode` + 系统主题监听 + `html.dark` 类名同步）。
    - 已实现：`matchMedia('(prefers-color-scheme: dark)')` 监听、`watch([themeMode,isDarkMode])` 响应式同步、组件卸载时清理监听器。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。


### 🔄 会话交接摘要（2026-03-12，供新会话续接）
- ✅ 本会话已完成并提交：
  - `fd4cee1` chore(cleanup): 删除无用 `src` 脚手架目录并验证构建
  - 上一轮已完成并存在本地提交：
    - `a70218e`（A.4.3.1 chat 迁移）
    - `db4bbb8`（A.4.3.2 mcp 迁移）
    - `d347989`（A.4.3.3 skill 迁移）
    - `011fd29`（进度文档归档）
- ✅ 当前代码状态：`pnpm build` 通过，仓库干净可继续。
- ✅ 目录结论：当前唯一生效渲染目录为 `render/`；`src/` 已确认无引用并清理。
- 🎯 下一会话首要任务（严格小步）：
  1. **B.1.1**：把 `Anywhere_main/src/main.js` 的插件与 i18n 初始化迁入 `render/main/main.js`（Element Plus + icons + i18n）。
  2. **B.1.2**：迁移 `Anywhere_main/src/assets/base.css` 与 `main.css` 到 `render/main/assets/`，保证主题变量与视觉一致。
  3. **B.2.1**：迁移 `Anywhere_main/src/App.vue` 主壳结构（tabs/header/layout），先保证 UI 架子与主题切换逻辑工作。
  4. 每小步都执行 `pnpm build` 并本地 commit（不 push）。

- [x] **B.2 设置与状态流转适配**

  - [x] Step B.2.0（已完成，2026-03-12）：补齐 `Setting.vue` 迁移前置依赖（`vuedraggable`、`webdav`）。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。


  - [x] Step B.2.1（已完成，2026-03-12）：迁移 `Anywhere_main/src/components/Setting.vue` 到 `render/main/components/` 并接入桌面端主控台壳。
    - 适配说明：`render/main/App.vue` 已改为 `config provide + Setting 宿主` 结构；新增默认配置兜底与系统主题监听，保障 `Setting.vue` 在桌面端可独立运行。
    - 依赖修正：`vuedraggable` 已切换至 Vue3 兼容版本 `^4.1.0`。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

  - [x] Step B.2.2（已完成，2026-03-12）：将 `Setting.vue` 的配置导入导出从浏览器下载/文件输入改造为 Electron `dialog + file IPC`（`showSaveDialog/showOpenDialog/writeLocalFile/readLocalFile`），保留 memory 同步逻辑。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

  - [x] Step B.2.3（已完成，2026-03-12）：落地 WebDAV 同步链路主进程化方案，新增 `main/core/webdav.js` + IPC + preload API，并将 `Setting.vue` 的 WebDAV 备份/恢复/列表/删除改为主进程桥接调用。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

- [x] `Setting.vue`: 接入新版 `window.api.getConfig` 和 `window.api.saveSetting`。
  - [x] 测试数据导入/导出功能（由于移除了 uTools 底座，需确保文件存取走 `dialog` API）。

- [x] 测试 WebDAV 同步功能网络请求是否在 Electron 渲染进程正常执行（或移交主进程处理跨域）。

- [/] **B.4 主控台样式深度复刻修复（新增，2026-03-12）**

  - [/] Step B.4.H2（进行中，2026-03-12 夜间）：运行态报错分治（按“先崩溃、后连通”策略）。
    - [x] 目标1（当前小步）：修复 `Chats.vue` 在部分文件项 `basename` 缺失时触发的 `endsWith` 崩溃，确保历史对话页可稳定渲染。
      - 已完成：为本地/云端历史文件列表增加 `normalizeChatFile`/`resolveFileBasename`/`resolveWebdavDataPath` 兜底；`formatFilenameDisplay` 已改为安全字符串处理。
      - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮小步）。
    - [/] 目标2（下一小步）：将 `Providers.vue` 的模型拉取改为主进程代理，规避 CSP `connect-src` 限制。
    - [ ] 目标3（后续小步）：为 `atomicSave` 增加配置深拷贝防护，规避 `An object could not be cloned`。


  - [/] Step B.4.H1（进行中，2026-03-12）：样式复刻后回归热修。

    - [x] 已完成修复1（启动崩溃）：`render/main/App.vue` 移除对 `window.api.defaultConfig.config` 的硬依赖，改为本地 fallback + `normalizeConfigPayload`，避免 preload 未暴露字段时空指针崩溃。
    - [x] 已完成修复2（CSP 警告）：`render/main/index.html` 头部恢复 CSP meta（不启用 `unsafe-eval`，保留 `style-src 'unsafe-inline'` 兼容主题内联样式，并放开文档拉取所需 `img/connect` 域）。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮热修）。
    - 🔜 下一步：请用户运行主窗口做运行态复测，重点观察：1) 启动不再报 `defaultConfig.config`；2) CSP 警告消失/显著收敛；3) 主样式保持复刻效果。

    - 用户反馈：主控台启动报错 `Cannot read properties of undefined (reading 'config')`，并出现 Electron CSP 安全警告。
    - 已定位：`render/main/App.vue` 依赖 `window.api.defaultConfig.config`，但桌面端 preload 未暴露该字段；此外 `render/main/index.html` 在复制后缺失 CSP meta。
    - 修复计划：
      1) 将 `App.vue` 改为“无 `defaultConfig` 依赖”的安全配置归一化逻辑（优先用 `getConfig` 返回值，失败走本地 fallback）；
      2) 在 `render/main/index.html` 恢复严格 CSP（不启用 `unsafe-eval`，保留 `style-src 'unsafe-inline'` 以兼容主题内联样式）；
      3) `pnpm build` 验证通过后立即本地 Git 存档。


  - [x] Step B.4.1（已完成，2026-03-12）：按“复制为主、少量适配”完成主样式入口迁移。

    - ✅ 本地 Git 存档：`63fa53e fix(main-style): 复刻Anywhere_main主题样式并补齐marked依赖`（不含 push）。

    - ✅ 已完成 Step B.4.1.a：执行 `pnpm add marked` 后再次 `pnpm build` 通过。
    - 构建结果：`out/renderer/main/index.html` 已包含迁移后的主题变量样式（体积显著增长，符合预期），主控台应恢复接近原项目视觉基线。
    - 🔜 下一步：运行态视觉复核（主窗口头部/选项卡/深浅色切换/对话框样式），通过后归档提交。

    - ⚠️ 构建阻塞（本步验证中发现）：`pnpm build` 失败，错误为 `Rollup failed to resolve import "marked"`（来源：`render/main/App.vue` 已复用原项目代码但桌面端未声明 `marked` 依赖）。
    - 处理计划：Step B.4.1.a 补齐 `marked` 依赖后重跑构建。
    - 已将 `E:\Programming\Anywhere\Anywhere_main\index.html` 整体复制到 `E:\Programming\Anywhere_desktop\render\main\index.html`。
    - 仅做一处适配：入口脚本从 `/src/main.js` 改为桌面端 `/main/main.js`（匹配 `electron.vite.config.mjs` renderer input）。
    - 🔜 下一步：执行 `pnpm build` 验证；通过后再进行运行态视觉复核与本地 Git 存档。


  - [x] Step B.4.0（已完成，2026-03-12）：完成样式偏差根因排查。
    - 结论1：`render/main/App.vue` 与原项目 `Anywhere_main/src/App.vue` SHA256 一致（说明 Vue 主壳代码已复制，非主要偏差源）。
    - 结论2：桌面端 `render/main/index.html` 仅保留最小骨架（约 15 行），缺失原项目 `Anywhere_main/index.html` 中大体量内联主题变量与 Element Plus 全局覆盖样式（约 400+ 行），这是“界面看起来像默认 Element 风格”的直接原因。
    - 结论3：当前桌面端仍受严格 CSP 限制（`img-src 'self' data:`），后续可能影响远程文档图片显示，但不影响本次主样式根因判断。
    - 🔜 下一步：执行 Step B.4.1，按“复制为主、少量适配”将原 `Anywhere_main/index.html` 主题样式块迁移到 `render/main/index.html`，仅保留 Electron 入口脚本路径适配。
  - 背景：用户反馈当前桌面端主壳样式与原 `Anywhere_main` 明显不一致。
  - 目标：优先复刻原 `Anywhere_main/src/App.vue` 主壳结构与 `index.html` 内联主题变量样式；保持“复制为主，少量适配”为原则。

- [x] **B.3 其他管理页面复刻**
  - [x] Step B.3.0（已完成，2026-03-12）：补齐 B.3 关键阻塞 API（`coderedirect` / `runTaskNow` / `getRandomItem`），已打通主进程 + IPC + preload 最小兼容层，并复用窗口事件总线实现 `coderedirect` 与 `runTaskNow` 的跨窗口投递。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [x] `Providers.vue` (服务商，已完成 2026-03-12)：已从原项目快速复用并接入主控台页面切换，完成桌面端 API 适配（`getConfig/updateConfigWithoutFeatures/saveSetting/getRandomItem`）与模型拉取链路验证。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [x] `Mcp.vue` (MCP 管理，已完成 2026-03-12)：已快速复用并接入主控台页面切换，打通 `testMcpConnection/testInvokeMcpTool/getMcpToolCache/saveMcpToolCache` 联调链路，并完成桌面端 `copyText` 返回结构兼容。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [x] `Skills.vue` (Skill 管理，已完成 2026-03-12)：已接入主控台页面切换（`render/main/App.vue` 导航与渲染分支），并完成桌面端基础链路联调（本地目录读取、Skill 导入导出、文件操作 API）。
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [x] `Prompts.vue` (快捷助手，已完成 2026-03-12)：已从原项目复用并接入桌面端主控台页面切换，完成正则匹配/数据关联基础链路与 `coderedirect` 调用链路接线。
  - [x] `Tasks.vue` (定时任务，已完成 2026-03-12)：已迁移并接入主控台页面切换，完成 `runTaskNow`、任务配置读写与历史记录文件操作链路接线。
  - [x] `Chats.vue` (历史对话，已完成 2026-03-12)：已迁移并接入主控台页面切换，完成本地文件列表读取、重命名/删除与云端同步链路接线。

### ✅ B.3 子任务记录（2026-03-12 / Step B.3.3）
- 已在 `render/main/App.vue` 接入 `Skills` 页面导航入口与渲染分支（`activePage === 'skills'`）。
- 已确认 `render/main/components/Skills.vue` 与既有 `preload + ipc` 的 skill/file API 兼容。
- ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
- 🔜 下一步：执行 Step B.3.4，迁移并接入 `Prompts.vue`。


### ✅ B.3 子任务记录（2026-03-12 / Step B.3.4）
- 已新增 `render/main/components/Prompts.vue`（复用自原项目并在桌面端落地）。
- 已在 `render/main/App.vue` 接入 `Prompts` 导航入口与渲染分支（`activePage === 'prompts'`）。
- API 对齐确认：`listSkills/coderedirect/getConfig/updateConfig/updateConfigWithoutFeatures/saveSetting` 在桌面端已可用。
- ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
- 🔜 下一步：执行 Step B.3.5，迁移并接入 `Tasks.vue`。


### ✅ B.3 子任务记录（2026-03-12 / Step B.3.5）
- 已新增 `render/main/components/Tasks.vue`（复用自原项目并在桌面端落地）。
- 已在 `render/main/App.vue` 接入 `Tasks` 导航入口与渲染分支（`activePage === 'tasks'`）。
- API 对齐确认：`runTaskNow/getConfig/updateConfigWithoutFeatures/listSkills/readLocalFile/deleteLocalFile/coderedirect/pathJoin` 在桌面端已可用。
- ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
- 🔜 下一步：继续 Step B.3.5，迁移并接入 `Chats.vue`。


### ✅ B.3 子任务记录（2026-03-12 / Step B.3.6）
- 已新增 `render/main/components/Chats.vue`（复用自原项目并在桌面端落地）。
- 已在 `render/main/App.vue` 接入 `Chats` 导航入口与渲染分支（`activePage === 'chats'`）。
- API 对齐确认：`getConfig/listJsonFiles/readLocalFile/renameLocalFile/deleteLocalFile/writeLocalFile/setFileMtime/coderedirect` 在桌面端已可用。
- ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
- 🔜 下一步：将 `B.3 其他管理页面复刻` 总项转为完成，并进入 Phase C。





---

### 会话任务记录（2026-03-12）
- [x] 文档任务：按用户要求重写 `README.md`（包含软件主要内容、项目架构、开发环境、本地开发指导），并与“插件->Electron App 从 0 重构”目标对齐。
- [/] 下一步：在 README 完成后继续输出/细化 `进度.md`（五阶段拆解：A~E）。


## 阶段 C：Anywhere_window 独立对话窗口复刻 (Phase C)

*确保聊天体验一致，特别是 Markdown 渲染、多模态处理和长图导出。*

- [ ] **C.1 对话框基础 UI 组装**

  - [ ] 搭建 `render/window/index.html` 及 Vue 实例。
  - [ ] 移植 `TitleBar.vue`：使用 Electron 的 IPC 控制最小化、最大化、关闭、置顶。
  - [ ] 移植 `ChatHeader.vue`、`ChatInput.vue`。
- [ ] **C.2 核心聊天组件适配**

  - [ ] 移植 `ChatMessage.vue`：集成 Markdown、KaTeX、Mermaid 及 DOMPurify 渲染。
  - [ ] 修复代码块一键复制的 API 调用。
  - [ ] **长图导出** (`html2canvas`)：由于前端安全策略限制，需确保在 Electron 中允许加载 `file://` 图片或将图片转 Base64 后再绘制，测试导出并调用原生系统弹窗保存。
- [ ] **C.3 工具审批与协同机制流转测试**

  - [ ] 测试 MCP 工具的自动执行与手动审批弹窗。
  - [ ] 测试多窗口间的协同通信（如调用 `summon_agent` 后，新弹出一个 BrowserWindow 并传递初始数据）。

---

## 阶段 D：Fast_window 快捷唤出面板重构 (Phase D)

*将原生的多个 HTML 重构，使其符合 Electron 最佳实践。*

- [ ] **D.1 Fast Input 悬浮输入条**

  - [ ] 重构 `fast_input.html`（可考虑转为轻量级 Vue3 组件或保持原生 HTML）。
  - [ ] 在 `windowManager.js` 中实现**透明、无边框、置顶**窗口属性。
  - [ ] 优化粒子特效性能（CSS Animation 在透明 Electron 窗口下的性能测试）。
  - [ ] 实现失焦自动关闭（监听 `blur` 事件并通知主进程隐藏窗口）。
- [ ] **D.2 追加选择器 (Append Selector)**

  - [ ] 重构 `append_selector.html`。
  - [ ] 实现跨窗口进程通信：获取当前存活的 `Agent Window` 列表并展示。
  - [ ] 实现点击后，将 Payload 转发至主进程，再由主进程投递给特定的对话窗口。

---

## 阶段 E：桌面端专属体验优化 (Phase E)

*利用纯桌面应用特权，提供原 uTools 版本无法实现的高级功能。*

- [ ] **E.1 全局快捷键与呼出机制**

  - [ ] 主进程注册 `globalShortcut`（如 `Alt+Space` 唤出主控台，`Alt+A` 唤出快捷输入）。
  - [ ] 在设置中心 (`Setting.vue`) 添加“自定义全局快捷键”的 UI 并绑定至主进程。
- [ ] **E.2 划词直接调用功能 (重磅扩展)**

  - [ ] 实现原生选词捕获机制：监听特定组合键（如连续按两下 `Ctrl`），触发主进程模拟发送 `Ctrl+C`。
  - [ ] 抓取系统剪贴板文本/图片，自动填入并直接弹出 Fast Input 或指定的独立对话窗口。
- [ ] **E.3 系统托盘 (System Tray) 与后台运行**

  - [ ] 创建 `Tray` 图标，添加右键菜单（显示主窗口、退出应用等）。
  - [ ] 拦截主窗口的“关闭”事件，改为“隐藏到托盘”（确保定时任务持续运行）。
- [ ] **E.4 打包与分发**

  - [ ] 优化应用启动速度（Vite 预构建、懒加载等）。
  - [ ] 配置 NSIS (Windows) 打包参数（包括应用图标、安装路径、自启动注册表配置）。
  - [ ] 执行端到端测试，检查数据在打包后的 `userData` 路径读写是否正常。

---

> **当前阶段标记**：🟡 **Phase B.3 准备启动（主控制台其余页面复刻）**
> *（下一步先补齐 B.3 阻塞 API：`coderedirect` / `runTaskNow` / `getRandomItem`，再逐页迁移 Providers/Mcp/Skills/Prompts/Tasks/Chats）*
