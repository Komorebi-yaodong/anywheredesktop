# Anywhere Desktop 重构进度（整理版）

> 最后整理：**2026-03-26**
>
> - 原项目：`E:\Programming\Anywhere`
> - 桌面端项目：`E:\Programming\Anywhere_desktop`
> - 当前目标：**在主控台与独立对话窗口已基本可用的前提下，继续把快捷召唤/快捷助手链路做成真正接近 uTools 的桌面端体验**

---

## 1. 固定执行原则

1. **严格按后端 → preload/IPC → 前端 顺序推进**。
2. **不做展位代码**：未打通底层链路前，不在前端硬写临时逻辑糊过去。
3. **彻底脱离 uTools**：所有 `utools.xxx` 能力都必须由 Electron / Node.js 原生能力替代。
4. **优先语义定位**：先用 `fast_context_search` 找入口和调用链，再按需读取文件。
5. **每个子步骤都要验证**：默认以 `pnpm build` 为基线，必要时补运行态验证。
6. **每个子步骤验证通过后都要 Git 存档**：优先做**定向提交**，避免把无关改动混进同一 commit。
7. **进度文档必须实时维护**：任务状态以本文件为准，不再依赖零散会话日志。

---

## 2. 项目结构映射（原插件 → 桌面端）

| 原项目模块 | 桌面端对应 | 当前判断 |
| --- | --- | --- |
| `backend/src/data.js` / `file.js` / `chat.js` / `mcp.js` / `skill.js` / `preload.js` | `main/core/*` + `main/ipcHandler.js` + `preload/*` | `[x]` 主体已迁移 |
| `Anywhere_main` | `render/main` | `[x]` 主体已重构完成 |
| `Anywhere_window` | `render/window` | `[/]` 基础重构完成，继续补完整体验 |
| `Fast_window/fast_input.html` | `render/fast_window` | `[/]` 当前是轻量转发器，不是完整 fast_input 复刻 |
| `Fast_window/append_selector.html` | `render/quick` + 后续独立 append selector | `[/]` quick 已成型，append selector 独立形态仍待补 |
| uTools 全局唤起 / 托盘 / 快捷键能力 | `main/index.js` + `main/shortcutManager.js` + `windowManager.js` | `[/]` 基础版已打通 |

---

## 3. 当前状态总览（以当前源码为准）

### 3.1 总体阶段判断

- `[x]` **P0 后端底座**：DB、文件、配置、系统能力、MCP、Skill、窗口管理、事件总线、IPC 主链均已落地。
- `[x]` **P1 Anywhere_main 主控台**：主控台、设置、Providers、MCP、Skills、Prompts、Tasks、Chats 已完成桌面端重构。
- `[/]` **P2 Anywhere_window 独立对话窗口**：基础重构完成、当前可用；后续主要补 UI/交互/多模态体验与完整复刻细节。
- `[/]` **P3 Quick 快捷召唤**：基础版已明显超出“空壳”，已具备真实可用的桌面端召唤能力，但仍未完全达到原插件体验。
- `[ ]` **P4 Append Selector 独立追加选择器**：原插件存在独立形态，桌面端尚未完整对齐为独立成品。
- `[/]` **P5 桌面端运行时能力**：全局快捷键、托盘、main/quick 显隐与基础运行时已打通，后续继续收口。
- `[ ]` **P6 打包与分发**：仍未进入系统化收尾阶段。

### 3.2 当前一句话结论

- `anywhere_main`：**已完成**。
- `anywhere_window`：**基础重构完成并可用**，后续做“完整体验补齐”，不是从零开始。
- `quick`：**基础版已成型且已做多轮修复**，当前真正需要的是“继续按后端到前端做体验收口和功能补齐”，不是重新搭骨架。
- `fast_window`：当前更像**轻量转发器/过渡层**，不等于原插件的完整 `fast_input.html` 复刻。

---

## 4. 已完成模块（当前可认可结论）

### 4.1 后端底座 `[x]`

已完成并已在桌面端存在明确代码落点：

- `main/dataConverter.js`
- `main/core/db.js`
- `main/core/data.js`
- `main/core/file.js`
- `main/core/chat.js`
- `main/core/mcp.js`
- `main/core/mcp_builtin.js`
- `main/core/skill.js`
- `main/core/system.js`
- `main/core/webdav.js`
- `main/eventBus.js`
- `main/ipcHandler.js`
- `main/windowManager.js`
- `preload/main_preload.js`
- `preload/window_preload.js`
- `preload/fast_preload.js`
- `preload/quick_preload.js`

已具备能力：

- 本地配置/文档/附件存储
- 文件读写与路径处理
- 系统剪贴板 / 选择内容捕获 / 桌面源读取
- MCP / Skill / Agent 基础后端链路
- 窗口创建、显隐、置顶、关闭、事件投递
- 多窗口初始化载荷注入 `window:init`
- 全局快捷键与托盘基础运行时

### 4.2 Anywhere_main 主控台 `[x]`

桌面端 `render/main` 已具备：

- 主控台 App 外壳
- `Setting.vue`
- `Providers.vue`
- `Mcp.vue`
- `Skills.vue`
- `Prompts.vue`
- `Tasks.vue`
- `Chats.vue`
- 样式、主题、i18n、导入导出、WebDAV 主进程化

**结论**：`Anywhere_main` 可视为当前桌面端主线中**已完成模块**。

### 4.3 Anywhere_window 独立对话窗口 `[/]`

桌面端 `render/window` 已存在真实可用实现：

- `App.vue`
- `components/TitleBar.vue`
- `components/ChatHeader.vue`
- `components/ChatInput.vue`
- `components/ChatMessage.vue`
- `components/ModelSelectionDialog.vue`

当前已完成的核心链路：

- `window:init` 初始化载荷注入
- `window:event-bus` 事件总线接收
- 历史会话恢复
- 任务打开/召唤打开/追加消息基础链路
- 配置更新同步
- 窗口尺寸、位置、缩放、置顶等基础能力
- 关闭链路稳定性修复
- Agent 窗口工具链基础读写能力

**结论**：不是“只开始了一点点”，而是**基础重构已完成**；下一步主要是继续补“完整复刻体验”。

### 4.4 Quick 快捷召唤基础版 `[/]`

当前桌面端 quick 主线落点：

- `main/index.js`：快捷键、quick 唤起、托盘入口
- `main/shortcutManager.js`：全局快捷键注册与冲突校验
- `preload/quick_preload.js`：quick 专属桥接层
- `render/quick/App.vue`：快捷召唤前端主体

当前 quick 已具备的基础能力：

- 全局快捷键唤起
- quick 单例窗口
- 剪贴板文本/图片/文件读取
- best-effort 当前选中内容捕获
- 快捷助手候选生成与排序
- 拼音 / 首字母名称匹配
- 图片 / 文件 / 多行文本 token 化展示
- `enable=false` 状态同步
- main / quick 可见性副作用收口
- 通过 `showMode` 分流到 `window` / `fast`

**结论**：quick 已不是“空壳”，而是**已形成真实可用基础版**。

### 4.5 fast_window 当前定位 `[/]`

桌面端 `render/fast_window/App.vue` 当前定位：

- 接收 `window:init`
- 显示一个轻量提示卡片
- 把 payload 转发给独立 `window`
- 自己关闭/隐藏

**结论**：

- 当前 `fast_window` 更像**轻量转发器**
- **不是**原插件 `Fast_window/fast_input.html` 的完整复刻
- 这部分后续要么继续产品化，要么明确降级为中转层，不再混淆定位

### 4.6 桌面端运行时 `[/]`

已具备：

- `Ctrl+Space` / `Alt+X` 等快捷键注册
- 桌面端快捷键配置录制与热更新
- Tray 创建与菜单
- main close-to-tray 基础行为
- quick / main 的显隐控制与副作用修复

---

## 5. 当前未完成 / 未完全对齐原插件的部分

### 5.1 Anywhere_window 仍待补齐

- 自定义标题栏的完整平台体验
- Markdown/代码块/工具卡片的完整交互收口
- 输入区 `@MCP` / `/Skill` 的完整体验与边界处理
- 图片查看、导出长图、背景图与毛玻璃细节
- 语音/录音/设备能力
- 完整运行态回归验证

### 5.2 quick 仍待改进

- 当前 quick 虽可用，但仍未完全达到原插件“快捷召唤 + 追加选择 + fast 输入”的整体体验
- `append selector` 仍未作为**独立成品**完成对齐
- `fast_input` 完整视觉与行为仍未原样复刻
- quick / fast / append 三者职责边界需要继续明确和收口

### 5.3 桌面端收尾仍待推进

- 打包、安装、分发
- 打包后 `userData` 路径读写回归
- 端到端桌面体验复测

---

## 6. 当前已知问题 / Notes

- `main/core/mcp.js` 仍存在“静态与动态同时引入”的**非阻塞构建告警**。
- 独立对话窗口虽已基本可用，但仍有一部分“完整复刻体验”尚未进入系统化收尾。
- 当前 quick 是主要快捷召唤入口；`fast_window` 不是完整 fast_input 等价物，这一点后续实现时必须始终保持清晰。
- 文档后的历史会话里有大量 quick 修复记录，但主线判断必须以**当前源码状态**为准。

---


### `[x]` Q_web_proxy 后端：统一主进程外网请求代理行为

**目标**

- 修复 `web_search` / `web_fetch` 在桌面端主进程未正确遵循系统/本地代理的问题。
- 优先让主进程外网请求默认遵循系统代理；若系统代理不可直取，至少兼容本地代理环境变量。
- 避免把“联网失败/代理未生效”误报成“无搜索结果”。

**重点文件**

- `main/core/mcp_builtin.js`
- `main/core/chat.js`
- 必要时补充 `main/core/*` 公共网络层

**完成标准**

- `web_search`、`web_fetch` 默认通过统一代理决策发起请求。
- 能区分“真正无结果”与“网络失败/代理失败”。
- 不破坏现有模型/工具调用链。

**验证方式**

- 用真实查询词验证 `web_search`
- 必要时验证 `web_fetch`
- `pnpm build`

- `[x]` 已定位主进程直接外网请求点：`mcp_builtin.js`、`chat.js`、`data.js`
- `[x]` 已新增 `main/core/net.js`，统一使用 Electron `net.fetch`（Chromium 网络栈）以默认遵循系统代理/默认 Session 代理策略
- `[x]` 已接入 `web_search`、`web_fetch`、`read_file(HTTP/HTTPS)`、`cacheBackgroundImage`、`listProviderModels`
- `[x]` 执行 `pnpm build` 验证
- `[x]` 通过后执行 Git 存档

- `[x]` 已补充 `web_search` 错误语义：请求失败时明确返回网络/代理错误，不再误报为“无结果”
- `[x]` 本轮验证结果：`pnpm build` 通过；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本次阻塞项）

- `[x]` 运行态验证：使用 Electron `net.fetch` 直接请求 DuckDuckGo 成功返回 `status=200`，确认主进程已可遵循系统代理链路
- `[x]` 结论：桌面端此前 `web_search` 异常的根因是主进程 Node 网络栈未正确遵循系统/本地代理；现已切换到 Electron Chromium 网络栈

**建议 commit**

- `fix(network): make main-process web requests honor proxy settings`

---

## 7. 接下来必须执行的主线（严格按后端 → preload → 前端）

> 以下是**下一阶段真正要做的任务顺序**。未完成上一项前，不跳步做下一项前端细节。

### `[/]` Q0 文档整理与基线确认

- `[x]` 充分阅读 `Anywhere_desktop` 与 `Anywhere` 的关键模块与入口。
- `[x]` 重新整理 `进度.md`，改为“当前状态 + 主线任务 + 历史摘要”的结构。
- `[x]` 执行一次基线验证：`pnpm build`。
- `[ ]` 验证通过后做文档专属 Git 存档。

**验证方式**

- `pnpm build`
- 文档中的状态与源码结构交叉一致

**建议 commit**

- `docs(progress): 重构进度文档为当前阶段看板`

---

### `[/]` Q1 后端：统一 quick 召唤协议与自动上传源

**目标**

- 让 quick 成为真正“即用即走”的快捷路由入口。
- 统一 quick 打开时的 payload 协议，明确文本 / 跨行文本 / 图片 / 文件的标准化表示。
- 打开 quick 时，优先使用“当前选中内容”；若未捕获到选中内容，则回退到“最近 5 秒内复制的内容”；再回退到当前剪贴板内容。
- quick 始终显示在**当前鼠标所在显示屏正中央**，并支持单例快速显隐。

**重点文件**

- `main/index.js`
- `main/windowManager.js`
- `main/ipcHandler.js`
- `main/core/system.js`

**完成标准**

- quick 打开位置稳定在当前鼠标所在屏幕中心。
- quick 热键再次触发时立即隐藏。
- quick 初始 payload 能正确区分文本 / 跨行文本 / 图片 / 文件，且 `svg` 归为文件而非图片。
- 当前选择内容 / 最近 5 秒复制内容 / 当前剪贴板三者优先级明确且稳定。

**验证方式**

- `pnpm build`
- 手工验证：快捷键反复呼出 / 隐藏、跨屏打开位置、文本 / 跨行文本 / 图片 / 文件自动上传

**建议 commit**

- `refactor(quick): unify summon payload and auto-upload sources`

---

### `[ ]` Q2 preload / IPC：收口 quick 的显隐、粘贴与上下文桥接

**目标**

- 让 quick 的 preload / IPC 语义稳定，只暴露 quick 真实需要的能力。
- 支持 quick 以“隐藏”而非销毁的方式快速关闭，提高打开/关闭速度。
- 收口 quick 的粘贴上传能力与窗口上下文获取能力。

**重点文件**

- `preload/quick_preload.js`
- `main/ipcHandler.js`
- `main/windowManager.js`

**完成标准**

- quick 可通过桥接层稳定执行 `open / hide / close-context / clipboard-read / selection-capture / file-path-resolve`。
- quick 关闭主路径改为“立即隐藏”，避免频繁销毁导致的体感迟缓。
- quick 打开时粘贴文本 / 图片 / 文件行为稳定。

**验证方式**

- `pnpm build`
- 手工验证：Esc 关闭、再次呼出、粘贴图片 / 文件 / 多行文本

**建议 commit**

- `refactor(quick): streamline hide and paste bridge semantics`

---

### `[ ]` Q3 前端：完成 quick 的匹配规则与自动路由

**目标**

- 实现与需求一致的 quick 匹配策略：名称匹配、文本类型匹配、图片类型匹配、文件类型匹配、通用类型兜底。
- 支持名称的全名 / 首字母 / 拼音（大小写无关）匹配。
- 对“文本 / 跨行文本 / 图片 / 文件 / 附件后二次输入”分别采用正确的排序与二次筛选规则。

**重点文件**

- `render/quick/App.vue`

**完成标准**

- 文本输入时：名称匹配 > 文本类型匹配 > 通用类型。
- 图片输入时：名称匹配 > 图片类型匹配 > 通用类型。
- 文件输入时：名称匹配 > 文件类型匹配 > 通用类型。
- 跨行文本按附件处理，并参与文本类型匹配。
- 已有附件后继续输入时，仅在首次附件匹配结果中做名称二次筛选。

**验证方式**

- `pnpm build`
- 手工验证：文本、跨行文本、图片、文件、附件后二次输入、`svg` 文件

**建议 commit**

- `feat(quick): implement smart matching and auto routing`

---

### `[ ]` Q4 前端：完成 quick 的键盘行为与即用即走体验

**目标**

- 让 quick 的键盘行为严格符合“即用即走”的交互预期。
- quick 显示时，如果输入框存在文本/附件，按 `Backspace` 或 `Esc` 立即清空内容。
- quick 显示时，如果输入框没有内容，按 `Esc` 立即隐藏 quick。
- 输入框保持自动聚焦，并保证回车打开目标助手稳定。

**重点文件**

- `render/quick/App.vue`

**完成标准**

- `Backspace` / `Esc` 清空优先级正确。
- 无内容 `Esc` 立即隐藏 quick。
- quick 打开后输入焦点稳定。
- 打开目标助手后 quick 立即隐藏，不拖泥带水。

**验证方式**

- `pnpm build`
- 手工回归：Esc、Backspace、Enter、上下键、重复呼出、打开后立即消失

**建议 commit**

- `feat(quick): refine ephemeral keyboard behavior`

---

### `[ ]` Q5 前端：对齐 uTools 风格的 quick 外观

**目标**

- 让 quick 更接近 uTools 召唤栏：简洁、聚焦、轻量、深浅色统一。
- 优化输入栏、附件 token、推荐区与候选项的视觉层级。
- 提升 quick 打开/关闭时的速度感与完成度。

**重点文件**

- `render/quick/App.vue`
- `render/quick/assets/main.css`

**完成标准**

- quick 视觉层级简洁清晰，接近 uTools 召唤栏风格。
- 深色 / 浅色模式随设置切换稳定。
- 附件展示、候选项选中态、信息密度与留白合理。

**验证方式**

- `pnpm build`
- 手工验证：深浅色切换、打开关闭体感、附件展示、候选高亮

**建议 commit**

- `style(quick): align summon bar with utools-like appearance`

---

### `[ ]` Q6 前端：继续补 Anywhere_window 的完整体验

**目标**

- 在 quick/append 主线明确后，继续进入独立窗口完整复刻

**重点内容**

- 标题栏完整体验
- Markdown/代码块/工具卡片
- 输入区 `@` / `/` 体验
- 图片查看与长图导出
- 音频与设备能力

**验证方式**

- `pnpm build`
- 必要运行态联调

**建议 commit**

- 按子模块拆分提交，不做大杂烩提交

---

### `[ ]` Q7 集成收尾

**目标**

- 做完整桌面端回归
- 再进入打包与分发

**重点内容**

- 快捷键 / 托盘 / quick / window / main 联调
- 数据路径、历史会话、文件发送、多窗口追加链路联调
- 打包配置与安装验证

**验证方式**

- `pnpm build`
- 打包后运行验证

---


### 15. 2026-03-31 本轮进行中（quick 召唤问题修复） `[x]`

- `[x]` 目标：修复 quick 的选中/复制源优先级、文件自动上传、跨行文本保真输入，以及快捷助手选择栏信息密度过低的问题。
- `[x]` Step A：已修正 quick 打开前的内容采集时机与优先级，改为优先当前选中内容，失败后再回退最近复制内容/当前剪贴板。
- `[x]` Step B：已统一 quick payload 语义，补齐跨行文本 `multiline-text` 独立类型，避免在打开助手时丢失换行语义。
- `[x]` Step C：已调整 quick 前端附件注入与路由逻辑，确保文件/图片/跨行文本都能正确带入目标助手。
- `[x]` Step D：已优化 quick 候选项 UI，放大图标与字体，并缩小单项宽度提高信息密度。
- `[x]` 验证：`pnpm build` 通过（2026-03-31）
- `[x]` 验证通过后已执行 Git 定向提交存档：`2d19aff fix(quick): prioritize selection and preserve multiline payloads`
- `[x]` 追加修复：已补齐目标窗口对 `multiline-text` 的落地语义，改为预填充到输入框，不再与普通单行文本走同一发送链。
- `[x]` 追加修复：已增强 Windows 文件剪贴板解析与 quick 粘贴处理，补齐多文件读取路径与多项附件注入链路。
- `[x]` 第二轮验证：`pnpm build` 再次通过（2026-03-31）
- `[x]` 第二轮修复已执行 Git 定向提交存档：`da13337 fix(quick): support multiline input and multi-file clipboard payloads`
- `[x]` 新增收口：已优先处理 quick 打开速度回退与“复制多个文件后作为多附件上传”的主链问题；“纯选中未复制后自动上传”暂降级为后续难点。
- `[x]` 第三轮修复：quick 唤起改回“先显示、后异步注入剪贴板快照”，收口 Alt+X 打开体感变慢的问题。
- `[x]` 第三轮修复：复制多个文件后，quick 现已按多附件展示，并在选择快捷助手后把全部附件一并传入目标窗口。
- `[x]` 第三轮验证：`pnpm build` 通过（2026-03-31）
- `[ ]` 第三轮修复待执行 Git 定向提交存档


- `[x]` 备注：本轮仍保留既有 `main/core/mcp.js` 动态/静态引入构建告警，非本次 quick 修复阻塞项。

---

## 8. 提交规则（执行时必须遵守）

### 8.1 每一步的固定节奏

1. 用 `fast_context_search` 找目标调用链
2. 只读必要文件
3. 先改后端，再改 preload，再改前端
4. 完成后立刻 `pnpm build`
5. 验证通过后立刻更新本文件
6. 只把本步骤相关文件做**定向 Git 提交**

### 8.2 禁止事项

- 禁止把多个无关修复混进同一次 commit
- 禁止前端先写“看起来像完成了”的假逻辑
- 禁止在未核对原插件实现前擅自改变交互语义

---

## 9. 当前最近验证结果

- `[x]` `pnpm build` 通过（2026-03-26，本轮整理文档基线验证）
- `[x]` 当前项目仍可构建出多入口产物：`main` / `window` / `fast_window` / `quick`
- `[x]` 当前 quick 主入口、window 主体与 main 主控台在源码层均存在稳定落点
- `[ ]` 本轮文档整理提交待执行
- `[x]` 本轮文档整理提交已完成：`42eb079 docs(progress): 重构进度文档为当前阶段看板`


---

## 10. 本轮整理结论（给后续会话看的摘要）

1. **Anywhere_main 已完成**，后续不是主战场。
2. **Anywhere_window 已完成基础重构并可用**，后续做完整体验补齐。
3. **quick 已经是当前桌面端真正的快捷召唤入口**，不是空壳。
4. **fast_window 目前只是轻量转发器，不是完整 fast_input 复刻。**
5. **append selector 独立成品仍缺失**，这是 quick 继续改进时必须正视的缺口。
6. 之后的开发必须继续按：
   **后端路由协议 → preload 收口 → quick 前端优化 → append selector 独立实现 → window 体验补齐**

---


### 12. 2026-03-26 本轮进行中（web_search 桌面端 0 结果 / 独立窗口背景图闪烁） `[/]`

- `[x]` Step A 已完成：按用户要求，已将桌面端 `web_search` 回退恢复到本轮开始前的原始 DuckDuckGo 实现，不再保留 `Electron net` / `net.fetch` / Bing 回退 等新增改造链路，避免继续引入额外错误。
- `[x]` Step B 已完成：独立窗口背景图闪烁修复保留，窗口背景仍采用 ready-state 显示：只有背景图真实预加载完成后才挂载 `has-bg` / `window-bg-layer`，并收短过渡时间，避免打开瞬间闪白/闪黑。
- `[x]` Step C 已完成：`pnpm build` 通过（2026-03-26，本轮 web_search 回退 + 独立窗口背景闪烁修复保留）。
- `[x]` Step D 已完成：本轮回退 Git 存档已完成：`fix(web-search): revert desktop web_search changes`


---

### 13. 2026-03-26 本轮进行中（同步插件端 3 个 commit 到桌面端） `[/]`

- `[x]` Step A 已完成：完成插件端 `0b5a84d` / `ff0f347` / `2d8f8e6` 三个 commit 的差异分析，并映射到桌面端对应模块：`render/main/components/Chats.vue`、`render/main/components/Tasks.vue`、`render/window/App.vue`、`main/core/mcp_builtin.js`。
- `[x]` Step B 已完成：确认插件端 `0b5a84d`（英文环境下恢复对话失败）在桌面端当前实现中已被等价规避——桌面端主控台与任务页恢复对话已改为直接 `openWindow('window', { code, type: 'over', payload, filename })`，不再依赖本地化标题文本匹配，因此无需机械迁移旧修复。
- `[x]` Step C 已完成：同步插件端 `2d8f8e6` 到桌面端 `main/core/mcp_builtin.js`，补齐 `summon_agent` / `continue_agent_chats` 的 `file_paths` 参数安全提示，并通过桌面端 MCP 初始化链路把当前窗口 Agent 名称注入到 `list_agents` 描述中。
- `[x]` Step D 已完成：同步插件端 `ff0f347` 到桌面端 `render/window/App.vue`，补齐发送前并发锁 `isPreparingSend`、生成中保存拦截，以及恢复会话时对 `history/chat_show` 的自愈对齐与尾部脏 assistant 节点清理。
- `[x]` Step E 已完成：按步骤执行 `pnpm build` 验证，并在验证通过后做定向 Git 提交存档。
- `[x]` Step E.1 已完成：本轮后端同步后执行 `pnpm build` 通过；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。
- `[x]` Step E.2 已完成：本轮前端同步后再次执行 `pnpm build` 通过；当前 3 个插件端 commit 在桌面端的必要同步已完成，其中 `0b5a84d` 以“桌面端已等价规避，无需迁移旧链路”的方式完成对齐。


---

### 14. 2026-03-26 本轮进行中（quick 重构为即用即走召唤栏） `[/]`

- `[x]` Step A 已完成：重排 `进度.md` 中的 quick 主线任务，把目标明确为“后端自动上传源统一 → preload 收口 → 匹配与自动路由 → 键盘行为 → uTools 风格外观”。
- `[x]` Step B 已完成：后端统一 quick 初始 payload 的基础规则，明确文本 / 图片 / 文件的标准化载荷，并保留“最近 5 秒剪贴板内容”回退链路；当前 `svg` 不再走图片直传分支。
- `[x]` Step C 已完成：quick 单例复用时会重新计算并应用当前鼠标所在显示屏中心位置，不再停留在旧屏幕位置。
- `[x]` Step D 已完成：quick 关闭主路径由销毁改为隐藏，提升重复呼出/关闭的体感速度。
- `[x]` Step E 已完成：执行 `pnpm build` 通过（2026-03-26，本轮 quick 后端 Step 1）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。
- `[x]` Step F 已完成：继续收口 quick preload / IPC 与前端键盘行为、自动路由、uTools 风格外观。
- `[x]` Step F 已完成：quick 前端已改为“内容优先”交互——打开时文本/跨行文本/图片/文件会作为附件或上传内容接入，`Esc` / `Backspace` 在有内容时优先清空、无内容时再隐藏 quick。
- `[x]` Step G 已完成：quick 匹配规则按“名称匹配（全名/首字母/拼音，不区分大小写）→ 类型匹配 → 通用类型”收口；已有附件后继续输入时，会在附件匹配结果中做名称二次筛选。
- `[x]` Step H 已完成：粘贴上传行为已收口，支持文本 / 跨行文本 / 图片 / 文件；`svg` 按文件处理，不再归入图片。
- `[x]` Step I 已完成：quick 外观已收紧为更接近 uTools 召唤栏的轻量样式，统一深浅色主题与候选项视觉层级。
- `[x]` Step J 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 前端 Step 2）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step K 已完成：去掉 quick 外层实体大框，窗口改为透明背景承载，仅保留召唤栏主体；候选区限制为 3 行并隐藏滚动条，超出部分直接截断不显示。
- `[x]` Step L 已完成：修复“手动复制文件/图片后 Alt+X 未自动上传”的问题——若最近 5 秒剪贴板已有有效内容，则 quick 直接复用，不再先发送模拟 `Ctrl+C` 冲掉用户刚复制的文件/图片。
- `[x]` Step M 已完成：修复 Alt+X 打开 quick 的明显延迟；当前优先使用新鲜剪贴板内容瞬时返回，仅在确实没有新鲜内容时才尝试选中捕获。
- `[x]` Step N 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 外观/自动上传/性能热修复）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step O 已完成：第二轮重构 quick 打开时序，改为“先显示空 quick，再异步补载最新剪贴板/选中内容”，显著降低图片/文件导致的首屏阻塞与 Alt+X 体感延迟。
- `[x]` Step P 已完成：加固 Windows 文件剪贴板解析，补充 `CF_HDROP` / `FileNameW` / `text/uri-list` 与文本路径回退解析，提高复制文件后自动上传命中率。
- `[x]` Step Q 已完成：加强 quick 焦点获取与置顶逻辑，打开与复用时同步 `focus + webContents.focus + moveTop`，修复“Alt+X 后未聚焦导致 Ctrl+V 落回原窗口”的问题。
- `[x]` Step R 已完成：调整 quick 热键显隐条件，仅在 quick 可见且已聚焦时再次按热键才隐藏，修复“失焦关闭后第一次 Alt+X 无反应、第二次才出现”的状态错误。
- `[x]` Step S 已完成：quick 模板与样式改成更接近 uTools 的扁平结构，去掉外层 panel 感，仅保留输入框与下方快捷助手栏，缩小图标/文字并限制 3 行展示。
- `[x]` Step T 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 第二轮修复）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step U 已完成：第三轮修复 quick 的显示时序，取消“二次 openWindow 重新打开 quick”的做法，改为首次显示后直接向可见 quick 推送 payload，修复“出现→消失→再出现”的闪烁问题。
- `[x]` Step V 已完成：quick 改为圆角矩形容器，并通过 `-webkit-app-region` 支持拖动；输入框、附件与候选项区域保持可交互。
- `[x]` Step W 已完成：placeholder 文案改为 Anywhere Desktop 语义，不再照抄 uTools。
- `[x]` Step X 已完成：进一步收紧快捷助手信息密度，缩小图标、文字、间距，使同屏排序更紧凑。
- `[x]` Step Y 已完成：已按用户允许清理 `.git/index.lock`，解除 git 提交阻塞。
- `[x]` Step Z 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 第三轮修复）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step AA 已完成：补齐 quick `index.html` 的 `html/body/#app` 透明与 margin reset，去掉浏览器默认外边距导致的最外层白边/套壳。
- `[x]` Step AB 已完成：调整 quick 输入框一行作为拖拽热区，允许用户在输入框所在横条区域按住拖动窗口，同时保留输入框与卡片点击交互。
- `[x]` Step AC 已完成：继续压缩 quick 宽度与布局密度，减少最外层空白边缘感，让圆角矩形主体更直接地呈现。
- `[x]` Step AD 已完成：本轮确认文件自动上传链路暂缓，优先收口样式与交互；hover 卡顿进入专项优化清单。
- `[x]` Step AE 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 第四轮样式/拖拽修复）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step AF 已完成：修复当前 quick 三个体验回归——`Alt+X` 打开仍有白边、输入框所在横条拖拽命中不一致、首项“博客”默认高亮被误判为 hover。
- `[x]` Step AG 已完成：本轮修复后执行 `pnpm build` 验证，通过；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step AF.1 已完成：取消候选列表“无操作时自动选中首项”的逻辑，只在键盘导航/鼠标进入/显式 `promptKey` 注入时显示 active 样式，消除“博客一直 hover”的误判。
- `[x]` Step AF.2 已完成：把 quick 输入框所在搜索横条恢复为主拖拽热区，仅输入框本体和卡片区域保持 `no-drag` 交互，收口拖动命中不一致问题。
- `[x]` Step AF.3 已完成：将 quick 主体改为近乎贴满透明窗口的圆角容器，减少透明边缘暴露，收口 Win11 下 `Alt+X` 打开时的白边感。
- `[x]` Step AH 已完成：验证通过后已执行本轮 quick 定向 Git 提交存档。

- `[x]` Step AH.1 已完成：本轮 quick 回归修复已定向提交：`1ca9eba fix(quick): remove phantom active state and tighten summon shell`。

- `[x]` Step AI 已完成：把 quick 从“透明矩形窗口 + 圆角内容块”改为“非透明原生圆角窗口壳 + 隐藏标题栏内容区”，窗口层不再依赖透明背景伪装圆角。
- `[x]` Step AJ 已完成：同步收口 quick 候选区底部留白，通过降低窗口高度与内容区贴边布局，让上下留白更接近一致。
- `[x]` Step AK 已完成：完成窗口壳与布局调整后重新执行 `pnpm build` 验证，通过；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step AI.1 已完成：`main/windowManager.js` 中 quick 窗口改为 `transparent: false + roundedCorners: true + titleBarStyle: 'hidden' + hasShadow: true`，改走原生圆角窗口壳路线。
- `[x]` Step AI.2 已完成：quick 主窗口背景色改为深色实体背景，避免 Win11 对透明无边框窗口的边缘合成把外层矩形壳感露出来。
- `[x]` Step AJ.1 已完成：`render/quick/App.vue` 中 quick 内容区改为 `width: 100% / min-height: 100% / padding: 14px / justify-content: flex-start`，直接贴合窗口内容区。
- `[x]` Step AJ.2 已完成：quick 默认高度由 `420` 收口到 `378`，减少候选区下沿多余空白。
- `[x]` Step AL 已完成：验证通过后已执行本轮定向 Git 提交存档。

- `[x]` Step AL.1 已完成：本轮 quick 原生圆角窗口壳修复已定向提交：`76c9218 fix(quick): use native rounded window shell`。






## 11. 历史说明

旧版 `进度.md` 中包含大量 2026-03-24 ~ 2026-03-25 的会话级流水记录、热修日志和细粒度修复条目。此次整理后：

- **本文件只保留当前状态、主线计划与必要结论**；
- 更细的单次修复细节以 Git 历史和后续定向提交为准；
- 若后续需要追某个历史修复点，优先看对应 commit，而不是继续把流水账堆回主文档。
