# 🚀 AI Anywhere Desktop 重构进度表（整理增强版 / 基准：2026-03-13）

> **整理原则（不删减内容，只重排与去重表达）：**
>
> 1) 所有阶段/子任务/验证/根因/修复/提交号/用户反馈 **全部保留**；
> 2) 将重复出现的“会话对齐/基线通过/下一步计划”合并为“统一总览 + 按时间日志”；
> 3) 用“✅已完成 / 🟡进行中 / ⏳待开始”统一状态口径，避免同一事项在不同位置出现冲突。

---

## 0. 项目信息（固定锚点）

- 原项目地址：`E:\Programming\Anywhere`
- 软件项目地址：`E:\Programming\Anywhere_desktop`

---

## 1. 核心原则重申（原文保留）

1. **拒绝展位代码**：严格按顺序开发，底层能力（如数据库、窗口管理）未实现前，绝不进行前端 UI 的硬绑定。
2. **彻底脱离 uTools**：将所有 `utools.xxx` API 替换为 Electron 原生或 Node.js 实现。
3. **前后端数据转换**：严格管控 IPC 通信前后的数据序列化与反序列化（如 `File` 对象、`Buffer`、`Error` 等）。

---

## 2. 当前全局状态总览（把分散信息汇总到一处）

### ✅ 已稳定完成并通过构建（多次复核）

- Phase 0.1 多入口基础设施（main/window/fast_window）可用
- Phase 0.2 `main/dataConverter.js` 核心数据转换器可用
- Phase A：A.1（DB）/ A.2（WindowManager+EventBus）/ A.3（System API）/ A.4（data/file/chat/mcp/skill 迁移）/ A.5（Preload 隔离 + 窗口控制）均已落地并 `pnpm build` 通过
- Phase B：
  - B.1（前端环境与公共资源挂载）关键步骤已完成（Element Plus + i18n + base.css/main.css 迁移）
  - B.2（Setting 适配：本地导入导出 + 主题 + memory 同步 + WebDAV 主进程化）已完成
  - B.3（Providers/Mcp/Skills/Prompts/Tasks/Chats）已完成并接入主控台导航
  - B.4（主控台样式深度复刻修复）已完成多轮热修与 CSP 收尾（见日志）

### ⏳ 待开始（后续主线）

- Phase C：Anywhere_window 独立对话窗口复刻（进行中：C.1-1 已完成）
- Phase D：Fast_window 快捷唤出面板重构
- Phase E：桌面端专属体验优化（全局快捷键、划词、托盘、打包分发等）

---

## 3. 关键验证结论（集中放置，避免到处重复）

- ✅ `pnpm build` 已通过（`E:\Programming\Anywhere_desktop`）
- 输出产物包含：
  - `out/main/index.js`
  - `out/preload/main_preload.js` / `window_preload.js` / `fast_preload.js`
  - `out/renderer/main/index.html` / `window/index.html` / `fast_window/index.html`
- 结论：Phase 0.1 多入口基础设施可用。

---

## 4. 阶段 0：项目初始化与基础设施 (Phase 0)

### 0.1 基础环境搭建 ✅

- [X] 初始化 `pnpm` 工作区，安装 `electron`、`vite`、`vue` 等核心依赖。
- [X] 划分目录结构：`main/` (主进程), `preload/` (预加载), `render/` (渲染进程)。
- [X] 配置 `vite.config.js`，支持多入口（`main/index.html`, `window/index.html`, `fast_window/index.html`）。
- [X] 配置 Electron Builder / Electron Packager 的基础打包脚本。

**初始化脚手架命令记录（原文保留）：**

```powershell
PS E:\Programming> pnpm create @quick-start/electron Anywhere_desktop
.../19cdd49b4ca-5abc                     |   +8 +
.../19cdd49b4ca-5abc                     | Progress: resolved 8, reused 8, downloaded 0, added 8, done
√ Package name: ... anywhere-desktop
√ Select a framework: » vue
√ Add TypeScript? ... No / Yes
√ Add Electron updater plugin? ... No / Yes
√ Enable Electron download mirror proxy? ... No / Yes

Scaffolding project in E:\Programming\Anywhere_desktop...

Done. Now run:

  cd Anywhere_desktop
  pnpm install
  pnpm dev
```

### 会话切换快照（2026-03-11）✅（原信息集中归档）

- ✅ 你已确认：当前无报错，主/窗/快窗口均可打开；界面仅骨架按钮（符合 Phase 0.1 预期）。
- ✅ 本地 Git 存档已完成（未 push）：
  - `9e463c9 fix(phase0.1): 修复空白窗口渲染报错并稳定三窗口骨架`
- 🔜 下一会话首要任务（Phase 0.2）：
  1. 新建 `main/dataConverter.js`
  2. 实现 `File -> Path/Buffer` 序列化转换
  3. 实现 `Error` 标准化序列化
  4. 实现 `safeClone`（JSON 安全深拷贝）
  5. 在主进程 IPC 层接入 converter 并完成 `pnpm build` 验证

### 验收修复记录（2026-03-11）✅（原文信息保留）

- 🐞 问题：主窗口空白，DevTools 报错：`Property "window" was accessed during render but is not defined on instance`。
- 根因：在 Vue 模板中直接访问 `window.api`，模板渲染上下文中不允许直接访问全局 `window`。
- 修复：
  - `render/main|window|fast_window/App.vue` 改为在 `<script setup>` 中声明 `appWindowType`，模板只使用响应式变量。
  - `preload/*_preload.js` 的 `openWindow` 改为 `electronAPI.ipcRenderer.invoke(...)`，避免依赖 `window.electron`。
- ✅ 再验证：`pnpm build` 通过。

### 验证记录（2026-03-11 / 2026-03-12）✅

- ✅ `pnpm build` 通过（2026-03-12）：dataConverter 初始版本构建成功。
- 📝 2026-03-12：用户授权 AI 执行 Git 存档；受当前系统 Git 只读策略限制，仍需人工提交。

---

### 0.2 核心数据转换器 (`main/dataConverter.js`) ✅

- [X] 实现 `File` 到 `Path` / `Buffer` 的序列化拦截处理。
- [X] 实现 Error 对象的标准化提取与传递封装。
- [X] 实现 JSON 安全的深拷贝验证工具。

---

## 5. 阶段 A：后端架构与 Preload 桥接 (Phase A)

> 复刻原后端逻辑，用 Electron 原生能力完全替代 uTools 底座功能。

---

### A.1 本地数据库重构 (`main/core/db.js`) ✅

- [X] 选型并接入本地 JSON 数据库（如 `lowdb` 或 `sqlite`）以替代 `utools.db`。
- [X] 实现文档 CRUD 操作：`put`, `get`, `remove`, `allDocs`, `bulkDocs`。
- [X] 实现附件存储机制：`postAttachment`, `getAttachment`（将 Base64/Buffer 写入本地特定缓存目录，维护元数据映射）。
- [X] 实现类 `localStorage` 接口：`dbStorage.setItem / getItem`。

**✅ A.1 子任务记录（2026-03-12 / Step A.1.1）**

- 已接入 `lowdb` 并落地 `main/core/db.js`。
- 已实现：
  - 文档 CRUD：`put/get/remove/allDocs/bulkDocs`
  - 附件：`postAttachment/getAttachment`（落盘到 `userData/attachments`）
  - 存储：`dbStorageSetItem/dbStorageGetItem/dbStorageRemoveItem/dbStorageListKeys`
  - 诊断：`isDbReady/getDbStats`
- IPC 已接入：`db:*` 与 `dbStorage:*` 通道。
- preload 已暴露 db 接口（main/window/fast 三窗口一致）。
- ✅ 验证：`pnpm build` 通过。

---

### A.2 窗口管理器重构 (`main/windowManager.js`) ✅

**目标与产出（原文要点保留、集中整理）：**

- 目标：把 `main/index.js` 中的窗口创建逻辑抽离为独立模块，形成可复用的 `windowManager`；并在 IPC 层形成统一的 invoke-handler 包装，保证返回值可序列化、异常可结构化回传。
- 产出文件：
  - `main/windowManager.js`：
    - `openWindow(type)`：`main/fast` 走单例复用；`window` 走多实例（用 `randomUUID()` 生成 id 并维护索引）。
    - 提供 `getWindowIds(type)` / `getWindowById(id)` 作为后续多窗口通信/投递的基础能力。
  - `main/ipcHandler.js`：
    - `handleInvoke(channel, handler)`：统一 try/catch；成功路径走 `serializeIpcPayload`；失败路径回 `{ok:false, error: serializeError(err)}`。
    - `registerIpcHandlers(...)`：已从单一 `window:open` 扩展到主窗口显隐、窗口列表与事件投递。
  - `main/index.js`：精简为应用生命周期 + 注册 IPC + `openWindow('main')`。
- 验证：`pnpm build` 通过；`pnpm dev` 可正常启动（renderer dev server 5173 + Electron app）。
- 约束说明：当前 `window:open` 仍返回基础信息（ok/type/id），后续做“消息投递/窗口间通信”时再引入 senderId 与更精细的路由。

**完成项（原 checklist 保留）：**

- [X] 替代 `utools.createBrowserWindow`，封装带状态的 `BrowserWindow` 创建逻辑。
- [X] 实现主控台（Main Window）的单例管理与显隐控制 (`hideMainWindow`, `showMainWindow`)。
- [X] 实现独立对话窗口（Agent Window）的多实例管理，维护 `windowMap`，注入唯一 `senderId`。
- [X] 实现快捷输入窗口（Fast Window）的无边框、透明、失去焦点自动隐藏处理。
- [X] 替代 `utools.redirect`：实现进程内事件总线（EventBus），完成窗口间的指令投递（如“追加消息”）。

**✅ A.2 子任务记录（按原 Step 顺序归档）**

- ✅ Step A.2.2：主窗口显隐能力

  - `showMainWindow()` / `hideMainWindow()`（`main/windowManager.js`）
  - IPC：`window:showMain` / `window:hideMain`（`main/ipcHandler.js`）
  - 主进程注入：`registerIpcHandlers({ openWindow, showMainWindow, hideMainWindow })`（`main/index.js`）
  - 三套 preload 暴露：`showMainWindow` / `hideMainWindow`
  - 骨架页面加入联调入口按钮（main/window/fast）
  - ✅ 验证：`pnpm build` 通过
- ✅ Step A.2.5：EventBus 投递链路

  - 新增 `main/eventBus.js`：`dispatchWindowEvent`
  - target：
    - `target: 'main' | 'fast' | '<windowId>'`（定向投递）
    - `target: 'type:window'`（按窗口类型投递）
    - `target: 'broadcast' | '*'`（全量广播）
  - IPC：
    - `window:list`
    - `window:event:emit`
  - windowManager 增强：
    - `getWindowByRef / listWindows / getWindowRefByWebContentsId`
    - 为每个窗口绑定 `webContents.id -> windowRef`
  - 三套 preload 暴露：`listWindows / emitWindowEvent / onWindowEvent`
  - 骨架页增加联调按钮与“最近收到事件”展示（追加消息式投递验证）
  - ✅ 验证：`pnpm build` 通过
- ✅ Step A.2.3：窗口初始化信号 + senderId 稳定注入

  - 注入初始化信号 `window:init`（did-finish-load 后发送）
  - 独立对话窗口提供稳定 `senderId`（`windowRef/id`），preload 通过 `getWindowContext()` 暴露
  - 三套 preload 增加：`onWindowInit` + `getWindowContext`
  - 三套骨架页展示 `senderId`（用于后续定向追问/追加消息联调）
  - ✅ 验证：`pnpm build` 通过

**🛠️ A.2 热修复记录（2026-03-12 / Close Crash，原文保留）**

- 用户反馈：关闭窗口时主进程报错 `TypeError: Object has been destroyed at unbindWindowRef`。
- 根因：`BrowserWindow` 进入 `closed` 阶段后，访问 `win.webContents` 可能抛出对象已销毁异常。
- 修复：
  - 新增 `unbindWindowRefByWebContentsId`，在窗口创建时预捕获 `webContentsId`。
  - `closed` 回调改为按预捕获 `webContentsId` 解绑映射，不再触碰已销毁对象。
- 兼容策略：主窗口显隐逻辑保留为“可选能力”，不将“打开对话前必须隐藏主窗口”作为桌面版强制策略。
- 体验修正：`hideMainWindow` 语义从“直接隐藏”改为“最小化主窗口”，避免主窗口彻底不可见难以找回。

**补充说明（原文保留）：**

- A.2 核心子项均已达成（窗口创建复用、主窗口显隐、Fast 失焦隐藏、EventBus、senderId 注入）；后续将把窗口控制能力（最小化/最大化/置顶切换）在 A.5 里以稳定 API 形式补齐。

---

### A.3 系统级 API 重写 (`main/core/system.js`) ✅

- [X] 剪贴板：基于 `electron.clipboard` 实现 `copyText`、`copyImage` 等。
- [X] 文件弹窗：基于 `electron.dialog` 实现 `showOpenDialog`、`showSaveDialog`。
- [X] 文件管理器：基于 `electron.shell` 实现 `shellOpenPath`、`shellShowItemInFolder`。
- [X] 屏幕拾取：基于 `desktopCapturer` 实现音视频/窗口源获取。

**✅ A.3 子任务记录（2026-03-12 / Step A.3.1）**

- 新增 `main/core/system.js`：
  - 剪贴板：`copyText / copyImage / readClipboardText`
  - 对话框：`showOpenDialog / showSaveDialog`
  - Shell：`shellOpenPath / shellShowItemInFolder / shellOpenExternal`
  - 桌面源：`getDesktopSources`（返回可序列化缩略图与图标）
- IPC 新增：
  - `system:clipboard:*`
  - `system:dialog:*`
  - `system:shell:*`
  - `system:desktop:getSources`
- preload 已同步暴露 system API（main/window/fast 三窗口一致）。
- ✅ 验证：`pnpm build` 通过。

---

### A.4 原后端业务代码迁移与适配 ✅/🟡（整体已落地，但后续仍会随“完整复刻”继续补齐）

#### A.4（总项）说明（原文要点保留）

- [/] **A.4 原后端业务代码迁移与适配**
  - [X] 重构 `main/core/data.js`：接入新版 DB API，替换所有 `utools.db` 调用。

    - 补充说明：已完成配置分块存储（`config/prompts/providers/mcpServers/tasks/config_local_desktop`）与 memory 导入导出；后续在迁移 `chat/mcp/skill` 时按需补充扩展能力。

    - [X] Step A.4.1（已完成）：先落地配置与 memory 同步最小闭环（`getConfig/saveSetting/updateConfig/updateConfigWithoutFeatures/exportMemoryData/importMemoryData`），并完成 IPC + preload 暴露。

    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

    - [X] Step A.4.3.3（已完成）：迁移 `skill.js` -> `main/core/skill.js`，并打通 skill IPC / preload（list/get/save/delete/export/extract/definition/resolve/pathJoin）。

    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [X] 重构 `main/core/file.js`：将原 uTools 文件对象逻辑改写为基于 Node.js `fs/promises` 的绝对路径读取。

    - [X] Step A.4.2（已完成）：新增 `main/core/file.js` 并接入 IPC（`file:*`）；三套 preload 已暴露文件相关 API（`handleFilePath/sendfileDirect/saveFile/selectDirectory/listJsonFiles/readLocalFile/renameLocalFile/deleteLocalFile/writeLocalFile/setFileMtime/isFileTypeSupported/parseFileObject/copyLocalPath`）。

    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
  - [X] 迁移 `chat.js`、`mcp.js`、`mcp_builtin.js`、`skill.js`：审查并替换其中可能存在的 uTools API。

    - [X] 说明：`mcp_builtin.js` 已在桌面端落地最小可用骨架（内置工具注册与调用入口），后续在 C/D 阶段按 UI/Agent 联调逐步填充具体内置工具实现。

    - ✅ 本轮本地提交：
      - `a70218e` feat(phase-a4.3.1): 迁移 chat 核心并接入 Electron IPC
      - `db4bbb8` feat(phase-a4.3.2): 迁移 mcp 管理核心并打通 IPC/preload
      - `d347989` feat(phase-a4.3.3): 迁移 skill 核心并接入 IPC/preload
  - [X] Step A.4.4（已完成，2026-03-12）：同步上游 `5ccb192` 的文件类型判定增强（`file.js`），并评估 `data.js` 差异在桌面端是否存在对应实现。

    - 结论：桌面端 `main/core/data.js` 当前未实现上游 `expectedMatchFeature/cmds` 那套分支，故本次 `data.js` 无需迁移该 diff。
    - 已同步到 `main/core/file.js`：
      - `isFileTypeSupported`：白名单支持 + 二进制黑名单拒绝 + 未知后缀放行待探针
      - `parseFileObject`：新增 Base64 内容探针（`\0` 检测），未知后缀且非二进制按文本处理
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。

---

### A.5 Preload 层隔离实现 ✅

- [X] Step A.5.1（已完成）：补齐窗口控制相关 IPC 与 preload API（`window:minimize/maximizeOrRestore/close/toggleAlwaysOnTop`），为后续 `TitleBar.vue` 迁移铺路。
  - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。
- [X] 编写 `preload/main_preload.js`：暴露给主设置界面的 API。
- [X] 编写 `preload/window_preload.js`：暴露给对话界面的 API（包含窗口控制、置顶切换等 IPC）。
- [X] 编写 `preload/fast_preload.js`：暴露给快捷指令窗的轻量级 API。

- 说明：已统一暴露数据层、文件层、窗口事件总线与窗口控制能力，并保留 `windowControl(action)` 兼容接口以承接旧代码迁移。
- ✅ 验证：`pnpm build` 通过（2026-03-12，本轮，见 Step A.5.1）。

---

## 6. 阶段 B：Anywhere_main 主控制台复刻 (Phase B)

> 直接复用 Vue 代码，修改接口对接方式，确保界面与设置数据跑通。

---

### B.1 前端环境与公共资源挂载 ✅

- [X] Step B.1.0：清理脚手架残留目录 `src/`（确认当前构建不引用）
  - ✅ `pnpm build` 通过（2026-03-12）
- [ ] 在 `render/main` 中配置 Vue + Element Plus，引入原项目的 `base.css`, `dark.css` 等样式文件。
- [X] Step B.1.2：同步 `Anywhere_main/src/assets/base.css` 与 `main.css` 到 `render/main/assets/`
  - ✅ `pnpm build` 通过（2026-03-12）
- [X] Step B.1.1：迁移插件与 i18n 初始化到 `render/main/main.js`（Element Plus + icons + i18n + locales）
  - 新增 `render/main/locales/en.json` 与 `render/main/locales/zh.json`（最小语言包，后续扩充）
  - 安装依赖：`element-plus`、`@element-plus/icons-vue`、`vue-i18n`
  - ✅ `pnpm build` 通过（2026-03-12）

---

### B.2 设置与状态流转适配 ✅

- [X] Step B.2.0：补齐 `Setting.vue` 迁移前置依赖（`vuedraggable`、`webdav`）
  - ✅ `pnpm build` 通过（2026-03-12）
- [X] Step B.2.1：迁移 `Setting.vue` 并接入桌面端壳（配置 + Memory 联调）
  - `render/main/App.vue`：config provide + Setting 宿主结构
  - `vuedraggable` 切换 Vue3 兼容版本 `^4.1.0`
  - ✅ `pnpm build` 通过（2026-03-12）
- [ ] 确保原主题切换逻辑（监听媒体查询及 `html.dark` 类名）正常工作。
- [X] Step B.2.2：最小主题闭环（`themeMode` + `isDarkMode` + 系统主题监听 + `html.dark`）
  - ✅ `pnpm build` 通过（2026-03-12）
- [X] Step B.2.2（导入导出适配）：从浏览器下载/文件输入改造为 Electron `dialog + file IPC`（保留 memory 同步）
  - `showSaveDialog/showOpenDialog/writeLocalFile/readLocalFile`
  - ✅ `pnpm build` 通过（2026-03-12）
- [X] Step B.2.3：WebDAV 同步主进程化
  - 新增 `main/core/webdav.js` + IPC + preload
  - Setting 的 WebDAV 备份/恢复/列表/删除改为主进程桥接调用
  - ✅ `pnpm build` 通过（2026-03-12）
- [X] `Setting.vue`：接入新版 `window.api.getConfig` 和 `window.api.saveSetting`
- [X] 测试导入/导出功能（文件存取走 `dialog` API）
- [X] 测试 WebDAV 同步网络请求是否在 Electron 渲染进程正常执行（或移交主进程处理跨域）

---

### B.3 其他管理页面复刻 ✅

- [X] Step B.3.0：补齐阻塞 API（`coderedirect` / `runTaskNow` / `getRandomItem`）
  - 主进程 + IPC + preload 最小兼容层
  - 复用窗口事件总线实现跨窗口投递（`coderedirect` 与 `runTaskNow`）
  - ✅ `pnpm build` 通过（2026-03-12）
- [X] Providers.vue（服务商，完成 2026-03-12）：
  - API：`getConfig/updateConfigWithoutFeatures/saveSetting/getRandomItem`
  - ✅ `pnpm build` 通过
- [X] Mcp.vue（MCP 管理，完成 2026-03-12）：
  - `testMcpConnection/testInvokeMcpTool/getMcpToolCache/saveMcpToolCache`
  - `copyText` 返回结构兼容
  - ✅ `pnpm build` 通过
- [X] Skills.vue（Skill 管理，完成 2026-03-12）：
  - 本地目录读取、Skill 导入导出、文件操作 API 联调
  - ✅ `pnpm build` 通过
- [X] Prompts.vue（快捷助手，完成 2026-03-12）：
  - 正则匹配/数据关联基础链路 + `coderedirect` 接线
- [X] Tasks.vue（定时任务，完成 2026-03-12）：
  - `runTaskNow`、任务配置读写、历史记录文件操作
- [X] Chats.vue（历史对话，完成 2026-03-12）：
  - 本地文件列表、重命名/删除、云端同步链路接线

（保留原子任务记录）

- ✅ Step B.3.3：Skills 导航入口与渲染分支接入，build 通过
- ✅ Step B.3.4：Prompts 接入，API 对齐确认，build 通过
- ✅ Step B.3.5：Tasks 接入，API 对齐确认，build 通过
- ✅ Step B.3.6：Chats 接入，API 对齐确认，build 通过

---

### B.4 主控台样式深度复刻修复（新增，2026-03-12）✅/🟡（按 H* 子步归档）

#### Step B.4.0 ✅（根因排查结论原文保留）

- 结论1：`render/main/App.vue` 与原项目 `Anywhere_main/src/App.vue` SHA256 一致。
- 结论2：桌面端 `render/main/index.html` 仅保留最小骨架，缺失原项目 `Anywhere_main/index.html` 的大体量内联主题变量与 Element Plus 全局覆盖样式，是“默认 Element 风格”的直接原因。
- 结论3：桌面端受严格 CSP（`img-src 'self' data:`）限制，可能影响远程文档图片显示。
- 下一步：复制原 `Anywhere_main/index.html` 主题样式块到 `render/main/index.html`，仅做 Electron 入口脚本路径适配。

#### Step B.4.1 ✅（样式入口迁移 + marked 依赖）

- 复制 `Anywhere_main/index.html` 到 `render/main/index.html`
- 适配入口脚本：`/src/main.js` -> `/main/main.js`
- 构建阻塞：缺少 `marked`，已 `pnpm add marked` 修复
- ✅ 本地 Git 存档：`63fa53e fix(main-style): 复刻Anywhere_main主题样式并补齐marked依赖`

#### Step B.4.H1 ✅（热修：启动崩溃 + CSP meta 恢复）

- 用户反馈：启动报错 `Cannot read properties of undefined (reading 'config')`，并出现 Electron CSP 安全警告。
- 定位：
  - `render/main/App.vue` 依赖 `window.api.defaultConfig.config`，但 preload 未暴露该字段
  - `render/main/index.html` 复制后缺失 CSP meta
- 修复：
  1) `App.vue` 改为安全配置归一化：优先用 `getConfig`，失败走本地 fallback
  2) `index.html` 恢复严格 CSP（不启用 `unsafe-eval`，保留 `style-src 'unsafe-inline'`）
- ✅ `pnpm build` 通过（2026-03-12）

#### Step B.4.H2 ✅（2026-03-13：运行态报错分治）

- 已完成：按“先崩溃、后连通”策略推进（作为 H3 的前置收敛）。

#### Step B.4.H3 🟡（2026-03-13：多点运行态 bug 收敛，按顺序修）

> 顺序（原文保留）：**设置/备份 → MCP 可见性 → MCP 测试 → CSP 资源**

- H3.1 ✅（已修复，待运行态复测 → 后续又补充多轮并已 build 通过）

  - 修复 `Setting.vue` WebDAV 备份/还原与本地配置导入失败（`[object Object]` / `Unexpected end of JSON input`）
  - 根因初判：
    - `readLocalFile` 返回值结构判断错误（字符串被当 `{ content }`）
    - 对 `webdav/importMemoryData` IPC 错误对象未做 message 解包
    - `mcp.js` 测试链路仅识别 `transport` 不识别页面 `type`
    - `mcp_builtin.js` 为空对象导致“内置 MCP 全空”
  - 修复范围：错误解包 + 本地导入修正、`type -> transport` 兼容、内置 MCP 最小可见性恢复（图片 CSP 另步）
  - 第二轮补充：
    - `webdav.js` 兼容 `exists()/HEAD` 405
    - Setting 备份读写统一走 `data_path`
    - 修正 `Mcp.vue` 对 `getMcpToolCache/testMcpConnection/testInvokeMcpTool` IPC 解包
    - ipcHandler 保存 sanitize 工具缓存，避免 `An object could not be cloned` 与 `执行错误: [object Object]`
  - 第三轮补充：`Mcp.vue#runToolTest` 对测试配置深拷贝，避免响应式对象穿透
  - ✅ `pnpm build` 通过（2026-03-13）
- H3.2 ✅：恢复内置 MCP 可见性

  - `main/core/mcp_builtin.js` 补回最小可见内置 MCP 服务与工具定义（Python / Filesystem / Bash / Search / Super-Agent / Tasks / Time / Memory）
  - ✅ `pnpm build` 通过（2026-03-13）
- H3.3 ✅：修复 `testMcpConnection/testInvokeMcpTool` 联调失败

  - `Mcp.vue` 正确解包 IPC 返回，失败优先展示 message
  - 缓存工具默认 `enabled !== false` 视为启用
  - `ipcHandler#mcp:testConnection` 先保存 sanitize 工具缓存，避免克隆异常
  - preload 统一 `toPlainPayload(JSON.parse(JSON.stringify(...)))` 入参纯对象化
  - `main/core/mcp.js#buildServerConfig` 兼容页面 `type` 映射到 `transport`
  - ✅ `pnpm build` 通过（2026-03-13）
- H3.4 ✅：CSP 收尾（按你要求“不要细粒度白名单，放行远程图片/请求”）

  - 策略：保留 `script-src 'self'`，放开 `img-src/connect-src` 到 `http/https/ws/wss + data/blob/file`
  - ✅ `pnpm build` 通过（2026-03-13）
- H3.5 🟡：继续补全桌面端内置 MCP/Skill 的完整实现（按原项目完整复刻，不再最小复刻）
  - ✅ H3.5.k（已完成，2026-03-20）：按方案 B 将主应用窗口调整为“非隔离上下文 + preload 直接挂载 `window.api`”，以更贴近 uTools 插件开发模型，并规避部分 IPC 对活对象/信号传递的限制。

    - 实施：`main/windowManager.js` 中所有主应用窗口统一切换为 `contextIsolation: false`（保持 `nodeIntegration: false`、`sandbox: false`），复用 preload 里已有的 `window.api = api` 直挂载分支。
    - 现状：桌面端本地应用窗口现在更接近 uTools 插件的“宿主直接注入 `window.api`”模式；但对主进程级能力（窗口控制、系统能力等）底层仍保留 Electron IPC 实现。
    - 安全边界：该方案仅适用于受信任的本地应用页面；若未来要像 Cherry Studio 那样在客户端内嵌浏览任意网页/远程站点，必须使用**单独的低权限窗口/视图**承载，不能复用这些高权限直挂载窗口。
    - ✅ 验证：`pnpm build` 通过（2026-03-20，本轮）。

  - ✅ H3.5.j（已完成，2026-03-20）：清理 Skill 拖拽导入排障阶段新增的调试日志，并回归验证构建。

    - 已移除 `render/main/components/Skills.vue` 中的 `[Skills][drag]` 调试日志与辅助函数。
    - 已移除 `preload/main_preload.js`、`window_preload.js`、`fast_preload.js` 中的 `[preload:*]` 调试输出，保留 `getDroppedFilePath`、同步 `pathJoin` 与 `invokeOrThrow` 功能实现。
    - ✅ 验证：`pnpm build` 通过（2026-03-20，本轮）。

  - ✅ H3.5.i（已完成，2026-03-20）：为 anywhere_main 的 Skill 拖拽导入链路补充调试日志并完成桌面端拖拽路径解析修复。
    - 二次定位结论：`window.api.pathJoin` 在桌面端被实现为异步 IPC，但插件前端按同步函数使用，导致导入链路里 `skillMdPath/importPath/pendingImportPath` 实际是 `Promise`，从而误报“未找到 SKILL.md”。


    - 调试结论：拖拽事件本身已触发，但 Electron 渲染进程中的拖拽 `File` 对象存在 `name/type/size`，却没有插件环境中的 `file.path`，导致旧逻辑在 `drop:ignored:no-item-path` 处静默返回。
    - 修复：在 `preload/main_preload.js`、`window_preload.js`、`fast_preload.js` 中新增 `getDroppedFilePath(file)`，基于 Electron `webUtils.getPathForFile(file)` 解析真实本地路径；`render/main/components/Skills.vue` 改为优先使用解析后的 `resolvedItemPath`，不再直接依赖 `File.path`。
    - 保留日志：`[Skills][drag]` 与 `[preload:*]` 仍保留，便于继续观测导入链路。
    - 已修复：`preload/main_preload.js`、`window_preload.js`、`fast_preload.js` 中的 `pathJoin` 已改回插件式本地同步 `path.join(...args)`，不再走异步 IPC。
    - ✅ 本轮验证：`pnpm build` 通过（2026-03-20，本轮）。

    - ✅ 验证：`pnpm build` 通过（2026-03-20，本轮）；保留一个既有非阻塞告警：`main/core/mcp.js` 静态/动态同时引入。
    - ✅ 用户运行态回归：已确认目录 / `SKILL.md` / `.zip` / `.skill` 拖拽导入成功。


  - ✅ H3.5.h（已完成，2026-03-20）：按插件项目当前行为修复桌面端 anywhere_main 的 Skill 拖拽导入链路（`SKILL.md` / `.zip` / `.skill`）。

    - 根因：桌面端 `skill:*` IPC 与 preload 返回语义偏离插件项目，前端把结构化返回对象当作原始值使用，导致 `.zip/.skill` 导入链路把对象当路径参与 `pathJoin`，最终拖拽导入失败。
    - 修复：`main/ipcHandler.js` 中 `skill:save/delete/exportPackage/extractPackage` 改回返回插件式原始值；`preload/main_preload.js`、`window_preload.js`、`fast_preload.js` 新增 `invokeOrThrow`，统一将 `{ ok:false, error }` 转成异常抛出，并让 `skill/file` 相关 API 恢复“成功返回原值、失败抛异常”的插件调用习惯。
    - ✅ 验证：`pnpm build` 通过（2026-03-20，本轮）；保留一个既有非阻塞告警：`main/core/mcp.js` 静态/动态同时引入。

  - ✅ H3.5.g（已完成，2026-03-20）：同步上游 `8c639430` 的 Skill 导入兼容性修复到桌面端 `main/core/skill.js` 与 `render/main/components/Skills.vue`。

    - `main/core/skill.js`：补齐 frontmatter 解析能力（多行 `|` / `>`、数组、布尔值、引号字符串、缩进列表），并将 `.skill/.zip` 解包后的 `SKILL.md` 定位改为递归查找。
    - `render/main/components/Skills.vue`：导入/编辑时保留 `extraMetadata`，保存时稳健回写 YAML，兼容 `SKILL.md` / `skill.md`，避免部分格式 Skill 导入失败或字段丢失。
    - 📝 验证：`pnpm build` 本轮主进程与 preload 构建通过，但整体构建被既有 `render/window/main.js -> '@/assets/dark.css'` 路径解析问题阻断；该报错与本次 Skill 导入修复无关。


  - ✅ H3.5.b（已完成，2026-03-13）：

    - 已修复 `main/core/mcp_builtin.js#get_full_memory` 的残留 `utools` 调用，统一改为桌面端 `dbGet` 读取
    - 已将 `main/core/mcp_builtin.js#getBuiltinTools` 改为异步动态注入 Super-Agent Agent 名单（从 `prompts` 文档 + config prompts 合并）
    - 已在 `main/core/mcp.js` 传递 `config.prompts` 到 `getBuiltinTools`，并移除“持久连接数量限制 + on-demand 并发阈值”逻辑
    - ✅ 验证：`pnpm build` 通过（2026-03-13）
    - 📝 备注：仍存在非阻塞打包告警（`mcp.js` 同时静态/动态引入），不影响构建产物
    - 补充修复：为兼容 `getBuiltinTools` 异步化，已将 `main/core/skill.js#getAllBuiltinToolNames/resolveSkillInvocation` 与 `main/ipcHandler.js#skill:resolveInvocation` 调整为 async 链路
    - ✅ H3.5.c（已完成，2026-03-13）：

      - 已同步上游 `2a4ecc6/9884f6a` 在 `backend/src/mcp.js` 的关键修复到桌面端 `main/core/mcp.js`
      - 新增 `preprocessStdioConfig` 并接入 `buildServerConfig`：
        - stdio `command` 支持引号安全拆分（修复 Windows 路径含空格时的参数拆分失败）
        - stdio `env` 为空对象时改为继承父进程环境；非空时与 `process.env` 合并（补齐 PATH）
      - 本轮聚焦最小改动：仅改 `mcp.js` 及其调用链，不做无关扩散
      - ✅ 验证：`pnpm build` 通过（2026-03-13）
      - 📝 备注：仍有非阻塞构建告警（`mcp.js` 静态/动态同引入），与本次改动无冲突
    - ✅ H3.5.d（已完成，2026-03-13）：

      - 同步上游“Disabled MCP 不应被默认启用”逻辑到桌面端 `render/main/components/Tasks.vue`
      - `handleAddTask` 的默认 `extraMcp` 改为仅包含 `type==='builtin' && isActive!==false` 的服务
      - ✅ 验证：`pnpm build` 通过（2026-03-13）
      - 📝 备注：普通 MCP `testInvokeMcpTool` 残余 clone 场景下一步继续沿 `Mcp.vue -> preload -> ipcHandler -> mcp.js` 定点收敛
    - ✅ H3.5.e（已完成，2026-03-13）：

      - 修复 `skill.js#getAllBuiltinToolNames`：
        - 跳过已禁用的内置 MCP 服务（`isActive === false`）
        - 跳过已禁用工具（`tool.enabled === false`）
      - `ipcHandler#skill:resolveInvocation` 补充传入当前配置 `mcpServers`，保证 Skill 解析期可正确感知启停状态
      - ✅ 验证：`pnpm build` 通过（2026-03-13）
      - 📝 备注：仍保留非阻塞构建告警（`mcp.js` 静态/动态同引入）
    - ✅ H3.5.f（已完成，2026-03-13）：

      - 按原项目语义进一步补强 `skill.js#getAllBuiltinToolNames`：
        - 优先读取 `mcpServers` 文档（服务级开关）
        - 读取 `mcp_tools_cache` 文档（工具级开关）
        - 双重过滤后再生成可授予的内置工具列表
      - 保持防递归约束：`sub_agent` 不进入可授予列表
      - ✅ 验证：`pnpm build` 通过（2026-03-13）
      - 📝 备注：已对齐你提供的上游函数行为（桌面端用 `dbGet` 替代 `utools.db.get`）
  - ✅ H3.5.a（已完成，2026-03-13）：

    - “复制为主、少量适配”迁移 `main/core/mcp_builtin.js` 第一批完整迁移
    - 恢复 Filesystem/Bash/Search/Time/Memory/Tasks 核心 handlers 与 background shell 管理
    - 你已人工修复字符串换行转义问题（`\n` 误写为实际换行），并清理 bk/bak 文件
    - 同步更新语言文件：`render/main/locales/en.json`
    - ✅ `pnpm build` 通过（2026-03-13）
    - 📝 构建备注：保留一个非阻塞告警（`mcp.js` 同时被静态与动态引入），不影响产物与运行

---

## 7. Chats / Providers / 配置写回 等专项修复（原记录不删，集中归档）

### Chats 专项

- ✅ 目标1：修复 `Chats.vue` 部分文件项 `basename` 缺失导致 `endsWith` 崩溃

  - normalizeChatFile/resolveFileBasename/resolveWebdavDataPath 兜底
  - `formatFilenameDisplay` 安全字符串处理
  - ✅ build 通过（2026-03-12）
  - ✅ commit：`cef592c fix(chats): 兜底历史对话文件名渲染崩溃`
- ✅ 目标2：Chats 云端 WebDAV 访问主进程代理化，规避 CSP `connect-src`

  - 读链路：复用 `webdav:listBackups` / `webdav:readBackup`
  - 写链路：rename/delete/sync/clean 继续迁移到 `webdav:*`；补齐 `moveFile` 与 `writeBackup.lastModified`
  - 时间显示异常：避免回退 Unix epoch；深挖时间源并恢复真实最后修改时间
  - dataConverter 修复：避免将普通元数据误判为 File-like 导致 `lastmod` 丢失
  - ✅ build 通过（2026-03-13）
  - ✅ commit：`f728a0e fix(chats): restore history timestamp display`

### Providers 专项

- ✅ 目标3：Providers 模型拉取改主进程代理，规避 CSP `connect-src`
  - `main/core/chat.js` 新增 `listProviderModels` + IPC + preload(main)
  - 前端仅传 `baseUrl/apiKey`，主进程请求 `/models`
  - Providers 解包 IPC 错误 `{ ok:false, error }`，失败展示 message
  - ✅ build 通过（2026-03-13）
  - ✅ commit：`1551f7a fix(providers): proxy model list requests through main`
  - ✅ commit：`78e9db7 fix(providers): handle ipc model fetch errors`

### 配置写回（克隆防护）

- ✅ 目标4：为 `atomicSave` 增加深拷贝防护，规避 `An object could not be cloned`
  - Mcp/Prompts/Tasks：`configToSave = { config: JSON.parse(JSON.stringify(latestConfig)) }`
  - ✅ build 通过（2026-03-13）
  - ✅ commit：`d2b7016 fix(main-config): harden atomic save payload cloning`

---

## 8. 阶段 C：Anywhere_window 独立对话窗口复刻 (Phase C) 🟡（进行中）

> **核心目标**：彻底剥离 uTools 沙箱环境，利用 Electron 原生能力，确保聊天体验一致。重点攻克窗口路由、Markdown 深度渲染、多模态处理（文件/语音）和长图导出等核心功能。

### C.1 窗口生命周期与通信基建 🟡 (进行中)

- [x] **C.1.1 预加载环境隔离 (`window_preload.js`)**
  - 重构独立窗口专用的 preload 脚本。
  - 暴露并映射桌面端完整的文件写入、长图导出和剪贴板接口。
  - 已完成：尽可能按原插件 `window_preload.js` 通信格式重建窗口专用后端桥，补回 `window.preload.receiveMsg / onAppendMessage` 兼容层。
  - 已完成：`createChatCompletion / invokeMcpTool / resolveSkillInvocation` 改为窗口 preload 直连后端模块，避免 `signal/AbortController`、`onUpdate` 等活对象跨 IPC 丢失。
  - 已完成：补齐 `defaultConfig / getUser / desktopCaptureSources / handleCodeClick / background cache / task history / skill fork` 等窗口专用 API。

  - ✅ C.1.1.i（已完成，2026-03-22）：进一步收敛 `window_preload.js` 依赖，只保留 `chat.js` 直连，其余 `data/file/skill/mcp` 全部改回 IPC/live bridge，彻底切断 preload 对 `@electron-toolkit/utils` 的间接依赖链；验证 `out/preload/window_preload.js` 已不再包含该模块。
  - ✅ C.1.2.h（已完成，2026-03-22）：将默认用户头像与默认 AI 图标切换为 `resources/user.png` / `resources/icon.png` 的 file URL，修复窗口启动时 `user.png`/默认 AI 图标资源 404。
  - [/] C.1.1.j（进行中，2026-03-22）：临时兼容 Electron preload 的 browser-like OpenAI SDK 限制，放开 `dangerouslyAllowBrowser` 以恢复当前窗口对话可用性；后续再将真实聊天请求迁回主进程以收回密钥暴露面。
  - ✅ 验证：`pnpm build` 通过（2026-03-22，本轮资源与 preload 根因修复复测）。


  - ✅ C.1.1.h（已完成，2026-03-22）：定位并修复 `Anywhere_window` preload 启动即崩溃问题。根因是 `window_preload.js` 静态引入 `data/skill/file` 相关主进程链路，间接拉起 `@electron-toolkit/utils`，在 preload 上下文中访问 `app.isPackaged` 失败。
  - ✅ 修复：将 `window_preload.js` 收敛为“仅直连 `chat.js` + 其余统一走 IPC/live bridge”的轻量版本，切断对 `windowManager/@electron-toolkit/utils` 的间接依赖；复核产物 `out/preload/window_preload.js` 已不再包含该危险依赖链。
  - ✅ 验证：`pnpm build` 通过（2026-03-22，本轮最终根因修复复测）。

- [x] **C.1.2 窗口初始化与事件总线 (`App.vue` 核心逻辑)**
  - 使用 `window.api.onWindowInit` 替代 uTools 启动传参。
  - 接入 `window.api.onWindowEvent` 事件总线，实现跨窗口“追加消息/一键追问”接收。
  - 加入前端兜底的 `window.resizeTo` 与 `window.moveTo` 控制机制。
  - 消除 CSP 安全警告，放宽图片与网络请求的限制。
  - 已完成（后端侧）：主进程 `window:init` 已注入 `os/code/type/payload/taskConfig/tempPromptConfig/isAlwaysOnTop/senderId` 等原插件窗口初始化字段；`config-updated / mcp-cache-updated / skills-updated / always-on-top-changed` 已补桌面端广播兼容。
  - 已完成（前端侧）：`App.vue` 初始化入口已改为“`window.api.onWindowInit` 优先，`window.preload.receiveMsg` 回退”；追加消息入口已改为“`window.api.onWindowEvent` 优先，`window.preload.onAppendMessage` 回退”，并抽出统一的追问/追加消息处理逻辑。
  - ✅ 验证：`pnpm build` 通过（2026-03-22，本轮）。

  - ✅ C.1.2.g（已完成，2026-03-22）：补齐 `skill:save/delete/toggleForkMode` 后的窗口广播通知，确保 `Anywhere_window` 中 `onSkillsUpdated` 能在桌面端正常收到更新。
  - ✅ C.1.3.h（已完成，2026-03-22）：修复 `Prompts.vue -> main_preload/fast_preload -> window:open` 主链上的 payload 透传缺失，确保 `code/payload/options` 不再在 preload 层丢失。
  - ✅ 验证：`pnpm build` 通过（2026-03-22，本轮最终复测）。


  - ✅ C.1.2.f（已完成，2026-03-22）：继续完成 `Anywhere_window` 前端运行态静态联调，统一追加消息处理逻辑并修正 `task:run-now` 事件落点，避免误走整窗重新初始化。
  - ✅ 验证：`pnpm build` 通过（2026-03-22，本轮复测）。

- [/] **C.1.3 窗口创建路由标准化（后端部分） (`main/windowManager.js`)**
  - 废弃原 `utools.redirect`（即 `coderedirect`）的绕路做法。
  - 在主控台直接调用 `window.api.openWindow('window', { code: promptKey })`。
  - 主进程 `windowManager` 读取数据库配置，直接以正确的宽高和坐标（`x`, `y`）创建 `BrowserWindow` 实例，并注入初始参数。
  - 已完成：桌面端 `window:open(type, payload/options)` 后端入口已打通，并支持向 `Anywhere_window` 注入原插件风格初始化载荷。
  - 进行中：修复独立对话窗口仍显示原生 titlebar、未按 prompt 配置尺寸/位置打开、保存窗口大小/缩放报错等运行态问题。
  - ✅ C.1.3.i（已完成，2026-03-23）：收敛独立窗口创建链路到 `main/windowManager.js`，补齐 `frame: false`、按 prompt 配置尺寸/位置打开、非固定位置跟随鼠标、重叠错位避让。
  - ✅ C.1.3.j（已完成，2026-03-23）：修复 `savePromptWindowSettings` 前端错误解包与缩放持久化链路，消除 “保存失败: [object Object]”。
  - ✅ C.1.3.k（已完成，2026-03-23）：补齐 `window_preload.js` 的真实 `setZoomFactor` 实现，并让初始化/快捷键缩放真实作用到窗口。
  - ✅ C.1.3.l（已完成，2026-03-23）：完善 `Anywhere_window` 对 `config-updated / mcp-cache-updated / skills-updated` 的实时同步，覆盖主题、模型、背景图、MCP、SKILL 等运行态更新。
  - ✅ C.1.3.m（已完成，2026-03-23）：修复保存窗口尺寸/位置时的回归崩溃，收敛 `savePromptWindowSettings -> config-updated` 对当前窗口的自触发重算。（已补保存态保护与配置回流跳过；并补错误 message 递归解包，避免残留 `[object Object]`）
  - ✅ C.1.3.n（已完成，2026-03-23）：修复独立对话窗口关闭按钮回归崩溃，为 `closePage` 增加关闭态保护与异步收口。（已将 titlebar 关闭改为脱离点击栈的异步关窗，避免在当前点击栈内同步销毁自身窗口）
  - ✅ C.1.3.o（已完成，2026-03-23）：修复历史对话恢复链路，确保 `Chats.vue` 打开的是带会话 payload 的历史窗口而非默认空白窗。（已改为直连 `openWindow('window', payload)`，并按会话内 `CODE` 复原助手）
  - ✅ C.1.3.p（已完成，2026-03-23）：修复非内置 MCP 工具取消与结果渲染，取消后立即结束，结构化结果不再显示为 `[object Object]`。（已补 AbortError 抛出、结构化结果格式化，以及临时 MCP client 在 abort 时主动 close）
  - [/] C.1.3.q（进行中，2026-03-23）：针对“保存失败仍显示 `[object Object]` / 关闭按钮仍崩溃”的回归进行排查与修复；已修复 `main/dataConverter.js` 对普通返回对象的 Error 误判（避免把保存成功结果序列化成 `Error`）。当前工作区已回退到“仅保留保存失败修复、撤销后续关窗实验”的状态：恢复 `closePage -> windowControl('close-window')` 主链，不再保留 `window.close()` T1 最小实验、`closeMcpClient` 前置实验，以及相关 close 自调度/销毁期调试实验。下一步将基于该基线继续对比历史版本与定位真实关窗根因。

  - ✅ C.1.3.r（已完成，2026-03-24）：针对独立窗口点击右上角关闭后 Electron 进程以 `3221226525` 崩溃的问题，定位为“当前窗口通过 `ipcRenderer.invoke('window:close')` 请求主进程关闭自身，主进程在 `ipcMain.handle` 内同步 `win.close()` 后仍需向即将销毁的 sender 回送 invoke 响应”。已改为 fire-and-forget 的 `ipcRenderer.send('window:close') / ipcMain.on('window:close')` 关闭链路；同时保持 preload `windowControl('close-window')` 返回本地成功占位结果，避免上层 await 断裂。

  - ✅ C.1.3.s（已完成，2026-03-24）：用户复测确认 `send/on + win.close()` 后仍以 `3221226525` 崩溃，说明根因不在 IPC 返回链本身，已转为排查窗口销毁阶段的渲染侧卸载/MCP 清理/原生 close 行为。现已切换为“独立对话窗口使用 `BrowserWindow.destroy()`、单例窗口仍保留 `close()`”的新策略，绕过 Chromium/Electron 的 renderer unload/close 链；用户后续实测已确认关闭功能恢复正常。

  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 `close() -> destroy()` 策略切换复测）。

  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 `window:close` fire-and-forget 改造复测）。
  - ✅ 验证：`pnpm build` 通过（2026-03-23，本轮关闭按钮/保存错误解包/MCP 取消优化复测）。
  - 📝 备注：仍存在既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。

  - ✅ C.1.3.t（已完成，2026-03-24）：已同步核对上游插件最近两次提交：`a303154 修复打开已有对话时，模型排序错误的bug` 与 `8c63943 修复部分格式skill导入失败的bug`。核对结果：功能代码层面桌面端已包含对应修复——`render/window/App.vue` 已使用 `updateModelListAndMap(currentConfig.value)` 修复历史对话恢复时的模型排序；`render/main/components/Skills.vue` 与 `main/core/skill.js` 已具备 YAML frontmatter 兼容解析、多行/数组/布尔值处理、额外元数据透传，以及解压后递归定位 `SKILL.md` 的能力。本轮仅补齐桌面端 `.gitignore` 对 `进度.md` 的忽略规则，并完成构建复核。

  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮上游两次提交同步复核）。

  - ✅ C.1.3.u（已完成，2026-03-24）：已继续同步上游后续提交（`fcbf12e`、`c5e1de7`、`c688367`）并清理桌面端调试日志。核对结果：`fcbf12e` 与 `c688367` 对应的窗口侧修复已在桌面端存在（发送时仅在底部才滚动、工具调用完成后仅在输入框未聚焦时才补焦点、思考预算 tooltip 文案已覆盖 `none/xhigh`）；本轮实际补齐 `c5e1de7` 到 `main/core/mcp_builtin.js`，让 `edit_file` 在 Windows 下兼容 CRLF/统一换行匹配后再恢复原始换行风格。同时已静音并移除 `main/index.js`、`main/ipcHandler.js`、`main/windowManager.js`、`main/core/data.js`、`preload/window_preload.js`、`main/core/mcp_builtin.js` 中的调试日志输出。

  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮上游后续提交同步 + 调试日志清理复核）。

  - 📝 Git 存档说明（2026-03-24）：已完成本轮代码与构建验证，但**未执行 Git 写操作**。原因：当前会话遵循 Git 只读约束，且 `git status --short` 显示工作区存在多处非本轮文件变更（包括若干核心模块、预加载脚本、UI 文件及 `Agent Recoder.md` 删除态、`resources/user.png` 未跟踪），不宜在未进一步拆分/甄别的情况下直接打包提交。建议后续人工按“关闭崩溃修复 / 上游 commit 同步 / 日志清理”分组存档。

  - ✅ C.1.3.v（已完成，2026-03-24）：已修复定时任务“召唤了窗口但未按任务配置指定快捷助手并自动发送内容”的问题。根因是 `data:runTaskNow` 仍走“先 `openWindow('window')` 打开默认窗口，再通过 `task:run-now` 事件补发任务内容”的旧链路，绕过了桌面端已收敛的 `openWindow('window', payload)` 初始化入口。现已改为直接 `await openWindow('window', openPayload)`，让首次初始化即命中任务配置的 `promptKey/code/taskConfig/extraMcp/extraSkills`，并沿用窗口原生任务自动发送逻辑。

  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮定时任务指定助手初始化链路修复复核）。

  - ✅ C.1.3.w（已完成，2026-03-24）：已修复 Agent 工具链两个问题：1）`list_agent_chats` 现优先使用稳定的 `context.senderId` 识别调用者窗口，并正确标注 ` <-- [This is YOU]`；2）`read_agent_chats` 失败的直接根因是主进程调用了不存在的 `window.__AGENT_API__.readChatMessage(...)`，而窗口侧此前仅暴露了 `getMessage(...)`。现已在窗口侧补齐 `readChatMessage(index, offset, length)` 兼容接口，并将主进程的跨窗读取逻辑改为先检查 `__AGENT_API__` 就绪、无 `message_index` 时读 `getOutline()`、有 `message_index` 时读 `readChatMessage()`，同时统一 `callerId` 判定以避免误读自身窗口。

  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 Agent 工具窗口标识与跨窗读取修复复核）。

  - 📝 用户复测结果（2026-03-24）：`list_agent_chats` 当前窗口标识与 `read_agent_chats` 跨窗读取均已恢复正常。

  - ✅ C.1.3.x（已完成，2026-03-24）：继续修复 Agent 工具链发送问题：`summon_agent` 文本/附件现已统一收敛到窗口侧 `prompt + fileList` 后走同一轮 `askAI(false)` 发送，不再先写 history 导致“文本显示了但未真正发出”或“文本/附件拆成两轮”；`continue_agent_chats` 已补齐窗口 `__AGENT_API__` ready 等待；窗口侧 `processFilePath` 改为失败抛错，`sendMessage` 改为汇总附件失败并拒绝假成功，避免“窗口已召唤但未回复 / 附件未真正带上”的静默失败状态。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 Agent 工具发送链修复复核）。
  - 📝 备注：保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。

  - ✅ C.1.3.y（已完成，2026-03-24）：按用户复测继续排查 Agent 工具附件真实发送链。已确认普通 UI 发送链会经过 `file_paths -> handleFilePath -> parseFileObject -> history -> askAI`，但 `summon_agent / continue_agent_chats` 复用 `prompt/fileList/sendFile` 时仍可能受界面态与直发分支影响，导致“文本发出但图片/文件未同轮带上”。现已新增 Agent 工具专用发送入口：直接将 `text + file_paths` 组装为同一条 user message 写入 `history/chat_show`，再走 `askAI(true)`，彻底绕开快捷助手 `isDirectSend_*` 配置与普通输入框/fileList 的二次装配链；同时增加临时摘要日志，覆盖 `built-content / queued-history / request-summary` 三个阶段，便于继续根据用户复测做精确收敛。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 Agent 附件真实发送链修复复核）。

  - ✅ 回归修复（2026-03-24）：已修正上一版专用发送入口中的错误实现，不再把 `handleFilePath` 的返回半成品对象再次喂给 `parseFileObject`；现改为窗口侧基于 `base64 + mimeType` 直接构造 `image_url / file / input_audio / 文本` 消息 part，避免 `smarthome.png` 这类图片在专用发送链中触发 `[file] failed to resolve file data`，并保持文本与附件同轮发送语义。

  - [/] C.1.3.z（进行中，2026-03-24）：根据用户复测继续收敛 Agent 工具发送链。上一版窗口侧手工组装附件消息虽避免了解析异常，但日志表明最终请求仍只剩文本，说明这条自定义链路已偏离原插件底层实现。现改为回到原插件思路：Agent 工具附件统一走底层 `sendfileDirect -> parseFileObject`，再与文本合并为同一条 user message 写入 `history/chat_show`，最后 `askAI(true)`，彻底移除窗口侧重复装配带来的偏差。

  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 Agent 附件专用发送回归修复复核）。

  - ✅ 底层对齐修复（2026-03-24）：已撤回窗口侧手工附件装配方案，改为按原插件思路统一走底层 `window.api.sendfileDirect(...) -> parseFileObject` 生成标准多模态 content，再与文本合并为同一条 user message 写入 `history/chat_show`，最后 `askAI(true)`。目标是让 `summon_agent / continue_agent_chats` 的附件行为与原插件底层保持一致，避免桌面端自定义装配造成“最终请求只剩文本”。

  - ✅ C.1.3.aa（已完成，2026-03-24）：继续收敛 Agent 图片发送校验。已确认本地图片在发送给 AI 前以 `data:image/...;base64,...` 形式写入 `image_url.url`；并定位并修复 `main/dataConverter.js#isFileLike` 对 `image_url.url` 的误判，避免跨 IPC 返回窗口时破坏 data URI。用户复测已确认 `summon_agent / continue_agent_chats` 当前图片发送恢复正常。

  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 Agent 发送链底层对齐修复复核）。

  - ✅ 底层根因修复（2026-03-24）：已定位到 `main/dataConverter.js#isFileLike` 误判问题——此前只要对象里存在 `.url = data:...` 就会被当作 FileLike 重新序列化，导致 `file:sendfileDirect` 返回的 `{ type: 'image_url', image_url: { url: 'data:image/...;base64,...' } }` 在跨 IPC 返回窗口时把 `image_url.url` 破坏为空值。现已收紧 FileLike 判定：仅保留真实文件语义字段（`arrayBuffer/base64/dataUrl/buffer/__type:File`），不再把 OpenAI 消息结构里的 `image_url.url` 当文件对象处理；同时修正窗口侧摘要日志，只有 `image_url.url` 非空才标记 `hasImageUrl`。

  - ✅ 已归档：本轮结果已同步记录到 `Agent Recoder.md`（2026-03-24，Agent 工具图片发送链修复归档）。

  - 📝 Git 存档替代（2026-03-24）：由于当前会话受 Git 只读限制，未执行 `git add/commit`。已生成 `archive/2026-03-24-working-tree.patch` 与 `archive/2026-03-24-working-tree-status.txt` 作为当前工作区归档，并补充 `archive/2026-03-24-commit-instructions.md` 提供可直接执行的提交命令。





  - 📝 备注：保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。



  - ✅ C.1.3.g（已完成，2026-03-22）：修复 `main_preload.js` / `fast_preload.js` 中 `openWindow(type, payload)` 参数被截断的问题，确保主控台 `Prompts.vue` 与快捷窗打开独立对话窗口时，`code/payload/options` 能真正传入主进程。
  - ✅ 验证：`pnpm build` 通过（2026-03-22，本轮复测）。


### C.2 核心对话交互与 UI 渲染 ⏳ (待开始)

- [ ] **C.2.1 自定义标题栏 (`TitleBar.vue`)**
  - 移除或重写无边框窗口的拖拽逻辑 (`-webkit-app-region: drag`)。
  - 实现 Windows/macOS/Linux 平台差异化的窗口控制按钮（最小化、最大化、关闭）。
  - 实现“窗口置顶”与“失焦关闭”的界面状态绑定与主进程 IPC 联调。
- [ ] **C.2.2 消息气泡与 Markdown 渲染 (`ChatMessage.vue`)**
  - 确保 `vue-element-plus-x`、`marked`、`DOMPurify` 及其依赖在桌面端正常工作。
  - 修复代码块的高亮显示与“一键复制”按钮的 DOM 注入。
  - 完善内联代码（`code`）点击触发路径跳转或链接打开的操作（`shellOpenPath` / `shellOpenExternal`）。
  - 确保数学公式（KaTeX）和 Mermaid 图表正常渲染。
- [ ] **C.2.3 输入区域交互 (`ChatInput.vue` & `ChatHeader.vue`)**
  - 适配输入框的拖拽文件、粘贴图片行为，转化为可跨进程读取的绝对路径或 Base64 格式。
  - 重构 MCP（`@`）和 Skill（`/`）的快捷指令唤出列表，确保位置不会超出 Electron 窗口边界。

### C.3 多模态文件与长图导出 ⏳ (待开始)

- [ ] **C.3.1 图片查看器与处理**
  - 修复 `ElImageViewer` 在独立窗口下的层级和退出逻辑（按 `Esc` 关闭时不关闭整个程序）。
  - 实现图片查看器中的“复制图片”和“保存图片”原生功能适配。
- [ ] **C.3.2 历史记录导出功能 (`App.vue` 保存逻辑)**
  - 导出为 JSON：接入原生的 `showSaveDialog` 和 `writeLocalFile`。
  - 导出为 HTML / Markdown：确保格式化函数正常运行。
  - **导出为长图 (重点难点)**：
    - 修复 `html2canvas` 在 Electron 环境下的背景透明与跨域图片污染问题。
    - 确保分块渲染（Chunk Render）逻辑不会导致 Electron 渲染进程 OOM（内存溢出）。
- [ ] **C.3.3 自定义背景与毛玻璃特效**
  - 确保主进程缓存背景图（`getCachedBackgroundImage`）的 Blob 传递逻辑顺畅。
  - 校验窗口透明属性（`transparent: true`）与背景模糊（`backdrop-filter`）在不同操作系统的表现。

### C.4 高级 Agent 交互 (MCP & Skills) ⏳ (待开始)

- [ ] **C.4.1 工具调用审批流 (`ChatMessage.vue` 工具卡片)**
  - 确保“等待批准”、“执行中”、“已完成”的状态流转在 UI 端正常反馈。
  - 联调 `testInvokeMcpTool`，验证前端发起中止（AbortController）能否正确中断主进程的执行。
- [ ] **C.4.2 Sub-Agent 后台协同**
  - 确保触发子智能体（Skill Fork 模式）时，后台日志能够通过 IPC 实时回传并在 `Tool Output` 卡片中滚动显示。

### C.5 语音与设备能力 ⏳ (待开始)

- [ ] **C.5.1 系统录音录屏集成**
  - 将 `desktopCaptureSources` 获取到的系统音频流正确剥离并喂给 `MediaRecorder`。
  - 确保麦克风权限在 Electron 打包环境下被正确申请和允许。
- [ ] **C.5.2 语音播放与打断**
  - 确保 AI 返回的音频 Buffer 能够通过 Blob URL 正常挂载到 `<audio>` 标签并播放。

---

## 9. 阶段 D：Fast_window 快捷唤出面板重构 (Phase D) ⏳（待开始）

> 将原生的多个 HTML 重构，使其符合 Electron 最佳实践。

- [ ] **D.1 Fast Input 悬浮输入条**

  - [ ] 重构 `fast_input.html`（可考虑转为轻量级 Vue3 组件或保持原生 HTML）。
  - [ ] 在 `windowManager.js` 中实现**透明、无边框、置顶**窗口属性。
  - [ ] 优化粒子特效性能（CSS Animation 在透明 Electron 窗口下的性能测试）。
  - [ ] 实现失焦自动关闭（监听 `blur` 事件并通知主进程隐藏窗口）。
- [ ] **D.2 追加选择器 (Append Selector)**

  - [ ] 重构 `append_selector.html`。
  - [ ] 实现跨窗口进程通信：获取当前存活的 `Agent Window` 列表并展示。
  - [ ] 实现点击后，将 Payload 转发至主进程，再由主进程投递给特定的对话窗口。

---

## 10. 阶段 E：桌面端专属体验优化 (Phase E) 🟡（进行中）

> 利用纯桌面应用特权，提供原 uTools 版本无法实现的高级功能。

- [/] **E.1 全局快捷键与呼出机制**

  - [/] 主进程注册 `globalShortcut`（本轮目标：主界面 `Ctrl+Space`、召唤界面 `Alt+X`、快捷助手直达快捷键）。
  - [/] 在设置中心 (`Setting.vue`) 添加“桌面端设置 / 自定义全局快捷键”的 UI，并补齐冲突校验与多语言。
  - [ ] 新增 `quick` 召唤窗口（简版）：支持剪贴板文本/图片/文件预填与候选快捷助手列表。
- [/] **E.2 划词直接调用功能 (重磅扩展)**

  - [/] 第一版先基于系统剪贴板文本/图片/文件实现自动带入；“未复制的系统选中内容”延后。
- [/] **E.3 系统托盘 (System Tray) 与后台运行**

  - [/] 创建 `Tray` 图标，添加右键菜单（显示主窗口、打开 quick、退出应用等）。
  - [/] 拦截主窗口的“关闭”事件，改为“隐藏到托盘”（确保快捷召唤与定时任务持续运行）。
- [ ] **E.4 打包与分发**

  - [ ] 优化应用启动速度（Vite 预构建、懒加载等）。
  - [ ] 配置 NSIS (Windows) 打包参数（包括应用图标、安装路径、自启动注册表配置）。
  - [ ] 执行端到端测试，检查数据在打包后的 `userData` 路径读写是否正常。

### E.2026-03-24.d 本轮精修（quick uTools 化 / 文件图片发送链修复） 🟡

- [x] quick UI 按 uTools 风格继续重做（更宽、无内滚动条、输入区缩略图/文件图标、深浅主题一致）。
- [x] quick 支持图片/文件剪贴板识别与粘贴辅助匹配。
- [x] quick 排序改为名称精确匹配 / 前缀 / 首字母缩写优先于类型规则。
- [x] 修复 quick / summon / continue_agent_chats 的文本、图片、文件发送链。
- [x] 去除 desktop 不必要的主窗口隐藏副作用。
- [x] 完成 `pnpm build` 验证，并回写结果/问题。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 quick uTools 化 / 剪贴板与粘贴 / 发送链修复）。
  - 📝 备注：保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。


### E.2026-03-24.e 本轮修复（quick 输入区重构 / 选中捕获 / 文件发送链 / 快捷键录制） 🟡

- [x] quick 顶部输入区改为单行/只读预览模式，避免输入区卷入滚动。
- [x] Alt+X 增加 Windows 下 best-effort 选中内容捕获（模拟 `Ctrl+C` 后读取剪贴板）。
- [x] 修复结构化文件对象进入发送链，兼容 `handleFilePath` 返回对象与原生 File。
- [x] 修复 quick 附件发送与 `continue_agent_chats` 附件发送。
- [x] 桌面端快捷键设置改为“录制键盘输入”模式，不再要求手输字符串。
- [x] 配置保存后主进程热更新快捷键注册，并把配置更新广播扩展到 main/quick。
- [x] 完成 `pnpm build` 验证，并回写结果/问题。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 quick 输入区/录制快捷键/选中捕获/发送链修复）。
  - 📝 备注：保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。



### E.2026-03-24.c 本轮修复（summon自动发送 / quick样式重做 / 恢复对话统一） 🟡

- [x] 修复 `summon_agent` 自动发送文本/文件与自动命名。
- [x] quick 改为单击召唤，并继续向 uTools 风格靠拢（深浅主题一致）。
- [x] quick 排序增加“名称精确匹配 / 名称或首字母缩写匹配”优先级。
- [x] 修复 quick 的 `window:close` 调用方式与报错。
- [x] 修复定时任务等入口恢复对话链路，统一与历史对话页一致。
- [x] quick 支持识别带 `"anywhere_history": true` 的 JSON 对话文件并显示恢复对话入口。
- [x] 完成 `pnpm build` 验证，并回写结果/问题。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 summon 自动发送 / quick 排序与恢复对话 / close API 收敛）。
  - 📝 备注：保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。


### E.2026-03-24.b 本轮修复（summon / toggle / main 唤起交互 / quick 居中改版 / icon） 🟡

- [x] 修复 `summon_agent` 打开默认空助手与 `Error: Failed to create agent window.`。
- [x] 修复 `Ctrl+Space` / `Alt+X` 为 toggle 行为（已显示则隐藏，隐藏则显示）。
- [x] 修复通过快捷键唤起的 main 主界面内容区不可点击问题，收敛托盘隐藏/恢复策略。
- [x] 将 quick 改为居中显示，并向 uTools 召唤界面风格靠拢。
- [x] 修复 Windows 主窗口标题栏图标仍显示 Electron 默认 logo。
- [x] 完成 `pnpm build` 验证，并回写结果/问题。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮 summon/toggle/main 交互/quick/icon 修复）。
  - 📝 备注：保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。


### E.2026-03-24.a 本轮实施（桌面端设置 / 快捷键 / quick / 托盘） 🟡

- [x] 设计桌面端专属共享配置 `config.desktop`，确保可随云同步且不影响 Anywhere 插件侧。
- [x] 新增快捷键注册/冲突校验链路，默认主界面 `Ctrl+Space`、召唤界面 `Alt+X`。
- [x] 新增 quick 简版窗口与 preload/API，支持候选快捷助手列表。
- [x] 修改主窗口右上角关闭行为为“最小化/隐藏到托盘”，由托盘菜单执行真正退出。
- [x] 完成 `pnpm build` 验证，并回写结果/问题。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮桌面端设置 / 快捷键 / quick / 托盘首版）。
  - 📝 备注：保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。

  - ✅ 复验：补齐 quick 单例窗口二次召唤 payload 透传后，`pnpm build` 再次通过（2026-03-24，本轮收口复测）。

---

### E.2026-03-24.f 本轮实施（quick 完整 uTools 化重构） 🟡

- [x] 方案对齐：按“5 秒内复制/当前选中优先、token 化输入、单行预览、Backspace 删除、文本优先匹配、名称/首字母次级匹配、按快捷助手配置决定召唤与是否直发”重构桌面端 quick。
- [x] 底层上下文捕获增强：补齐最近复制时间戳/新鲜度判断，收敛 quick 召唤时的 payload 优先级。
- [x] quick 输入模型重构：将图片/文件/多行文本提升为可管理 token，单行文本作为 query/userText。
- [x] quick 匹配与排序重构：单行文本先文本匹配，再名称/首字母，再其他助手；带 token/多行文本时优先助手名称筛选。
- [x] quick 召唤链重构：点击候选后按快捷助手配置决定窗口类型与是否自动发送，不再一律固定窗口路径。
- [x] 交互收口：支持 Backspace 删除 token、统一单行显示、去除多余滚动与主界面隐藏副作用复核。
- [x] 完成 `pnpm build` 验证，并回写结果/问题。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮完成 quick 完整 uTools 化重构：clipboard watcher、新鲜度判断、token 化输入、单行输入区、Backspace 删除、中文拼音/首字母匹配、showMode 分流召唤、fast 窗口轻量转发器、window:init 补齐 userText 透传）。
  - 📝 备注1：本轮新增依赖 `pinyin-pro`，用于中文名称拼音/首字母检索。
  - 📝 备注2：用户已授权在允许目录执行 `git add . / git commit` 进行阶段存档，但**当前会话仍受系统级 Git 只读约束**，本轮无法执行 Git 写操作；改以 `进度.md` 实时回写 + 补丁/状态文件归档替代。




### E.2026-03-24.g 本轮返修（quick 时间线修正 / 选中捕获生效 / UI 再收口） 🟡

- [/] 将 quick 输入源时间线改为**文本 / 图片 / 文件三路独立时间戳**，按时间顺序只认最新一种输入源，修复“旧文件残留 + 新文本混入”的错误匹配。
- [/] 修复快捷键唤起时的当前选中内容捕获：优先尝试抓选中文件/文本，再与最近 5 秒剪贴板时间线比较，解决“不手动复制粘贴就没有上传”的问题，并补齐多文件场景。
- [/] 修复 quick 顶部 UI 为更接近 uTools 的宽扁搜索条布局，去除页面滚动条与内容区滚动条，收紧为固定高度面板。
- [/] 修复 Esc 全局关闭 quick；补齐 quick 失焦自动关闭；修复 Backspace 行为为“先删输入，输入为空时再删附件 token”。
- [/] 修复文本类型匹配与正则匹配回落：单行文本应先命中文本类型 / 正则满足的快捷助手，并至少覆盖通用类型快捷助手。
- [/] 修复独立窗口文件发送链 `handleFilePath -> file2fileList` 对结构化文件对象的兼容，消除 `readAsDataURL` 的 Blob 类型报错。
- [ ] 完成 `pnpm build` 验证，并回写结果/问题。


- [x] 修复文本类型匹配与正则回落：单行文本现在优先匹配 `over/general` 文本类快捷助手，正则命中优先，其次文本内容命中，最后仍至少覆盖通用类型快捷助手。
- [x] 修复多文件 / 选中文件捕获链：当前唤起 quick 时只认最新输入源，并补齐多文件路径透传与展示。
- [x] 补齐 quick 失焦自动关闭与全局 Esc 关闭。
- [x] 修复独立窗口文件发送链对结构化文件对象的兼容，避免 `FileReader.readAsDataURL` 误读非 Blob 对象导致报错。
- [x] 再次完成 `pnpm build` 验证。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮补齐文本匹配回落 / 多文件捕获 / quick 失焦关闭 / 文件发送链 Blob 兼容）。


- [x] 放开 quick 推荐展示数量限制，并通过缩小 icon / 间距提升可见快捷助手数量，避免部分快捷助手因展示上限被截断不可见。
- [x] 补强 quick 打开后自动聚焦输入框（增加二次延迟聚焦），继续保持失焦关闭。
- [x] 修复独立窗口文件接收链对 IPC 序列化后的 `buffer/base64/url` 三种结构兼容，解决 `plugin.json` 等文本文件仍报“无可用的 Blob 或 Base64 数据”的问题。
- [x] 再次完成 `pnpm build` 验证。
  - ✅ 验证：`pnpm build` 通过（2026-03-24，本轮补齐 quick 展示数量 / 自动聚焦 / 文件 buffer 兼容）。

### E.2026-03-25.a 本轮修复（quick 唤起不再隐藏 main） ✅

- [x] 后端排查并收口 quick 唤起入口，新增 `openQuickWindowPreservingMain()` 与 `ensureMainWindowVisible()`，确保 main 已可见时唤起 quick 不再被隐式隐藏。
- [x] 前端核对 quick 转发/关闭链路：`render/quick/App.vue` 当前仅打开目标窗口并关闭 quick 自身，未发现直接隐藏 main 的前端调用。
- [x] 完成 `pnpm build` 验证，并回写结果/问题。
- [/] 每个子步骤验证通过后执行 Git 存档（本轮仅提交与“quick 唤起不再隐藏 main”直接相关的后端修复代码，避免混入工作区既有改动）。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 quick 唤起保持 main 可见性修复）。
  - 📝 结论：当前源码层未发现“打开 quick 直接调用 `hideMainWindow()`”的前端链路；本轮通过后端显式恢复 main 可见性，收敛 quick 唤起副作用。


### E.2026-03-25.b 本轮修复（quick 关闭误伤 main / disable 状态同步） ✅

- [x] 排查 `Alt+X` 唤起后 quick 关闭/隐藏误伤 main 的真实链路，确认根因是 `quick_preload.js` 直接复用 `main_preload.js`，导致 quick 默认 `windowType/senderId` 被污染为 main。
- [x] 修复 quick 仅关闭/隐藏自身，不再错误影响 main：已重写 `preload/quick_preload.js`，为 quick 提供独立上下文、独立 `senderId` 与独立 `closeWindow` 调用链。
- [x] 修复 quick 仍显示 `enable=false` 快捷助手的问题：`render/quick/App.vue` 当前候选源已改为仅展示 `prompt && prompt.enable !== false` 的快捷助手。
- [x] 打通 main → quick 配置实时同步，确保 enable 状态即时生效：已补 `onConfigUpdated` 监听，并在 quick 收到配置更新后重新应用配置与清理失效选中项。
- [x] 完成 `pnpm build` 验证，并回写结果/问题。
- [x] 每个子步骤验证通过后执行 Git 定向存档。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 quick 专属 preload 重写 / disable 状态同步修复）。
  - 📝 用户复测结果：1）`Alt+X` 呼出 quick 后关闭/跳转不再导致 main 消失；2）disable 状态的快捷助手已不会继续显示在 quick 中。


### E.2026-03-25.e 本轮梳理（quick agent 路由现状总览） 🟡

- [x] Step 1：从后端入口梳理 quick/fast/window 的窗口创建与路由分发关系（`main/index.js` / `main/windowManager.js` / `main/ipcHandler.js`）。
  - ✅ 验证：已确认后端主链为 `shortcutManager -> main/index.js(triggerQuickSummon/triggerPromptShortcut) -> ipc/windowManager.openWindow(...)`；其中 `quick` 为单例窗口、`window` 为多实例窗口，`window:open` 是统一 IPC 入口，`window:init` 承担 quick/window 初始化载荷注入。
- [x] Step 2：梳理 quick 相关 preload / IPC 暴露链路（`preload/quick_preload.js` 及其调用面）。
  - ✅ 验证：已确认 `quick_preload.js` 为 quick 专属桥接层，不再复用 main preload；其核心能力包括 `openWindow/listWindows/emitWindowEvent/closeWindow/getConfig/readClipboardPayload/captureSelectionPayload`，并通过 `window:init` 持续维护 quick 自身的 `windowType/senderId/latestInitMessage`，为前端路由判断提供上下文。
- [x] Step 3：梳理 quick 前端候选、点击、转发与关闭链路（`render/quick` / `render/fast_window`）。
  - ✅ 验证：已确认 `render/quick/App.vue` 负责读取剪贴板/选中内容、生成附件态（文本/图片/文件）、按 `prompt.type + 名称/拼音/正则/文本` 排序候选；点击候选时根据 `prompt.showMode` 分流到 `openWindow('fast'|'window', payload)`，随后关闭 quick。`render/fast_window/App.vue` 只做轻量中转：收到 `window:init` 后立即把 payload 转发到独立 `window`，再关闭自身。
- [x] Step 4：梳理 quick 命中 agent 后，如何进入独立对话窗口/Agent 工具链并完成消息投递。
  - ✅ 验证：已确认 `openWindow('window', payload)` 最终由 `main/windowManager.js` 生成 `window:init`，把 `code/type/payload/userText/summonData/taskConfig/tempPromptConfig/senderId` 注入 `render/window/App.vue -> initializeWindow()`；窗口侧再按 `type` 分支处理：`over/img/files/task/summon` 分别决定“仅预填”还是“直接压入 history/chat_show 并 askAI/sendAgentToolMessage 自动发送”。Agent 工具链则通过 `main/core/mcp_builtin.js` 直接复用这一窗口能力：`summon_agent` 打开 `type='summon'` 的独立窗口，`continue_agent_chats/read_agent_chats/list_agent_chats` 则经 `window.__AGENT_API__` 与已打开窗口交互。
- [x] Step 5：交叉验证整体链路，输出“后端 → preload → 前端 → agent window”的结构化总结。
  - ✅ 验证：已完成交叉核对，当前 quick 不是直接调用 Agent API 的前端聊天面板，而是“内容捕获 + 快捷助手匹配 + 路由分发器”；真正的 Agent 会话初始化、自动发送、跨窗读写都收敛在独立 `window` 与 `main/core/mcp_builtin.js`。
- [x] Step 6：每完成一步即验证并执行 Git 定向存档。
  - ✅ 已执行 Git 定向存档：`8cc1da0`（Step 1 后端入口）、`af3c2d6`（Step 2 preload）、`0845f9c`（Step 3 前端候选/转发）；本轮收口后继续补充总结提交。


### E.2026-03-25.d 本轮实施（先同步插件更新：`b646211` / `22e2fb4`） 🟡

- [x] Step 1：同步 `b646211` 到桌面端后端/窗口链路（`main/core/data.js`、`render/main/components/Prompts.vue`、`render/window/App.vue`）。
  - 已同步：`main/core/data.js` 配置版本与 `autoSaveChat(_global)` 布尔归一化；`render/main/components/Prompts.vue` 保存 prompt 时强制 `autoSaveChat` 布尔化；`render/window/App.vue` 失焦关闭监听改为统一 `syncAutoCloseOnBlurListener()` 收口，并在 loading 前后动态启停，修复失焦关闭与自动保存互相影响的问题。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 Step 1：同步插件提交 `b646211`）。
- [x] Step 2：同步 `22e2fb4` 到桌面端工具显示与 memory 提示词（`render/window/components/ChatMessage.vue`、`main/core/mcp_builtin.js`）。
  - 已同步：`main/core/mcp_builtin.js` 中 memory 工具描述与入参说明；`render/window/components/ChatMessage.vue` 中工具调用标题增加 tooltip、长名称省略展示与头部布局收口。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 Step 2：同步插件提交 `22e2fb4`）。
- [ ] Step 3：在上述同步完成后，再继续改进 quick（后端 → preload → 前端）。
- [ ] 每个子步骤完成后执行 `pnpm build` 验证；验证通过后执行 Git 定向存档。


### E.2026-03-25.c 本轮整理（工作区分组提交） ✅

- [x] 盘点当前未提交改动，按功能域拆分提交批次。
- [x] 确认每个批次的文件边界与验证方式。
- [x] 分组方案：A=底层剪贴板时间线/选中捕获 + `pinyin-pro` 依赖（`main/core/system.js`、`main/ipcHandler.js`、`package.json`、`pnpm-lock.yaml`）✅ 已提交；B=`render/fast_window/App.vue` 产品态改造 ✅ 已提交；C=`render/window/App.vue` 文件接收链兼容修复 ✅ 已提交。
- [x] 回写 `进度.md` 与提交结果。
  - ✅ Commit A：`65a0138 feat(quick): add clipboard timeline and selection capture support`
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮分组提交 A：剪贴板时间线 / 选中捕获 / 依赖）。
  - ✅ Commit B：`fdb2796 feat(fast-window): upgrade fast window to prompt forwarding card`
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮分组提交 B：fast_window 产品态改造）。
  - ✅ Commit C：`510d284 fix(window): support serialized file payload variants`
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮分组提交 C：window 文件接收链兼容修复）。


### E.2026-03-25.f 本轮实施（quick 匹配机制优化） ✅

- [x] Step 1：收敛 quick 输入源 freshness 规则，只自动带入最近 5 秒内的选中/复制文件、图片、文本。
  - ✅ 验证：已核对 `main/core/system.js`，当前 `CLIPBOARD_FRESHNESS_MS = 5000` 已在 `captureSelectionPayload/readClipboardPayload -> getFreshClipboardPayload()` 生效；超过 5 秒会自动回退为空输入，不再自动带入 quick。
- [x] Step 2：重构 `render/quick/App.vue` 候选池生成逻辑，严格区分“无附件输入”与“有附件输入”的匹配/筛选规则。
  - ✅ 验证：已按规则改写 `candidateSections`：无附件时走“名称匹配 + 文本正则/文本类型 + 通用类型”三路并集排序；有文件/图片/多行文本附件时，先限定候选池，再在有输入时只保留名称命中项。
- [x] Step 3：收紧名称匹配边界，仅允许快捷助手名称 / 名称缩写 / 拼音缩写参与名称匹配，不再混入提示词正文。
  - ✅ 验证：已删除 `buildPromptSearchProfile()` 中对 `prompt.prompt` 的名称匹配参与，名称匹配现在仅基于 `key + 拼音/首字母`。
- [x] Step 4：执行 `pnpm build` 验证；每个子步骤验证通过后执行 Git 定向存档。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 quick 匹配机制优化）。
  - 📝 结论：当前 quick 已满足“5 秒 freshness 自动带入 + 无附件三路排序 + 有附件名称硬筛 + 名称匹配不混入提示词正文”的目标规则；保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。


### E.2026-03-25.g 本轮回归修复（quick 附件清除 / 自动带入 / 打开助手崩溃） ✅

- [x] Step 1：修复 quick 在“只有附件、无文本”时按 Backspace 无法清除附件的问题。
  - ✅ 验证：已放宽 `handleGlobalKeydown` 对焦点元素的判断；当前输入框为空且存在附件时，无论焦点在输入框或页面本体，Backspace 都会触发 `clearAttachment()`。
- [x] Step 2：修复选中/复制文件、图片后打开 quick 时未自动带入的问题。
  - ✅ 验证：已修复 quick 打开时“`window:init` 注入附件态被后续 `refreshFromClipboard(true)` 清空/覆盖”的竞态；当前优先保留主进程注入的 files/img/over 初始化载荷，不再被后续空剪贴板刷新覆盖。
- [x] Step 3：修复从 quick 打开快捷助手后软件崩溃的问题。
  - ✅ 验证：已移除 quick 渲染层“打开目标窗口后立即主动 `closeQuick()` + blur 再次 close”的双关闭链路，改为只负责 `openWindow(...)`，交由主进程 quick 单例窗口的 blur-hide 策略自然收口，避免打开助手时触发 quick 自身重复关窗竞态。
- [x] Step 4：执行 `pnpm build` 验证；每个子步骤验证通过后执行 Git 定向存档。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 quick 回归修复）。
  - 📝 备注：保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。


### E.2026-03-25.h 本轮修复（quick 退出崩溃 / destroy 方案） ✅

- [x] Step 1：为 quick 退出链路补充最小必要日志，定位崩溃发生在 `window:close` 还是 `BrowserWindow.close()` 阶段。
  - ✅ 已补日志：当前仅在 `main/ipcHandler.js` 与 `main/windowManager.js` 打开 quick 退出链路相关日志，便于后续从控制台确认 quick 退出实际走到了哪一步。
- [x] Step 2：将 quick 单例窗口关闭策略从 `close()` 切换为 `destroy()`，绕过 Chromium/Electron 的 renderer unload/close 链。
  - ✅ 已实现：`main/windowManager.js#closeWindow()` 现对 `windowRef === 'quick'` 强制走 `destroy()`；多实例 `window` 仍保持 destroy，其它单例窗口维持原策略不变。
- [x] Step 3：执行 `pnpm build` 验证；验证通过后执行 Git 定向存档。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 quick destroy 方案修复）。
  - 📝 备注：本轮暂时保留 quick 退出链路日志，便于继续根据你的实测定位；保留既有非阻塞告警——`main/core/mcp.js` 被静态/动态同时引入，不影响本轮构建产物。


### E.2026-03-25.i 本轮修复（quick 附件退格 / 自动带入 / SVG 发送链 502） ✅

- [x] Step 1：修复 quick 在“只有附件、无文本”时按退格键仍无法稳定清除附件的问题。
  - ✅ 已修复：补回 `inputRef` 定义，当前 `handleGlobalKeydown` 不再因 `inputRef` 未定义导致逻辑短路；输入框为空且有附件时，Backspace 可稳定清除附件。
- [x] Step 2：修复选中/复制文件、图片后打开 quick 时未自动带入的问题，并核对 init payload 与 clipboard freshness 的优先级。
  - ✅ 已修复：`refreshFromClipboard(true)` 现会在已有 `window:init` 初始化载荷时直接跳过，避免主进程注入的 files/img 被后续刷新覆盖为空。
- [x] Step 3：修复通过 quick 打开的快捷助手对话在发送 SVG 时误走 `image_url(data:image/svg+xml...)`，应改为与常规文件链一致的文本文件/文件发送路径，消除 502。
  - ✅ 已按原插件思路收敛：撤回 `.svg` 硬编码白名单方案，恢复为“未知扩展名 + 内容探针”；同时修复 quick 入口层，若捕获到的是 `data:image/svg+xml`，不再直接按 `img` 路由，而是降级为 `over` 文本输入，避免 SVG 被误当多模态图片发送。
  - ✅ 已增强 Windows 文件剪贴板读取：`main/core/system.js` 现同时尝试 `FileNameW / text/uri-list / CF_HDROP`，尽量优先拿到真实文件路径，而不是落到 imageDataUrl 分支。
- [x] Step 4：执行 `pnpm build` 验证；必要时运行 `pnpm dev` 观察日志；验证通过后执行 Git 定向存档。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 quick 按原插件逻辑对齐修复）。
  - 📝 结论：当前 quick 的 `.svg` 处理已回到“参考项目优先”的思路——不是把 `.svg` 当图片白名单，而是避免 quick 入口误判为图片，并让文件类型判定继续走原插件风格的“白名单 + 二进制探针”链路。


### E.2026-03-25.j 本轮修复（quick 选中优先 / 自动聚焦 / SVG 入口误判） ✅

- [x] Step 1：按“先读 5 秒内新剪贴板，再检测当前选中内容；若选中有效则覆盖剪贴板结果”的规则重构 quick 入口 payload 采集。
  - ✅ 已实现：`captureSelectionPayload()` 现先保留 5 秒内新剪贴板内容，再尝试当前选中；若选中捕获成功，则覆盖此前的剪贴板结果。
- [x] Step 2：增强 Windows 选中捕获等待与轮询，提升 Explorer 文件/图片选中后被识别为 `filePaths` 的概率。
  - ✅ 已实现：`tryCaptureSelectionToClipboard()` 已增加更长等待和 3 轮补轮询，尽量等到 Explorer/系统把文件选中结果真正写入剪贴板。
- [x] Step 3：修复 quick 打开后自动聚焦不足的问题，并收紧 quick 入口与粘贴链对 SVG 的误判，不再把 SVG 当作图片附件。
  - ✅ 已实现：quick 打开后新增多轮延迟 `focusInputToEnd()`；同时 `applyImageAttachment()` 对 `data:image/svg+xml` 直接降级为文本输入，不再显示成图片附件 token。
- [x] Step 4：执行 `pnpm build` 验证；验证通过后执行 Git 定向存档。
  - ✅ 验证：`pnpm build` 通过（2026-03-25，本轮 quick 选中优先 / 自动聚焦 / SVG 入口误判修复）。
  - 📝 结论：当前 quick 入口已调整为“新鲜剪贴板优先、选中覆盖”的策略，并增强了 Explorer 选中文件捕获与打开后自动聚焦；若仍存在特殊文件被误判，下一步应继续抓取 `captureSelectionPayload()` 的原始 formats/filePaths/imageDataUrl 输出对症修正。


## 18. 用户要求的“下一步严格顺序”

1. **进入 Phase C：Anywhere_window 完整复刻**（UI/聊天/工具审批/长图导出/协同联调）
