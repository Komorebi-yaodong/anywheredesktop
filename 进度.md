# Anywhere Desktop 重构进度（整理版）- `[x]` Step G 已完成：已新增自动追问全局快捷键默认 `Alt+S`，并将 quick 默认召唤快捷键默认值改为 `Alt+A`；主进程已支持“只有一个窗口时直接追问、多个窗口时以 append-only 模式打开 quick”。- `[x]` Step I 已完成：已补上桌面设置页中的“自动追问快捷键”可视化配置入口，当前默认值为 `Alt+S`，并复用既有快捷键录制/保存链路。- `[x]` Step L 已完成：紧急止血修复 quick 白屏与 Alt+S 无响应回归；根因是 `render/quick/App.vue` 新增 `watch(...)` 后遗漏导入 `watch`，导致 quick 渲染期直接报错白屏。现已修复，并把 Alt+S 在“无 payload”场景下改为仍可进入 append-only 追问界面，不再表现为完全无反应。- `[x]` Step M 已完成：已对 quick 的追问目标区样式做视觉收口——压缩 append-only 模式的垂直留白、收紧 chip 高度/头像尺寸/角标尺寸，并让自动追问目标区支持更自然的换行排列，减少空白感与错位感。
- `[x]` Step M.1 已完成：样式收口后已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。


- `[x]` Step L.1 已完成：紧急回归修复后已再次执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。


- `[x]` Step J 已完成：已修复 quick 追问目标列表角标不显示的问题；`quick_preload` 现会透传 `promptOrdinal / promptWindowCount`，同一助手多窗口时右上角角标可正常显示。
- `[x]` Step K 已完成：已收紧 append-only 模式显示——隐藏匹配推荐与恢复区，并进一步压缩窗口默认高度；当前多窗口自动追问时不再保留大面积空白。
- `[x]` Step K.1 已完成：本轮回归修复已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。


- `[x]` Step H 已完成：同一快捷助手的多个窗口现已支持右上角角标编号；编号按当前存活窗口的出现顺序动态重排，例如同一 prompt 关闭中间窗口后再新开，界面仍展示为连续的 `1/2/3`。
- `[x]` Step H.1 已完成：本轮后端/配置层已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step H.2 已完成：quick 前端现已支持 `append-only` 模式——在多个窗口自动追问时，只显示下方追问目标区，不显示上方快捷助手；同一快捷助手的多个窗口已增加右上角角标编号，并按当前存活窗口的出现顺序编号。
- `[x]` Step H.3 已完成：前端 quick 阶段已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step N 已完成：`Alt+S` 自动追问现已与 `Alt+A` 一致；当 quick 正处于 `append-only` 追问界面且窗口已可见聚焦时，再按一次 `Alt+S` 会立即隐藏界面。
- `[x]` Step N.1 已完成：后端主进程已为 quick 单例缓存当前 `triggerMode`，并让 `Alt+S` 在 `append-only` + 已可见聚焦时直接隐藏。
- `[x]` Step N.2 已完成：已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step N.3 已完成：已执行 Git 定向提交存档 `397241e fix(quick): allow alt+s to hide append-only summon`。
- `[x]` Step O 已完成：已修复 `Alt+S` 追问界面二次打开高度不稳定，并收口 `Alt+S` 相比 `Alt+A` 打开明显偏慢的问题。
- `[x]` Step O.1 已完成：后端主进程现已按 quick `triggerMode` 直接生成稳定窗口高度，避免主进程固定高度与前端 `resizeTo` 双重控制导致的二次打开尺寸跳变。
- `[x]` Step O.2 已完成：后端已将 `Alt+S` 多窗口追问链路改为“先打开 append-only quick，再异步采集 payload 并注入”，收口打开体感。
- `[x]` Step O.3 已完成：前端已移除 quick 的窗口二次 `resizeTo`，改为仅根据模式渲染内容，不再主动改窗体高度。
- `[x]` Step O.4 已完成：已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step O.5 已完成：已执行 Git 定向提交存档 `bb74a77 fix(quick): stabilize append-only height and speed up alt+s`。
- `[x]` Step P 已完成：已修复追问与 quick 召唤链路下快捷助手“文本/图片/文件自动发送”失效问题；当前追问默认自动发送，quick 召唤也会继续遵循快捷助手的自动发送配置。
- `[x]` Step P.1 已完成：已修复 append/quick 到 window 的 payload 协议中自动发送语义丢失问题，补齐 `multiline-text`、图片附带文本与文件直发场景。
- `[x]` Step P.2 已完成：已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step P.3 已完成：已执行 Git 定向提交存档 `a66e429 fix(window): restore direct-send semantics and sync export/perf tweaks`。
- `[x]` Step Q 已完成：已同步插件端 `7f20d08` 的导出行为收口，并从 `1f725fc` 中择要同步独立窗口的性能收口（滚动锚点 / 去除 MutationObserver 强推滚动 / 流式渲染节流刷新）。
- `[x]` Step Q.1 已完成：已分析插件端两个 commit 的真实改动与桌面端映射文件。
- `[x]` Step Q.2 已完成：已在桌面端完成必要同步。
- `[x]` Step Q.3 已完成：已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step Q.4 已完成：已执行 Git 定向提交存档 `a66e429 fix(window): restore direct-send semantics and sync export/perf tweaks`。
- `[x]` Step R 已完成：已准备 GitHub Actions 三平台（Windows / macOS / Linux）手动打包方案，统一从 `resources/icon.png` 生成各平台图标资源，并补齐构建所需的 `build/` 目录资源。
- `[x]` Step R.1 已完成：已创建 `build/` 目录并补齐 `entitlements.mac.plist`，后续由 workflow 基于 `resources/icon.png` 动态生成平台图标。
- `[x]` Step R.2 已完成：已调整 `electron-builder.yml`，移除不必要的发布配置并收口 Linux 目标与三平台图标路径。
- `[x]` Step R.3 已完成：已创建仅支持手动触发的 GitHub Actions 三平台打包 workflow，并在构建前自动从 `resources/icon.png` 生成平台图标。
- `[x]` Step R.4 已完成：已执行 `pnpm build` 做基线验证，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step R.5 已完成：已执行 Git 定向提交存档 `69d3756 build(ci): add manual multi-platform packaging workflow`。
- `[x]` Step S 已完成：已修复 GitHub Actions 三平台打包 workflow 的 `pnpm` 初始化失败，并排查/修复本地安装包启动时报 `Cannot find module '@langchain/core/tools'` 的打包缺依赖问题。
- `[x]` Step S.1 已完成：已修复 workflow 中 `pnpm` 安装/缓存顺序，并增加 Node 24 actions 环境变量收口告警。
- `[x]` Step S.2 已完成：已定位 `@langchain/mcp-adapters` 对 `@langchain/core` 的必需 peer 依赖缺失，并在项目依赖中显式补齐，修复打包后运行时缺模块问题。
- `[x]` Step S.3 已完成：已执行 `pnpm build` 与 `pnpm build:unpack` 验证；构建与本地 unpack 打包均通过（2026-04-07），保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step S.4 已完成：已执行 Git 定向提交存档 `37b95db fix(build): restore ci pnpm setup and package langchain core`。
- `[x]` Step T 已完成（阶段一）：已定位 Windows 安装包发布质量问题的主要根因——安装目录不可选源于 NSIS one-click 默认配置；`example.com` 源于项目元数据；安装体积偏大则主要由 `app.asar`（约 365MB）与 Electron Runtime（约 201MB）构成。
- `[x]` Step T.1 已完成：已定位安装体积偏大的真实来源（`resources/app.asar` / Electron Runtime / locales），并确认当前不是单纯安装器压缩问题。
- `[x]` Step T.2 已完成（阶段一）：已修复 NSIS 安装目录选择能力，并将项目作者邮箱更新为 `BY2439213@buaa.edu.cn`；同时确认 Windows“发布者”在当前未签名场景下不能仅靠 builder 字段稳定伪装，最终需依赖代码签名证书。
- `[x]` Step T.3 已完成（阶段一）：已执行 `pnpm build` 与 `pnpm build:unpack` 验证；构建与 unpack 打包通过（2026-04-07），保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[ ]` Step T.4 待办：如需继续压缩体积，下一步进入 `app.asar` 深度瘦身与 Windows 签名方案，再执行 Git 定向提交存档。
- `[x]` Step U 已完成（低风险瘦身阶段）：已系统化拆解 Windows 安装包体积来源，并完成两轮低风险减重。
- `[x]` Step U.1 已完成：已拆解 `app.asar` 顶层目录与大文件占比，确认核心热点是 `node_modules`（约 336MB），其中全局 `*.map` 约 148.69MB、误打包 `.tmp` 约 22.97MB，为首轮减重主要目标。
- `[x]` Step U.2 已完成：已完成低风险瘦身——排除 `.tmp/**`、全局 `*.map`，保留有效的 Windows 语言裁剪，并将仅在渲染层使用的大型 UI/渲染库迁移到 `devDependencies` 以减少打包主包依赖体积。
- `[x]` Step U.3 已完成：已执行 `pnpm build:unpack` 验证体积变化与运行完整性；最新产物约为 `dist/anywhere-desktop-1.0.0-setup.exe` **122.99MB**、`dist/win-unpacked` **497.15MB**，较此前显著下降；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[ ]` Step U.4 待办：如需继续压缩体积，下一步进入大依赖子目录裁剪/运行时按需加载级别的深度瘦身，再执行 Git 定向提交存档。
- `[x]` Step V 已完成：已修复 GitHub Actions Linux 打包链路中无效的 `.icns` 生成步骤，收口三平台 workflow 的图标生成逻辑。
- `[x]` Step V.1 已完成：已移除 Linux job 中不存在的 `@fiahfy/icns-cli` 调用，仅保留 Linux 实际需要的图标生成。
- `[x]` Step V.2 已完成：已为 workflow 增加图标生成结果检查日志，便于后续排错。
- `[ ]` Step V.3 待办：验证通过后执行 Git 定向提交存档。
- `[x]` Step W 已完成：已修复安装包场景下全局快捷键整体失效（主界面 / quick / 追问 / 快捷助手），并同步收口 GitHub Actions 云端打包产物与跨平台构建报错。
- `[x]` Step W.1 已完成：已定位安装包场景下全局快捷键整体失效的核心根因——历史配置迁移未补齐 `desktop.shortcuts.appendFollowUp`，而主进程启动时会把该字段当成必填项校验，导致旧用户升级后 `syncDesktopShortcuts` 在启动期直接失败；同时确认 `promptBindings` 历史脏值（空快捷键 / 空 promptKey）也可能放大全局失败范围。
- `[x]` Step W.2 已完成：已修复快捷键注册容错与配置清洗——补齐 `appendFollowUp` 默认迁移、把 `quickSummon` 旧回退值收口为 `Alt+A`、清洗无效 `promptBindings`，并让主进程在同步失败时至少不中断应用启动。
- `[x]` Step W.3 已完成：已执行本地 `pnpm build` 与 `pnpm build:win` 验证；主线构建与 Windows 安装包打包均通过（2026-04-07），当前仅保留既有 `main/core/mcp.js` 静态 + 动态引入告警，以及 electron-builder 子进程 `DEP0190` 非阻塞告警。
- `[x]` Step W.4 已完成：已重写 GitHub Actions workflow，只上传各平台最终可分发产物——Windows `*-setup.exe`、Linux `*.AppImage`、macOS `*.dmg`，不再上传整包目录或无关文件。
- `[x]` Step W.5 已完成：已修复 Linux / macOS 云端打包失败关键项——workflow 现统一生成 512/1024 图标资源，解决 Linux `build/icon.png must be at least 256x256`；同时移除仓库 `.npmrc` 中失效的 `electron_builder_binaries_mirror=npmmirror`，并将构建脚本统一显式改为 `--publish never`，规避 macOS 隐式发布与 `dmg-builder` 镜像 404。
- `[x]` Step W.6 已完成：已执行 Git 定向提交存档 `e6f9309 fix(desktop): restore packaged shortcuts and stabilize ci packaging`。
- `[x]` Step X 已完成：已将项目图标源切换为用户指定的 SVG 像素机器人，并收口本地/云端多分辨率图标生成链路。
- `[x]` Step X.1 已完成：已新增 `resources/icon.svg` 作为主图标源，并按同一像素矩形定义生成 `resources/icon.png`（1024x1024）供运行时与打包链路兼容使用。
- `[x]` Step X.2 已完成：已更新 GitHub Actions workflow，改为优先从 `resources/icon.svg` 生成各平台图标资源。
- `[x]` Step X.3 已完成：已执行 `pnpm build` 验证图标替换后构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step X.4 已完成：已执行 Git 定向提交存档 `ef07e46 build(icon): switch source icon to svg pixel robot`。
- `[x]` Step Y 已完成：已二次修复云端打包与安装包运行时问题——移除 workflow 中残留的自动图标生成步骤，并修复 Windows 安装包启动时报 `data-uri-to-buffer` 缺失。
- `[x]` Step Y.1 已完成：已精简 workflow，直接复用仓库内现成的 `build/icon.ico` / `build/icon.icns` / `build/icon.png`，不再依赖 ImageMagick / `magick` 自动生成图标；同时显式覆盖 `ELECTRON_BUILDER_BINARIES_MIRROR`，避免 macOS 继续命中 `npmmirror` 的 `dmg-builder` 404。
- `[x]` Step Y.2 已完成：已定位 `data-uri-to-buffer` 为运行时缺失依赖来源（经 `node-fetch@3` 间接依赖触发），并已显式补入项目依赖，修复 Windows 安装包主进程启动时报 `ERR_MODULE_NOT_FOUND` 的问题。
- `[x]` Step Y.3 已完成：已执行本地 `pnpm install`、`pnpm build` 与 `pnpm build:win` 验证；构建与 Windows 安装包打包通过（2026-04-07）。
- `[x]` Step Y.4 已完成：已执行 Git 定向提交存档 `847a9f9 fix(build): reuse checked-in icons and package missing data-uri dependency`。
- `[x]` Step Z 已完成：已继续修复 macOS DMG 打包仍命中 `npmmirror` 与 Windows 安装包第二层缺依赖（`fetch-blob`）问题。
- `[x]` Step Z.1 已完成：已定位 macOS 问题来自 `dmg-builder` 二进制下载链仍命中 `npmmirror`；当前已在 workflow 同时显式覆盖 `ELECTRON_BUILDER_BINARIES_MIRROR` 与 `npm_config_electron_builder_binaries_mirror`，并将 mac 云端产物先收口为 `zip`，绕开 `dmg-builder` 外部二进制下载。
- `[x]` Step Z.2 已完成：已补齐 Windows 安装包运行时缺失的 `fetch-blob` / `formdata-polyfill` 依赖，并确认 asar 已打入对应模块，修复安装包第二层 `ERR_MODULE_NOT_FOUND`。
- `[x]` Step Z.3 已完成：已执行本地 `pnpm build` 与 `pnpm build:win` 验证；构建与 Windows 安装包打包通过（2026-04-07）。
- `[x]` Step Z.4 已完成：已执行 Git 定向提交存档 `2b9981c fix(build): avoid dmg mirror path and package fetch blob chain`。
- `[x]` Step AA 已完成：已继续排查 Windows 安装包下一层缺依赖，并收口 mac `zip` 打包方案与产物命名。
- `[x]` Step AA.1 已完成：已补齐 `node-fetch` 运行时整条依赖链（含 `fetch-blob` / `formdata-polyfill` / `node-domexception`），避免安装包逐层爆缺依赖。
- `[x]` Step AA.2 已完成：已验证 mac `zip` 方案的脚本、artifact 匹配与 CI 产物命名现在一致——脚本输出 `zip`，workflow 产物匹配 `dist/*-mac.zip`。
- `[x]` Step AA.3 已完成：已执行本地构建与必要包内容验证；`pnpm build:win` 通过，且 `app.asar` 已确认包含 `data-uri-to-buffer` / `fetch-blob` / `formdata-polyfill` / `node-domexception`（2026-04-07）。
- `[x]` Step AA.4 已完成：已执行 Git 定向提交存档 `bec1aaa fix(build): complete node-fetch runtime chain for packaged app`。
- `[x]` Step BB 已完成：已收口桌面端运行时能力——支持复杂全局快捷键（如 `Ctrl+Shift+~`）并修复 WebDAV 备份恢复未走系统代理导致的明显卡慢问题。
- `[x]` Step BB.1 已完成：已扩展快捷键录制与主进程快捷键规范化，对 OEM / 标点键建立稳定映射，当前支持 `Backquote / Minus / Equal / BracketLeft / BracketRight / Backslash / Semicolon / Quote / Comma / Period / Slash` 等键位，可覆盖 `Ctrl+Shift+~` 等复杂组合。
- `[x]` Step BB.2 已完成：已收口前端录制与主进程标准化规则，使复杂快捷键的录制显示、冲突检测与主进程注册行为保持一致。
- `[x]` Step BB.3 已完成：已重构 WebDAV 网络层，当前 `main/core/webdav.js` 底层请求改为复用 `fetchWithProxy`，默认走桌面端统一代理策略 / 系统代理链，收口恢复备份速度问题。
- `[x]` Step BB.4 已完成：已执行本地 `pnpm build` 验证通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step BB.5 已完成：已执行 Git 定向提交存档 `f3cdeb3 fix(runtime): support complex shortcuts and proxy webdav fetches`。
- `[x]` Step CC 已完成：已统一剩余未收口的渲染层联网出口——窗口内直连 WebDAV、主控台文档拉取与窗口图片远程读取现均改走主进程统一代理出口。
- `[x]` Step CC.1 已完成：已替换 `render/window/App.vue` 中直连 WebDAV 的保存/重命名逻辑，改为复用主进程 `webdav:*` IPC 能力，避免渲染层自行创建 WebDAV client。
- `[x]` Step CC.2 已完成：已收口 `render/main/App.vue` 中 GitHub / Gitee 文档拉取链路，并将 `render/window/App.vue` 图片复制/下载的远程读取统一改为走主进程 `file:readRemoteText / file:readRemoteBinary`，统一复用系统代理网络层。
- `[x]` Step CC.3 已完成：已执行本地 `pnpm build` 验证通过，并复核渲染层残留网络出口；当前仅保留无直接网络调用的构建产物变更与既有 `main/core/mcp.js` 动态/静态引入告警（非本步骤阻塞项）。
- `[x]` Step CC.4 已完成：已执行 Git 定向提交存档 `a2d4c65 fix(proxy): route remaining renderer network calls through main`。
- `[x]` Step DD 已完成：已接入用户新提供的 logo 资源（`resources` + `build`），统一桌面端运行时与打包链路的图标引用；本轮仅处理 logo，未改动追问功能代码。
- `[x]` Step DD.1 已完成：已盘点 `resources` / `build` 下的新图标资源；当前运行时主链路使用 `resources/icon.png`，打包链路使用 `build/icon.ico` / `build/icon.icns` / `build/icon.png`，新 logo 文件已到位。
- `[x]` Step DD.2 已完成：已复核运行时图标链路——主进程托盘与 BrowserWindow 创建链路均使用 `resources/icon.png`，并修复窗口默认 AI 头像仍写死开发机绝对路径的问题，现统一改为随资源打包生效的 asset 引用。
- `[x]` Step DD.3 已完成：已复核 electron-builder 图标配置继续正确指向 `build/icon.ico` / `build/icon.icns` / `build/icon.png`，与本轮新 logo 资源一致，无需额外改动构建配置。
- `[x]` Step DD.4 已完成：已执行 `pnpm build` 验证，构建通过（2026-04-08）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step DD.5 已完成：已对本轮 logo 接入相关文件执行 Git 定向提交存档。
- `[x]` Step EE 已完成：已修复本地/WebDAV 配置恢复时旧插件配置会把桌面端设置重置为默认的问题；当前恢复策略为“备份含 desktop 时同步、不含 desktop 时保留当前桌面设置”。
- `[ ]` Step EE.1 待办：定位本地配置恢复与 WebDAV 配置恢复统一入口，收口 desktop 缺失时的合并策略。
- `[ ]` Step EE.2 待办：验证旧插件配置恢复后桌面设置不再被清空。
- `[ ]` Step EE.3 待办：验证含 desktop 的桌面端配置恢复后桌面设置可同步。
- `[ ]` Step EE.4 待办：验证通过后执行 Git 定向提交存档。
- `[x]` Step EE.1 已完成：已定位本地配置恢复与 WebDAV 配置恢复统一入口，新增后端 `restoreImportedConfig` 合并策略；当备份显式包含 `desktop / settingsCardOrder / settingsCardCollapsed` 时才覆盖，否则保留当前桌面端值；`skillPath / webdav.localChatPath` 继续走“缺失即保留当前值”。
- `[x]` Step EE.2 已完成：恢复链路现已避免旧插件配置把桌面快捷键/closeToTray 等桌面设置重置为默认；前端本地恢复与 WebDAV 恢复均已改为复用统一恢复接口。
- `[x]` Step EE.3 已完成：统一恢复接口保留“备份显式包含 desktop 时同步桌面设置”的能力；桌面端备份恢复链路语义已收口。
- `[x]` Step EE.4 已完成：已执行 `pnpm build` 验证通过（2026-04-08）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。

- `[x]` Step EF 已完成：已排查并收口 Windows 安装后桌面图标仍显示旧 logo 的问题；图标资源、安装器图标配置与重新打包产物现已统一到新 logo。
- `[x]` Step EG 已完成：已将独立对话窗口的文件支持性检测前置到“粘贴/拖拽/选择文件”进入输入区时；当前不再等到点击发送后才提示不支持并仅发送文本。
- `[x]` Step EH 已完成：已修复快捷键召唤快捷助手与 quick 召唤快捷助手时错误绕过快捷助手“文本/图片/文件直接发送”配置的问题。
- `[ ]` Step EH.1 待办：定位桌面端快捷键召唤 / quick 召唤 / 自动提交判定链路，并对照插件实现。
- `[ ]` Step EH.2 待办：确保文本、图片、文件是否自动发送严格遵循快捷助手 `isDirectSend_normal / isDirectSend_image / isDirectSend_file` 配置。
- `[ ]` Step EH.3 待办：保留追问/快捷召唤既有 payload 注入能力，但关闭 direct-send 时仅进入草稿、不自动提交。
- `[ ]` Step EH.4 待办：执行 `pnpm build` 验证，并回归文本/图片/文件三种 direct-send 开关行为。
- `[ ]` Step EH.5 待办：验证通过后执行 Git 定向提交存档。
- `[x]` Step EH.1 已完成：已定位桌面端快捷键召唤 / quick 召唤 / 自动提交判定链路，并对照插件实现确认 direct-send 应由快捷助手自身配置控制。
- `[x]` Step EH.2 已完成：已修复快捷键召唤快捷助手时错误绕过 `isDirectSend_normal / isDirectSend_image / isDirectSend_file` 的问题；当前追加 payload 事件在 `triggerMode === 'shortcut'` 时会严格遵循对应快捷助手配置。
- `[x]` Step EH.3 已完成：已保留追问/追加到已有窗口链路的既有自动发送语义；仅对“快捷键召唤快捷助手”场景收口为关闭 direct-send 时进入草稿、不自动提交。
- `[x]` Step EH.4 已完成：已执行 `pnpm build` 验证通过（2026-04-08）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step EH.5 已完成：已完成本轮 Git 定向提交存档。


- `[ ]` Step EG.1 待办：定位独立对话窗口文件粘贴/拖拽/选择与发送阶段的上传校验链路。
- `[ ]` Step EG.2 待办：统一前置文件支持性检测，确保不支持文件在进入草稿时立即提示并阻止加入待发送列表。
- `[ ]` Step EG.3 待办：保留发送阶段兜底校验，但不再作为用户首次感知错误的主要入口。
- `[ ]` Step EG.4 待办：执行 `pnpm build` 验证并回归三种入口提示时机。
- `[ ]` Step EG.5 待办：验证通过后执行 Git 定向提交存档。
- `[x]` Step EG.1 已完成：已定位独立对话窗口文件粘贴/拖拽/选择与发送阶段上传校验链路，并对照原插件 `E:\Programming\Anywhere\Anywhere_window\src\App.vue` / `backend\src\file.js` 确认插件端也是在 `file2fileList` 阶段前置校验。
- `[x]` Step EG.2 已完成：已将桌面端独立对话窗口文件支持性检测前置到文件进入草稿阶段；当前粘贴/拖拽/选择文件时，会先用 `parseFileObject` 做真实可上传性校验，不支持文件会立即提示且不会加入待发送列表。
- `[x]` Step EG.3 已完成：已保留发送阶段 `sendFile()` 兜底校验，但用户首次错误感知不再依赖点击发送；同时批量上传改为单文件失败不阻断其它文件。
- `[x]` Step EG.4 已完成：已执行 `pnpm build` 验证通过（2026-04-08）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step EG.5 已完成：已完成本轮 Git 定向提交存档。


- `[ ]` Step EF.1 待办：核验 build/icon.ico 与打包产物中的实际图标来源。
- `[ ]` Step EF.2 待办：修正 Windows 图标打包链路或缓存触发条件。
- `[ ]` Step EF.3 待办：构建验证通过后执行 Git 定向提交存档。
- `[x]` Step EF.1 已完成：已核验 `build/icon.ico` / `build/icon.png` / `build/icon.icns` 与 `resources/icon.png` 时间戳均为 2026-04-08；同时确认你此前 `dist` 中安装包仍为 2026-04-07 旧产物，这会直接导致安装后继续看到旧图标。
- `[x]` Step EF.2 已完成：已按 electron-builder 官方 NSIS 配置补充 `installerIcon / uninstallerIcon / installerHeaderIcon`，统一指向 `build/icon.ico`，收口安装器侧图标来源。
- `[x]` Step EF.3 已完成：已执行 `pnpm build:win` 重新生成 Windows 安装包（2026-04-08）；最新产物为 `dist/anywhere-desktop-1.0.0-setup.exe` 与 `dist/win-unpacked/anywhere-desktop.exe`。保留既有 `main/core/mcp.js` 静态 + 动态引入告警及 electron-builder `DEP0190` 非阻塞告警。
- `[x]` Step EF.4 已完成：结论收口——你此前“安装后桌面图标仍是旧 logo”至少由两部分共同造成：一是实际安装使用了 4/7 的旧安装包；二是 Windows 可能继续复用桌面快捷方式/资源管理器图标缓存。当前代码与打包配置已统一指向新图标资源。





































- `[/]` Step G 进行中：新增自动追问全局快捷键（默认 `Alt+S`），并把 quick 默认召唤快捷键从 `Alt+X` 改为 `Alt+A`；同时支持“只有一个窗口时直接追问、多个窗口时以 append-only 模式打开 quick”。

- `[x]` Step G 已完成：已新增自动追问全局快捷键默认 `Alt+S`，并将 quick 默认召唤快捷键默认值改为 `Alt+A`；主进程已支持“只有一个窗口时直接追问、多个窗口时以 append-only 模式打开 quick”。
- `[x]` Step H 已完成（后端基础）：`listWindows('window')` 现已按当前存活窗口顺序为同一 `promptCode` 计算 `promptOrdinal` / `promptWindowCount`，前端可直接渲染角标编号。
- `[x]` Step H.1 已完成：本轮后端/配置层已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。

- `[ ]` Step H 待办：为同一快捷助手的多个窗口增加稳定的顺序角标（右上角角标样式），按当前存活窗口的出现顺序重新编号。


- `[/]` Step F 进行中：修正 quick 的“本地文件输入类型识别 + 名称优先/类型次优先排序”规则；重点解决本地图片文件未被识别为图片输入、HTML 文件误命中 OCR、文本正则命中优先级不正确等问题。
- `[x]` Step F 已完成：已修正 quick 的输入类型识别与排序规则——本地图片文件现在按“图片输入”参与排序，HTML / 其他非图片文件不再误命中 OCR；整体排序收口为“名称优先 > 输入类型 > 文本正则/类型匹配强度 > 通用类型”。
- `[x]` Step F.1 已完成：本轮热修已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。




> 最后整理：**2026-03-26**
>
> - 原项目：`E:\Programming\Anywhere`
> - 桌面端项目：`E:\Programming\Anywhere_desktop`
> - 当前目标：**在主控台与独立对话窗口已基本可用的前提下，继续把快捷召唤/快捷助手链路做成真正接近 uTools 的桌面端体验**

---

- `[x]` Step EI 已完成：已修复 quick 自动上传慢、3 秒阈值失真与“相同内容重新复制不自动上传”问题。
- `[x]` Step EI.1 已完成：后端启动时已接入真实剪贴板 watcher，并让 3 秒阈值尽量从复制发生时开始计时；应用 ready 后会主动启动 watcher，不再等到首次读取 quick 时才初始化时间线。
- `[x]` Step EI.2 已完成：已重排 quick payload 采集顺序，优先复用 recent clipboard 快路径；仅在普通快路径拿不到 fresh 内容时才回退到较慢的 PowerShell 选区/文件探测，收口自动上传延迟。
- `[x]` Step EI.3 已完成：已补齐 Windows clipboard sequence 感知；即使重新复制相同内容，只要系统剪贴板序号发生变化，也会刷新 fresh 时间戳，避免“相同内容二次复制不自动上传”。
- `[x]` Step EI.4 已完成：已收口 quick 前端初始化时的二次采集逻辑；当前挂载后仅走轻量 clipboard 读取，不再重复触发 `captureSelectionPayload()` 慢路径。
- `[x]` Step EI.5 已完成：已执行 `pnpm build` 验证通过（2026-04-08）；当前保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step EI.6 已完成：已清理仓库残留 `.git/index.lock`，并以串行方式完成 Git 定向提交存档 `c089d2a fix(quick): speed up auto upload and restore 3s freshness`。


- `[x]` Step EJ 已完成：已同步插件端 `6ab8d54`，修复默认快捷助手/默认助手模型映射错误问题。
- `[x]` Step EJ.1 已完成：已对照插件端 `6ab8d54` 与桌面端映射文件，确认问题核心在默认模型解析与 `__DEFAULT__` 临时 prompt 注入链路。
- `[x]` Step EJ.2 已完成：已同步 `main/core/data.js` 默认模型解析逻辑；当前会校验 provider/model 是否真实可用，并统一修复 `defaultTaskModel` 与 prompt 无效模型自动回退。
- `[x]` Step EJ.3 已完成：已同步 `fastInput / mcp_builtin / ipcHandler / openWindow` 的默认临时 prompt 模型注入逻辑，默认助手不再映射到失效模型。
- `[x]` Step EJ.4 已完成：已执行 `pnpm build` 验证通过（2026-04-08）；默认助手/快捷助手链路构建通过，当前保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step EJ.5 已完成：已清理仓库残留 `.git/index.lock` 并完成 Git 定向提交存档。


- `[x]` Step EK 已完成：已同步插件端 `e8523ad` 到桌面端独立窗口，修复设置界面更新导致窗口缩放还原的问题，并收口会话恢复时的 zoom 解析优先级。
- `[x]` Step EK.1 已完成：已定位桌面端独立窗口 zoom 初始化、配置热更新与会话恢复链路，并对照插件端 `e8523ad` 确认差异点集中在 `render/window/App.vue`。
- `[x]` Step EK.2 已完成：已补齐 zoom 归一化/解析逻辑，并让配置热更新时保留当前窗口已手动调整的缩放，不再被设置更新覆盖。
- `[x]` Step EK.3 已完成：已同步会话恢复时的 zoom 解析优先级，当前按“会话 prompt zoom → 当前 prompt 配置 zoom → 全局 zoom → 1”恢复。
- `[x]` Step EK.4 已完成：已执行 `pnpm build` 验证通过（2026-04-08）；当前保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step EK.5 已完成：已执行 Git 定向提交存档 `6ff6dbe fix(window): preserve zoom across config updates`。

- `[x]` Step EL 已完成：已修复 quick 纯文本名称匹配召唤快捷助手时误自动发送匹配词的问题；当前当输入仅作为快捷助手名称/拼音/缩写/前缀匹配命中时，会打开目标快捷助手但不再把匹配词当作正文下发。
- `[x]` Step EL.1 已完成：已定位 quick 名称匹配与 payload 自动发送判定链路；问题根因是 `render/quick/App.vue` 会把纯文本查询统一下发为 `over` payload，导致名称匹配输入被误当作正文发送。
- `[x]` Step EL.2 已完成：已在 quick 前端收口“仅文本 + 名称/拼音/缩写/前缀匹配”场景；当前 `resolveQuickDispatchPayload(prompt)` 会对目标 prompt 做名称匹配识别，命中后改为仅打开快捷助手、不附带正文 payload。
- `[x]` Step EL.3 已完成：已执行 `pnpm build` 验证通过（2026-04-08）；本轮保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step EL.4 已完成：已执行 Git 定向提交存档。

- `[x]` Step EM 已完成：已将历史对话界面的默认每页展示条数从 10 调整为 20。
- `[x]` Step EM.1 已完成：已定位历史对话分页默认值落点；当前默认值直接定义在 `render/main/components/Chats.vue` 的 `pageSize`，未发现额外初始化/持久化默认值覆盖。
- `[x]` Step EM.2 已完成：已将历史对话默认每页条数从 10 改为 20，并保持其余分页行为不变。
- `[x]` Step EM.3 已完成：已执行 `pnpm build` 验证通过（2026-04-08）；当前历史对话分页默认值已调整为 20 条/页，本轮保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step EM.4 已完成：已执行 Git 定向提交存档。




## 1. 固定执行原则

1. **严格按后端 → preload/IPC → 前端 顺序推进**。
2. **不做展位代码**：未打通底层链路前，不在前端硬写临时逻辑糊过去。
3. **彻底脱离 uTools**：所有 `utools.xxx` 能力都必须由 Electron / Node.js 原生能力替代。
4. **优先语义定位**：先用 `fast_context_search` 找入口和调用链，再按需读取文件。
5. **每个子步骤都要验证**：默认以 `pnpm build` 为基线，必要时补运行态验证。
6. **每个子步骤验证通过后都要 Git 存档**：优先做**定向提交**，避免把无关改动混进同一 commit。
7. **进度文档必须实时维护**：任务状态以本文件为准，不再依赖零散会话日志。

---

## 2. 项目结构映射（原插件 → 桌面端）

| 原项目模块 | 桌面端对应 | 当前判断 |
| --- | --- | --- |
| `backend/src/data.js` / `file.js` / `chat.js` / `mcp.js` / `skill.js` / `preload.js` | `main/core/*` + `main/ipcHandler.js` + `preload/*` | `[x]` 主体已迁移 |
| `Anywhere_main` | `render/main` | `[x]` 主体已重构完成 |
| `Anywhere_window` | `render/window` | `[/]` 基础重构完成，继续补完整体验 |
| `Fast_window/fast_input.html` | `render/fast_input` | `[x]` 已切换为真实桌面端 fast_input 主线 |
| `Fast_window/append_selector.html` | `render/quick` + 后续独立 append selector | `[/]` quick 已成型，append selector 独立形态仍待补 |
| uTools 全局唤起 / 托盘 / 快捷键能力 | `main/index.js` + `main/shortcutManager.js` + `windowManager.js` | `[/]` 基础版已打通 |

---

## 3. 当前状态总览（以当前源码为准）

### 3.1 总体阶段判断

- `[x]` **P0 后端底座**：DB、文件、配置、系统能力、MCP、Skill、窗口管理、事件总线、IPC 主链均已落地。
- `[x]` **P1 Anywhere_main 主控台**：主控台、设置、Providers、MCP、Skills、Prompts、Tasks、Chats 已完成桌面端重构。
- `[/]` **P2 Anywhere_window 独立对话窗口**：基础重构完成、当前可用；后续主要补 UI/交互/多模态体验与完整复刻细节。
- `[/]` **P3 Quick 快捷召唤**：基础版已明显超出“空壳”，已具备真实可用的桌面端召唤能力，但仍未完全达到原插件体验。
- `[ ]` **P4 Append Selector 独立追加选择器**：原插件存在独立形态，桌面端尚未完整对齐为独立成品。
- `[/]` **P5 桌面端运行时能力**：全局快捷键、托盘、main/quick 显隐与基础运行时已打通，后续继续收口。
- `[ ]` **P6 打包与分发**：仍未进入系统化收尾阶段。

### 3.2 当前一句话结论

- `anywhere_main`：**已完成**。
- `anywhere_window`：**基础重构完成并可用**，后续做“完整体验补齐”，不是从零开始。
- `quick`：**基础版已成型且已做多轮修复**，当前真正需要的是“继续按后端到前端做体验收口和功能补齐”，不是重新搭骨架。
- `fast_input`：**当前真实快捷输入主线已落地**，后续在此基础上继续补体验与边界。

---

## 4. 已完成模块（当前可认可结论）

### 4.1 后端底座 `[x]`

已完成并已在桌面端存在明确代码落点：

- `main/dataConverter.js`
- `main/core/db.js`
- `main/core/data.js`
- `main/core/file.js`
- `main/core/chat.js`
- `main/core/mcp.js`
- `main/core/mcp_builtin.js`
- `main/core/skill.js`
- `main/core/system.js`
- `main/core/webdav.js`
- `main/eventBus.js`
- `main/ipcHandler.js`
- `main/windowManager.js`
- `preload/main_preload.js`
- `preload/window_preload.js`
- `preload/fast_input_preload.js`
- `preload/quick_preload.js`

已具备能力：

- 本地配置/文档/附件存储
- 文件读写与路径处理
- 系统剪贴板 / 选择内容捕获 / 桌面源读取
- MCP / Skill / Agent 基础后端链路
- 窗口创建、显隐、置顶、关闭、事件投递
- 多窗口初始化载荷注入 `window:init`
- 全局快捷键与托盘基础运行时

### 4.2 Anywhere_main 主控台 `[x]`

桌面端 `render/main` 已具备：

- 主控台 App 外壳
- `Setting.vue`
- `Providers.vue`
- `Mcp.vue`
- `Skills.vue`
- `Prompts.vue`
- `Tasks.vue`
- `Chats.vue`
- 样式、主题、i18n、导入导出、WebDAV 主进程化

**结论**：`Anywhere_main` 可视为当前桌面端主线中**已完成模块**。

### 4.3 Anywhere_window 独立对话窗口 `[/]`

桌面端 `render/window` 已存在真实可用实现：

- `App.vue`
- `components/TitleBar.vue`
- `components/ChatHeader.vue`
- `components/ChatInput.vue`
- `components/ChatMessage.vue`
- `components/ModelSelectionDialog.vue`

当前已完成的核心链路：

- `window:init` 初始化载荷注入
- `window:event-bus` 事件总线接收
- 历史会话恢复
- 任务打开/召唤打开/追加消息基础链路
- 配置更新同步
- 窗口尺寸、位置、缩放、置顶等基础能力
- 关闭链路稳定性修复
- Agent 窗口工具链基础读写能力

**结论**：不是“只开始了一点点”，而是**基础重构已完成**；下一步主要是继续补“完整复刻体验”。

### 4.4 Quick 快捷召唤基础版 `[/]`

当前桌面端 quick 主线落点：

- `main/index.js`：快捷键、quick 唤起、托盘入口
- `main/shortcutManager.js`：全局快捷键注册与冲突校验
- `preload/quick_preload.js`：quick 专属桥接层
- `render/quick/App.vue`：快捷召唤前端主体

当前 quick 已具备的基础能力：

- 全局快捷键唤起
- quick 单例窗口
- 剪贴板文本/图片/文件读取
- best-effort 当前选中内容捕获
- 快捷助手候选生成与排序
- 拼音 / 首字母名称匹配
- 图片 / 文件 / 多行文本 token 化展示
- `enable=false` 状态同步
- main / quick 可见性副作用收口
- 通过 `showMode` 分流到 `window` / `fast`

**结论**：quick 已不是“空壳”，而是**已形成真实可用基础版**。

### 4.5 fast_input 当前定位 `[x]`

桌面端 `render/fast_input/App.vue` 当前定位：

- 接收 `window:init` 与 `fast-input:event`
- 在主进程直接执行快捷输入请求并流式展示结果
- 支持复制结果、发送到当前输入框、取消请求与自动关闭策略
- 已成为桌面端真实 `fast_input` 主线，而非中转层

**结论**：

- 当前 `fast_input` 已接管原插件 `fast_input.html` 的桌面端主线职责
- 遗留 `fast_window` 中转层已于 2026-04-07 清理
- 后续继续在 `fast_input` 主线上做体验补齐，不再保留双轨实现

### 4.6 桌面端运行时 `[/]`

已具备：

- `Ctrl+Space` / `Alt+X` 等快捷键注册
- 桌面端快捷键配置录制与热更新
- Tray 创建与菜单
- main close-to-tray 基础行为
- quick / main 的显隐控制与副作用修复

---

## 5. 当前未完成 / 未完全对齐原插件的部分

### 5.1 Anywhere_window 仍待补齐

- 自定义标题栏的完整平台体验
- Markdown/代码块/工具卡片的完整交互收口
- 输入区 `@MCP` / `/Skill` 的完整体验与边界处理
- 图片查看、导出长图、背景图与毛玻璃细节
- 语音/录音/设备能力
- 完整运行态回归验证

### 5.2 quick 仍待改进

- 当前 quick 虽可用，但仍未完全达到原插件“快捷召唤 + 追加选择 + fast 输入”的整体体验
- `append selector` 仍未作为**独立成品**完成对齐
- `fast_input` 完整视觉与行为仍未原样复刻
- quick / fast / append 三者职责边界需要继续明确和收口

### 5.3 桌面端收尾仍待推进

- 打包、安装、分发
- 打包后 `userData` 路径读写回归
- 端到端桌面体验复测

---

## 6. 当前已知问题 / Notes

- `main/core/mcp.js` 仍存在“静态与动态同时引入”的**非阻塞构建告警**。
- 独立对话窗口虽已基本可用，但仍有一部分“完整复刻体验”尚未进入系统化收尾。
- 当前 quick 是主要快捷召唤入口；`fast_input` 已是桌面端真实快捷输入实现，后续不要再回退到旧 `fast_window` 中转层。
- 文档后的历史会话里有大量 quick 修复记录，但主线判断必须以**当前源码状态**为准。

---


### `[x]` Q_web_proxy 后端：统一主进程外网请求代理行为

**目标**

- 修复 `web_search` / `web_fetch` 在桌面端主进程未正确遵循系统/本地代理的问题。
- 优先让主进程外网请求默认遵循系统代理；若系统代理不可直取，至少兼容本地代理环境变量。
- 避免把“联网失败/代理未生效”误报成“无搜索结果”。

**重点文件**

- `main/core/mcp_builtin.js`
- `main/core/chat.js`
- 必要时补充 `main/core/*` 公共网络层

**完成标准**

- `web_search`、`web_fetch` 默认通过统一代理决策发起请求。
- 能区分“真正无结果”与“网络失败/代理失败”。
- 不破坏现有模型/工具调用链。

**验证方式**

- 用真实查询词验证 `web_search`
- 必要时验证 `web_fetch`
- `pnpm build`

- `[x]` 已定位主进程直接外网请求点：`mcp_builtin.js`、`chat.js`、`data.js`
- `[x]` 已新增 `main/core/net.js`，统一使用 Electron `net.fetch`（Chromium 网络栈）以默认遵循系统代理/默认 Session 代理策略
- `[x]` 已接入 `web_search`、`web_fetch`、`read_file(HTTP/HTTPS)`、`cacheBackgroundImage`、`listProviderModels`
- `[x]` 执行 `pnpm build` 验证
- `[x]` 通过后执行 Git 存档

- `[x]` 已补充 `web_search` 错误语义：请求失败时明确返回网络/代理错误，不再误报为“无结果”
- `[x]` 本轮验证结果：`pnpm build` 通过；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本次阻塞项）

- `[x]` 运行态验证：使用 Electron `net.fetch` 直接请求 DuckDuckGo 成功返回 `status=200`，确认主进程已可遵循系统代理链路
- `[x]` 结论：桌面端此前 `web_search` 异常的根因是主进程 Node 网络栈未正确遵循系统/本地代理；现已切换到 Electron Chromium 网络栈

**建议 commit**

- `fix(network): make main-process web requests honor proxy settings`

---

## 7. 接下来必须执行的主线（严格按后端 → preload → 前端）

> 以下是**下一阶段真正要做的任务顺序**。未完成上一项前，不跳步做下一项前端细节。

### `[/]` Q0 文档整理与基线确认

- `[x]` 充分阅读 `Anywhere_desktop` 与 `Anywhere` 的关键模块与入口。
- `[x]` 重新整理 `进度.md`，改为“当前状态 + 主线任务 + 历史摘要”的结构。
- `[x]` 执行一次基线验证：`pnpm build`。
- `[ ]` 验证通过后做文档专属 Git 存档。

**验证方式**

- `pnpm build`
- 文档中的状态与源码结构交叉一致

**建议 commit**

- `docs(progress): 重构进度文档为当前阶段看板`

---

### `[/]` Q1 后端：统一 quick 召唤协议与自动上传源

**目标**

- 让 quick 成为真正“即用即走”的快捷路由入口。
- 统一 quick 打开时的 payload 协议，明确文本 / 跨行文本 / 图片 / 文件的标准化表示。
- 打开 quick 时，优先使用“当前选中内容”；若未捕获到选中内容，则回退到“最近 5 秒内复制的内容”；再回退到当前剪贴板内容。
- quick 始终显示在**当前鼠标所在显示屏正中央**，并支持单例快速显隐。

**重点文件**

- `main/index.js`
- `main/windowManager.js`
- `main/ipcHandler.js`
- `main/core/system.js`

**完成标准**

- quick 打开位置稳定在当前鼠标所在屏幕中心。
- quick 热键再次触发时立即隐藏。
- quick 初始 payload 能正确区分文本 / 跨行文本 / 图片 / 文件，且 `svg` 归为文件而非图片。
- 当前选择内容 / 最近 5 秒复制内容 / 当前剪贴板三者优先级明确且稳定。

**验证方式**

- `pnpm build`
- 手工验证：快捷键反复呼出 / 隐藏、跨屏打开位置、文本 / 跨行文本 / 图片 / 文件自动上传

**建议 commit**

- `refactor(quick): unify summon payload and auto-upload sources`

---

### `[ ]` Q2 preload / IPC：收口 quick 的显隐、粘贴与上下文桥接

**目标**

- 让 quick 的 preload / IPC 语义稳定，只暴露 quick 真实需要的能力。
- 支持 quick 以“隐藏”而非销毁的方式快速关闭，提高打开/关闭速度。
- 收口 quick 的粘贴上传能力与窗口上下文获取能力。

**重点文件**

- `preload/quick_preload.js`
- `main/ipcHandler.js`
- `main/windowManager.js`

**完成标准**

- quick 可通过桥接层稳定执行 `open / hide / close-context / clipboard-read / selection-capture / file-path-resolve`。
- quick 关闭主路径改为“立即隐藏”，避免频繁销毁导致的体感迟缓。
- quick 打开时粘贴文本 / 图片 / 文件行为稳定。

**验证方式**

- `pnpm build`
- 手工验证：Esc 关闭、再次呼出、粘贴图片 / 文件 / 多行文本

**建议 commit**

- `refactor(quick): streamline hide and paste bridge semantics`

---

### `[ ]` Q3 前端：完成 quick 的匹配规则与自动路由

**目标**

- 实现与需求一致的 quick 匹配策略：名称匹配、文本类型匹配、图片类型匹配、文件类型匹配、通用类型兜底。
- 支持名称的全名 / 首字母 / 拼音（大小写无关）匹配。
- 对“文本 / 跨行文本 / 图片 / 文件 / 附件后二次输入”分别采用正确的排序与二次筛选规则。

**重点文件**

- `render/quick/App.vue`

**完成标准**

- 文本输入时：名称匹配 > 文本类型匹配 > 通用类型。
- 图片输入时：名称匹配 > 图片类型匹配 > 通用类型。
- 文件输入时：名称匹配 > 文件类型匹配 > 通用类型。
- 跨行文本按附件处理，并参与文本类型匹配。
- 已有附件后继续输入时，仅在首次附件匹配结果中做名称二次筛选。

**验证方式**

- `pnpm build`
- 手工验证：文本、跨行文本、图片、文件、附件后二次输入、`svg` 文件

**建议 commit**

- `feat(quick): implement smart matching and auto routing`

---

### `[ ]` Q4 前端：完成 quick 的键盘行为与即用即走体验

**目标**

- 让 quick 的键盘行为严格符合“即用即走”的交互预期。
- quick 显示时，如果输入框存在文本/附件，按 `Backspace` 或 `Esc` 立即清空内容。
- quick 显示时，如果输入框没有内容，按 `Esc` 立即隐藏 quick。
- 输入框保持自动聚焦，并保证回车打开目标助手稳定。

**重点文件**

- `render/quick/App.vue`

**完成标准**

- `Backspace` / `Esc` 清空优先级正确。
- 无内容 `Esc` 立即隐藏 quick。
- quick 打开后输入焦点稳定。
- 打开目标助手后 quick 立即隐藏，不拖泥带水。

**验证方式**

- `pnpm build`
- 手工回归：Esc、Backspace、Enter、上下键、重复呼出、打开后立即消失

**建议 commit**

- `feat(quick): refine ephemeral keyboard behavior`

---

### `[ ]` Q5 前端：对齐 uTools 风格的 quick 外观

**目标**

- 让 quick 更接近 uTools 召唤栏：简洁、聚焦、轻量、深浅色统一。
- 优化输入栏、附件 token、推荐区与候选项的视觉层级。
- 提升 quick 打开/关闭时的速度感与完成度。

**重点文件**

- `render/quick/App.vue`
- `render/quick/assets/main.css`

**完成标准**

- quick 视觉层级简洁清晰，接近 uTools 召唤栏风格。
- 深色 / 浅色模式随设置切换稳定。
- 附件展示、候选项选中态、信息密度与留白合理。

**验证方式**

- `pnpm build`
- 手工验证：深浅色切换、打开关闭体感、附件展示、候选高亮

**建议 commit**

- `style(quick): align summon bar with utools-like appearance`

---

### `[ ]` Q6 前端：继续补 Anywhere_window 的完整体验

**目标**

- 在 quick/append 主线明确后，继续进入独立窗口完整复刻

**重点内容**

- 标题栏完整体验
- Markdown/代码块/工具卡片
- 输入区 `@` / `/` 体验
- 图片查看与长图导出
- 音频与设备能力

**验证方式**

- `pnpm build`
- 必要运行态联调

**建议 commit**

- 按子模块拆分提交，不做大杂烩提交

---

### `[ ]` Q7 集成收尾

**目标**

- 做完整桌面端回归
- 再进入打包与分发

**重点内容**

- 快捷键 / 托盘 / quick / window / main 联调
- 数据路径、历史会话、文件发送、多窗口追加链路联调
- 打包配置与安装验证

**验证方式**

- `pnpm build`
- 打包后运行验证

---


### 15. 2026-03-31 本轮进行中（quick 召唤问题修复） `[x]`

- `[x]` 目标：修复 quick 的选中/复制源优先级、文件自动上传、跨行文本保真输入，以及快捷助手选择栏信息密度过低的问题。
- `[x]` Step A：已修正 quick 打开前的内容采集时机与优先级，改为优先当前选中内容，失败后再回退最近复制内容/当前剪贴板。
- `[x]` Step B：已统一 quick payload 语义，补齐跨行文本 `multiline-text` 独立类型，避免在打开助手时丢失换行语义。
- `[x]` Step C：已调整 quick 前端附件注入与路由逻辑，确保文件/图片/跨行文本都能正确带入目标助手。
- `[x]` Step D：已优化 quick 候选项 UI，放大图标与字体，并缩小单项宽度提高信息密度。
- `[x]` 验证：`pnpm build` 通过（2026-03-31）
- `[x]` 验证通过后已执行 Git 定向提交存档：`2d19aff fix(quick): prioritize selection and preserve multiline payloads`
- `[x]` 追加修复：已补齐目标窗口对 `multiline-text` 的落地语义，改为预填充到输入框，不再与普通单行文本走同一发送链。
- `[x]` 追加修复：已增强 Windows 文件剪贴板解析与 quick 粘贴处理，补齐多文件读取路径与多项附件注入链路。
- `[x]` 第二轮验证：`pnpm build` 再次通过（2026-03-31）
- `[x]` 第二轮修复已执行 Git 定向提交存档：`da13337 fix(quick): support multiline input and multi-file clipboard payloads`
- `[x]` 新增收口：已优先处理 quick 打开速度回退与“复制多个文件后作为多附件上传”的主链问题；“纯选中未复制后自动上传”暂降级为后续难点。
- `[x]` 第三轮修复：quick 唤起改回“先显示、后异步注入剪贴板快照”，收口 Alt+X 打开体感变慢的问题。
- `[x]` 第三轮修复：复制多个文件后，quick 现已按多附件展示，并在选择快捷助手后把全部附件一并传入目标窗口。
- `[x]` 第三轮验证：`pnpm build` 通过（2026-03-31）
- `[x]` 第三轮修复已执行 Git 定向提交存档：`cafddc0 fix(quick): restore fast summon and batch file attachments`
- `[x]` 追加专项：针对“资源管理器里仅选中多个文件时 quick 只拿到 1 个或 0 个”的问题，已新增前台 Explorer 多选直读链路，绕开慢且不稳定的全局选中模拟复制。
- `[x]` 第四轮验证：`pnpm build` 通过（2026-03-31）
- `[x]` 第四轮修复已执行 Git 定向提交存档：`5c75ec5 fix(quick): read multi-selected explorer files for summon payload`

- `[/]` 调试增强：已为 quick 增加主进程/前端多点日志，覆盖 Explorer 多选读取、剪贴板文件读取、payload 构建与 quick 注入全过程，等待你复现后回传日志定位根因。

- `[/]` 日志结论（首轮）：当前复现里前台 Explorer 多选读取返回 `0`，且剪贴板阶段仅见 `text/uri-list` / `text/plain` 未转成有效文件数组；已继续增强 Explorer 窗口匹配与 PowerShell FileDropList 兜底，等待二次日志。

- `[/]` 新增修复目标：处理 Windows 混合附件（位图图片预览 + 其他文件路径）被 quick 当成二选一来源的问题，改为统一合并成 `files` 批量附件上传。

- `[x]` 追加修复目标：资源管理器多选结果里含中文文件名时，PowerShell 输出链可能乱码，导致 `fs.existsSync` 过滤误删附件；同时去掉 quick 顶部 token 文本中的重复数量，仅保留右上角角标。
- `[x]` 乱码路径修复：Explorer 多选返回的路径不再因为 `fs.existsSync` 校验而误删，避免中文文件名附件被丢失。
- `[x]` UI 收口：quick 顶部 token 文字不再重复拼接数量，仅保留右上角黑底角标展示附件总数。
- `[x]` 本轮验证：`pnpm build` 通过（2026-03-31）
- `[x]` 本轮 Git 定向提交存档：`9790e53 fix(quick): keep mixed explorer selections and simplify count badge`

- `[/]` 新增修复目标：PowerShell 输出编码导致中文文件名路径乱码、图片文件点击报“无法访问该路径”；同时取消常驻剪贴板轮询，避免持续读取剪贴板与后台资源消耗。

- `[/]` 新增修复目标：当 quick 已有附件且用户继续输入时，候选区应只保留名称命中的快捷助手，不再把名称不匹配的类型兜底项混进结果；但纯文本输入时仍保留“名称匹配 + 文本匹配”并行策略。
- `[x]` 附件后二次输入修复：已有附件且继续输入时，候选区现在只保留名称命中的快捷助手；例如输入 `moss` 时只显示 `moss`，不再混入播客等无关助手。
- `[x]` 纯文本输入策略保持不变：无附件时仍保留“名称匹配 + 文本类型匹配 + 通用类型”并行排序，不会退化成只做名称匹配。
- `[x]` 本轮验证：`pnpm build` 通过（2026-03-31）
- `[x]` 本轮 Git 定向提交存档：`1f1a22e fix(quick): restrict attachment search to strict name matches`

- `[x]` 新增修复目标：选中内容与复制内容的自动上传时效统一收口为 3 秒；超过 3 秒则不再自动带入 quick。
- `[x]` 时效阈值修复：自动上传 freshness 窗口已从 5 秒收口到 3 秒，超时后不再自动带入 quick。
- `[x]` 本轮验证：`pnpm build` 通过（2026-03-31）
- `[x]` 本轮 Git 定向提交存档：`617c708 fix(quick): reduce auto-upload freshness window to 3s`

- `[x]` 追加修复目标：Explorer 当前仍被选中的文件不应无限期自动上传，需像复制内容一样受 3 秒 freshness 窗口约束。
- `[x]` Explorer 选中时效修复：当前仍被选中的文件现在也会记录签名与时间戳，只在最近 3 秒内自动带入 quick，避免几分钟前的旧选中反复自动上传。
- `[x]` 本轮验证：`pnpm build` 通过（2026-03-31）
- `[x]` 本轮 Git 定向提交存档：`ce7fb31 fix(quick): expire stale explorer selections after 3s`
- `[x]` 剪贴板文件时效修复：PowerShell FileDropList 兜底链路现在也接入 3 秒 freshness 约束，避免一分钟前复制的文件持续自动上传。
- `[x]` 本轮验证：`pnpm build` 通过（2026-03-31）
- `[x]` 本轮 Git 定向提交存档：`6dcdb67 fix(quick): expire stale clipboard file-drop payloads after 3s`

- `[x]` 收尾目标：已清理 quick 调试阶段加入的日志插装与临时调试辅助，避免污染正式代码与运行输出；同时未覆盖同事已更新的其他逻辑。
- `[x]` 收尾说明：已移除 quick 调试日志函数、主进程调试调用和 quick 前端临时 console 输出，仅保留业务修复逻辑。






- `[x]` 混合附件修复：已将 `image/png` 预览图与 `filePaths` 同时存在时统一合并为一个 `files` 附件批次，避免 png 抢走 svg/html 或反过来只保留非图片文件。
- `[x]` 混合附件验证：`pnpm build` 通过（2026-03-31）
- `[x]` 混合附件修复已执行 Git 定向提交存档：`c09ea3d fix(quick): merge mixed image and file selections into one attachment batch`






- `[x]` 备注：本轮仍保留既有 `main/core/mcp.js` 动态/静态引入构建告警，非本次 quick 修复阻塞项。

---

## 8. 提交规则（执行时必须遵守）

### 8.1 每一步的固定节奏

1. 用 `fast_context_search` 找目标调用链
2. 只读必要文件
3. 先改后端，再改 preload，再改前端
4. 完成后立刻 `pnpm build`
5. 验证通过后立刻更新本文件
6. 只把本步骤相关文件做**定向 Git 提交**

### 8.2 禁止事项

- 禁止把多个无关修复混进同一次 commit
- 禁止前端先写“看起来像完成了”的假逻辑
- 禁止在未核对原插件实现前擅自改变交互语义

---

## 9. 当前最近验证结果

- `[x]` `pnpm build` 通过（2026-03-26，本轮整理文档基线验证）
- `[x]` 当前项目仍可构建出多入口产物：`main` / `window` / `fast_input` / `quick`
- `[x]` 当前 quick 主入口、window 主体与 main 主控台在源码层均存在稳定落点
- `[ ]` 本轮文档整理提交待执行
- `[x]` 本轮文档整理提交已完成：`42eb079 docs(progress): 重构进度文档为当前阶段看板`


---

## 10. 本轮整理结论（给后续会话看的摘要）

1. **Anywhere_main 已完成**，后续不是主战场。
2. **Anywhere_window 已完成基础重构并可用**，后续做完整体验补齐。
3. **quick 已经是当前桌面端真正的快捷召唤入口**，不是空壳。
4. **fast_input 已成为当前桌面端真实快捷输入主线，旧 fast_window 中转层已清理。**
5. **append selector 独立成品仍缺失**，这是 quick 继续改进时必须正视的缺口。
6. 之后的开发必须继续按：
   **后端路由协议 → preload 收口 → quick 前端优化 → append selector 独立实现 → window 体验补齐**

---


### 12. 2026-03-26 本轮进行中（web_search 桌面端 0 结果 / 独立窗口背景图闪烁） `[/]`

- `[x]` Step A 已完成：按用户要求，已将桌面端 `web_search` 回退恢复到本轮开始前的原始 DuckDuckGo 实现，不再保留 `Electron net` / `net.fetch` / Bing 回退 等新增改造链路，避免继续引入额外错误。
- `[x]` Step B 已完成：独立窗口背景图闪烁修复保留，窗口背景仍采用 ready-state 显示：只有背景图真实预加载完成后才挂载 `has-bg` / `window-bg-layer`，并收短过渡时间，避免打开瞬间闪白/闪黑。
- `[x]` Step C 已完成：`pnpm build` 通过（2026-03-26，本轮 web_search 回退 + 独立窗口背景闪烁修复保留）。
- `[x]` Step D 已完成：本轮回退 Git 存档已完成：`fix(web-search): revert desktop web_search changes`


---

### 13. 2026-03-26 本轮进行中（同步插件端 3 个 commit 到桌面端） `[/]`

- `[x]` Step A 已完成：完成插件端 `0b5a84d` / `ff0f347` / `2d8f8e6` 三个 commit 的差异分析，并映射到桌面端对应模块：`render/main/components/Chats.vue`、`render/main/components/Tasks.vue`、`render/window/App.vue`、`main/core/mcp_builtin.js`。
- `[x]` Step B 已完成：确认插件端 `0b5a84d`（英文环境下恢复对话失败）在桌面端当前实现中已被等价规避——桌面端主控台与任务页恢复对话已改为直接 `openWindow('window', { code, type: 'over', payload, filename })`，不再依赖本地化标题文本匹配，因此无需机械迁移旧修复。
- `[x]` Step C 已完成：同步插件端 `2d8f8e6` 到桌面端 `main/core/mcp_builtin.js`，补齐 `summon_agent` / `continue_agent_chats` 的 `file_paths` 参数安全提示，并通过桌面端 MCP 初始化链路把当前窗口 Agent 名称注入到 `list_agents` 描述中。
- `[x]` Step D 已完成：同步插件端 `ff0f347` 到桌面端 `render/window/App.vue`，补齐发送前并发锁 `isPreparingSend`、生成中保存拦截，以及恢复会话时对 `history/chat_show` 的自愈对齐与尾部脏 assistant 节点清理。
- `[x]` Step E 已完成：按步骤执行 `pnpm build` 验证，并在验证通过后做定向 Git 提交存档。
- `[x]` Step E.1 已完成：本轮后端同步后执行 `pnpm build` 通过；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。
- `[x]` Step E.2 已完成：本轮前端同步后再次执行 `pnpm build` 通过；当前 3 个插件端 commit 在桌面端的必要同步已完成，其中 `0b5a84d` 以“桌面端已等价规避，无需迁移旧链路”的方式完成对齐。


---

### 14. 2026-03-26 本轮进行中（quick 重构为即用即走召唤栏） `[/]`

- `[x]` Step A 已完成：重排 `进度.md` 中的 quick 主线任务，把目标明确为“后端自动上传源统一 → preload 收口 → 匹配与自动路由 → 键盘行为 → uTools 风格外观”。
- `[x]` Step B 已完成：后端统一 quick 初始 payload 的基础规则，明确文本 / 图片 / 文件的标准化载荷，并保留“最近 5 秒剪贴板内容”回退链路；当前 `svg` 不再走图片直传分支。
- `[x]` Step C 已完成：quick 单例复用时会重新计算并应用当前鼠标所在显示屏中心位置，不再停留在旧屏幕位置。
- `[x]` Step D 已完成：quick 关闭主路径由销毁改为隐藏，提升重复呼出/关闭的体感速度。
- `[x]` Step E 已完成：执行 `pnpm build` 通过（2026-03-26，本轮 quick 后端 Step 1）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。
- `[x]` Step F 已完成：继续收口 quick preload / IPC 与前端键盘行为、自动路由、uTools 风格外观。
- `[x]` Step F 已完成：quick 前端已改为“内容优先”交互——打开时文本/跨行文本/图片/文件会作为附件或上传内容接入，`Esc` / `Backspace` 在有内容时优先清空、无内容时再隐藏 quick。
- `[x]` Step G 已完成：quick 匹配规则按“名称匹配（全名/首字母/拼音，不区分大小写）→ 类型匹配 → 通用类型”收口；已有附件后继续输入时，会在附件匹配结果中做名称二次筛选。
- `[x]` Step H 已完成：粘贴上传行为已收口，支持文本 / 跨行文本 / 图片 / 文件；`svg` 按文件处理，不再归入图片。
- `[x]` Step I 已完成：quick 外观已收紧为更接近 uTools 召唤栏的轻量样式，统一深浅色主题与候选项视觉层级。
- `[x]` Step J 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 前端 Step 2）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step K 已完成：去掉 quick 外层实体大框，窗口改为透明背景承载，仅保留召唤栏主体；候选区限制为 3 行并隐藏滚动条，超出部分直接截断不显示。
- `[x]` Step L 已完成：修复“手动复制文件/图片后 Alt+X 未自动上传”的问题——若最近 5 秒剪贴板已有有效内容，则 quick 直接复用，不再先发送模拟 `Ctrl+C` 冲掉用户刚复制的文件/图片。
- `[x]` Step M 已完成：修复 Alt+X 打开 quick 的明显延迟；当前优先使用新鲜剪贴板内容瞬时返回，仅在确实没有新鲜内容时才尝试选中捕获。
- `[x]` Step N 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 外观/自动上传/性能热修复）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step O 已完成：第二轮重构 quick 打开时序，改为“先显示空 quick，再异步补载最新剪贴板/选中内容”，显著降低图片/文件导致的首屏阻塞与 Alt+X 体感延迟。
- `[x]` Step P 已完成：加固 Windows 文件剪贴板解析，补充 `CF_HDROP` / `FileNameW` / `text/uri-list` 与文本路径回退解析，提高复制文件后自动上传命中率。
- `[x]` Step Q 已完成：加强 quick 焦点获取与置顶逻辑，打开与复用时同步 `focus + webContents.focus + moveTop`，修复“Alt+X 后未聚焦导致 Ctrl+V 落回原窗口”的问题。
- `[x]` Step R 已完成：调整 quick 热键显隐条件，仅在 quick 可见且已聚焦时再次按热键才隐藏，修复“失焦关闭后第一次 Alt+X 无反应、第二次才出现”的状态错误。
- `[x]` Step S 已完成：quick 模板与样式改成更接近 uTools 的扁平结构，去掉外层 panel 感，仅保留输入框与下方快捷助手栏，缩小图标/文字并限制 3 行展示。
- `[x]` Step T 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 第二轮修复）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step U 已完成：第三轮修复 quick 的显示时序，取消“二次 openWindow 重新打开 quick”的做法，改为首次显示后直接向可见 quick 推送 payload，修复“出现→消失→再出现”的闪烁问题。
- `[x]` Step V 已完成：quick 改为圆角矩形容器，并通过 `-webkit-app-region` 支持拖动；输入框、附件与候选项区域保持可交互。
- `[x]` Step W 已完成：placeholder 文案改为 Anywhere Desktop 语义，不再照抄 uTools。
- `[x]` Step X 已完成：进一步收紧快捷助手信息密度，缩小图标、文字、间距，使同屏排序更紧凑。
- `[x]` Step Y 已完成：已按用户允许清理 `.git/index.lock`，解除 git 提交阻塞。
- `[x]` Step Z 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 第三轮修复）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step AA 已完成：补齐 quick `index.html` 的 `html/body/#app` 透明与 margin reset，去掉浏览器默认外边距导致的最外层白边/套壳。
- `[x]` Step AB 已完成：调整 quick 输入框一行作为拖拽热区，允许用户在输入框所在横条区域按住拖动窗口，同时保留输入框与卡片点击交互。
- `[x]` Step AC 已完成：继续压缩 quick 宽度与布局密度，减少最外层空白边缘感，让圆角矩形主体更直接地呈现。
- `[x]` Step AD 已完成：本轮确认文件自动上传链路暂缓，优先收口样式与交互；hover 卡顿进入专项优化清单。
- `[x]` Step AE 已完成：再次执行 `pnpm build` 通过（2026-03-26，本轮 quick 第四轮样式/拖拽修复）；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step AF 已完成：修复当前 quick 三个体验回归——`Alt+X` 打开仍有白边、输入框所在横条拖拽命中不一致、首项“博客”默认高亮被误判为 hover。
- `[x]` Step AG 已完成：本轮修复后执行 `pnpm build` 验证，通过；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step AF.1 已完成：取消候选列表“无操作时自动选中首项”的逻辑，只在键盘导航/鼠标进入/显式 `promptKey` 注入时显示 active 样式，消除“博客一直 hover”的误判。
- `[x]` Step AF.2 已完成：把 quick 输入框所在搜索横条恢复为主拖拽热区，仅输入框本体和卡片区域保持 `no-drag` 交互，收口拖动命中不一致问题。
- `[x]` Step AF.3 已完成：将 quick 主体改为近乎贴满透明窗口的圆角容器，减少透明边缘暴露，收口 Win11 下 `Alt+X` 打开时的白边感。
- `[x]` Step AH 已完成：验证通过后已执行本轮 quick 定向 Git 提交存档。

- `[x]` Step AH.1 已完成：本轮 quick 回归修复已定向提交：`1ca9eba fix(quick): remove phantom active state and tighten summon shell`。

- `[x]` Step AI 已完成：把 quick 从“透明矩形窗口 + 圆角内容块”改为“非透明原生圆角窗口壳 + 隐藏标题栏内容区”，窗口层不再依赖透明背景伪装圆角。
- `[x]` Step AJ 已完成：同步收口 quick 候选区底部留白，通过降低窗口高度与内容区贴边布局，让上下留白更接近一致。
- `[x]` Step AK 已完成：完成窗口壳与布局调整后重新执行 `pnpm build` 验证，通过；保留既有 `main/core/mcp.js` 静态+动态引入告警（非本步骤阻塞项）。

- `[x]` Step AI.1 已完成：`main/windowManager.js` 中 quick 窗口改为 `transparent: false + roundedCorners: true + titleBarStyle: 'hidden' + hasShadow: true`，改走原生圆角窗口壳路线。
- `[x]` Step AI.2 已完成：quick 主窗口背景色改为深色实体背景，避免 Win11 对透明无边框窗口的边缘合成把外层矩形壳感露出来。
- `[x]` Step AJ.1 已完成：`render/quick/App.vue` 中 quick 内容区改为 `width: 100% / min-height: 100% / padding: 14px / justify-content: flex-start`，直接贴合窗口内容区。
- `[x]` Step AJ.2 已完成：quick 默认高度由 `420` 收口到 `378`，减少候选区下沿多余空白。
- `[x]` Step AL 已完成：验证通过后已执行本轮定向 Git 提交存档。

- `[x]` Step AL.1 已完成：本轮 quick 原生圆角窗口壳修复已定向提交：`76c9218 fix(quick): use native rounded window shell`。






### 15. 2026-04-01 本轮进行中（快捷键召唤快捷助手自动上传收口） `[/]`

- `[x]` Step A 已完成：按“后端 → preload/IPC → 前端”重新核对 quick / fast / window 的 3 秒内容捕获与自动上传链路，确认当前真实缺口不是上传能力缺失，而是快捷键直达助手未完全复用 quick 的 3 秒 payload 捕获策略，以及 quick/window 初始化载荷对 `userText` 的透传与消费不完整。
- `[x]` Step B 已完成：统一快捷键直达助手与 quick summon 的 3 秒 payload 捕获策略；`main/index.js` 中 `triggerPromptShortcut()` 已改为复用 `collectQuickPayloadFast()`，优先走 quick 已验证的文件 / 图片 / 文本采集链路，并补充快捷键链路调试摘要。
- `[x]` Step C 已完成：收口 quick / fast / window 初始化载荷字段；`main/windowManager.js` 已为 quick init 透传 `userText`，`render/window/App.vue` 已补齐图片/文件场景下对 `userText` 的消费，使“附件 + 附带文本说明”在快捷键召唤链路中可一起自动上传。
- `[x]` Step D 已完成：已执行 `pnpm build`，构建通过（2026-04-01）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。后续仍建议补一次真实桌面手测，重点检查“复制文件/图片/文本后 3 秒内用快捷键直达助手”与“选中文件后快捷键直达助手”两个场景。
- `[x]` Step E 已完成：已做定向 Git 提交存档：`6b02306 fix(shortcut): auto-forward recent payload to quick assistant`；文档收口补充提交：`8601309 docs(progress): finalize shortcut summon auto-upload status`

---


### 16. 2026-04-01 本轮进行中（快捷键直达助手首开性能优化） `[/]`

- `[x]` Step A 已完成：对比“quick 内点击助手”与“全局快捷键直达助手”的调用链，确认性能差异主要来自快捷键链路在开窗前同步等待 3 秒 payload 采集，而 quick 是“先开壳、后补内容”，因此体感明显更快。
- `[x]` Step B 已完成：参考 quick 的“先显示壳体、再异步补载内容”模式，已把快捷键直达 `window` 改造成“先 `openWindow('window', { code })`，再异步采集并投递 payload”，避免主进程在开窗前等待文件 / 图片 / 文本捕获。
- `[x]` Step C 已完成：为窗口管理层补充最小就绪等待能力 `waitForWindowReady()`，并在 `render/window/App.vue` 增加启动期 payload 队列；窗口未完成 init 时先排队，初始化完成后再 flush，避免 payload 丢失、事件过早处理或为性能优化而二次开窗造成闪烁。
- `[x]` Step D 已完成：已执行 `pnpm build`，构建通过（2026-04-01）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。运行态重点建议手测“快捷键直达助手首开速度”与“出现后是否无闪烁、无二次跳动”。
- `[x]` Step E 已完成：已做定向 Git 提交：`45bad9b perf(shortcut): open assistant window before payload capture`

---


### 17. 2026-04-01 本轮进行中（SSL 握手失败刷屏 / debug 残留清理） `[/]`

- `[x]` Step A 已完成：定位启动期 `ssl_client_socket_impl` / `net_error -100` 的主要来源，确认当前最可疑链路为窗口背景图远程缓存请求；同时核对当前工作区，确保修复过程不覆盖用户已有未提交改动。
- `[x]` Step B 已完成：收口窗口背景图的重复加载路径；`render/window/App.vue` 中 `applyPromptRuntimeConfig()` 已不再主动 `await loadBackground(...)`，改为统一由 `watch(promptBackgroundImage, { immediate: true })` 驱动，避免启动期同一远程背景图被重复请求。
- `[x]` Step C 已完成：为失败的远程背景图请求增加会话内冷却；同一 URL 在 60 秒内失败后不再立即重复抓取，从而显著减少 TLS 握手失败刷屏。
- `[/]` Step D 进行中：执行 `pnpm build` 验证，并继续观察是否仍有启动期 SSL 握手刷屏风险。
- `[ ]` Step E 待办：由于当前工作区已含用户未提交改动，本轮默认不主动 Git 提交；待用户确认后再做定向存档。

---

### 18. 2026-04-01 本轮进行中（fast_input 快捷输入桌面端恢复） `[/]`
- `[x]` Step O 已完成：已修复 fast_input 取消请求 / 关闭窗口时的崩溃；`main/windowManager.js` 中 `fast` 单例关闭已改为 `destroy` 策略，避免请求进行中或 idle 提示态触发不安全的 close/hide 竞争。
- `[x]` Step P 已完成：已修复空 payload 提示“请先复制或选择文本、文件、图片后再召唤”后的卡死与崩溃；idle 提示态不再主动进入危险关闭链路，前端关闭与主进程 session 状态也已同步收口。
- `[x]` Step Q 已完成：已修复“重复复制相同文本/文件/图片时 freshness 时间戳不刷新”的问题；`main/core/system.js` 现在会在检测到仍有有效 clipboard/selection 内容时刷新时间戳，从而保证重新复制相同内容后仍可按最近 3 秒 payload 自动上传。


- `[x]` Step M 已完成：已修复 fast_input 默认置顶、默认定位到鼠标所在屏幕中央偏下（距底边约 15%）、以及请求完成后自动复制到剪贴板；`main/windowManager.js` 与 `render/fast_input/App.vue` 已同步收口相关行为。
- `[x]` Step N 已完成：已修复 fast_input 的关闭策略与防崩溃保护；取消“失焦立即关闭”，改为“请求完成后失焦 5 秒自动关闭 / 用户复制或输入后自动关闭”，并为空 payload、取消请求增加稳定提示与主进程 abort 兜底。


- `[x]` Step K 已完成：已将 `main/core/chat.js` 的 OpenAI 客户端网络层对齐到桌面端已验证可用的 Chromium 代理链路，接入 `fetchWithProxy` 并补充浏览器态请求头，避免 fast_input 主进程请求比独立窗口更容易被 Cloudflare/网关拦截；本轮重点收敛 `403 Your request was blocked` 与 SSL 握手刷屏风险。
- `[x]` Step L 已完成：已修复 fast_input 默认定位与拖动体验；窗口现在默认出现在鼠标所在屏幕的中央下方，且胶囊条主体支持拖动，左右按钮/图标区域保持可点击。


- `[x]` Step H 已完成：已按用户反馈把 `render/fast_input/App.vue` 收口为接近原插件 `fast_input.html` 的极简胶囊通知条，移除模型信息、附件说明等冗余内容，仅保留关闭 / 通知结果 / 复制 / 输入（含拖拽入口）核心操作。
- `[x]` Step I 已完成：已修复 quick → fast_input 的文本 payload 识别问题；`render/quick/App.vue` 现在会把 `attachment.type === 'text'` 的单行复制文本正确映射为 `type='over'`，不再误判为 `empty`；`main/core/fastInput.js` 也已将 `multiline-text` 纳入直接请求语义。
- `[x]` Step J 已完成：已修复全局快捷键直达 `fastinput` 助手时的 3 秒 payload 自动发送链路；`main/index.js` 在 `showMode === 'fastinput'` 时已改为直接 `openWindow('fast', ...)`，不再错误回落到 quick 中转。


- `[x]` Step A 已完成：已核对原插件 `Fast_window/fast_input.html`、`backend/src/data.js`、`fast_window_preload.js` 与桌面端 `quick` / `fast_window` / `windowManager` 现状，确认当前桌面端 `fast_window` 仅为中转层，尚未恢复原插件的“主进程执行 + 流式展示 + 复制/输入”快捷输入能力。
- `[x]` Step B 已完成：已新增 `main/core/fastInput.js`，在主进程内按快捷助手配置直接发起请求，并通过 `fast-input:event` 向快捷输入窗口推送初始化、流式 chunk、完成态与错误态；`main/windowManager.js` 已将 `fast` 窗口切换到独立 `fast_input` 资源并在单例首开/复用时自动启动该链路。
- `[x]` Step C 已完成：已在 `main/core/system.js` 增加 Windows 11 下的“写入剪贴板 + PowerShell SendKeys(^v)”输入投递实现，并通过 `system:input:pasteText` IPC 暴露给渲染层。
- `[x]` Step D 已完成：已新增 `preload/fast_input_preload.js` 并接入 `electron.vite.config.mjs`，使快捷输入拥有独立 preload 与独立 renderer 构建入口，后续可与追问/append selector 继续彻底分离。
- `[x]` Step E 已完成：已新增 `render/fast_input/*` 首版前端，恢复快捷输入悬浮窗的流式展示、复制、发送到当前输入框、关闭与错误反馈闭环；当前附件型场景先以“可见提示 + 后续补强”方式收口，未再沿用旧的中转器实现。
- `[x]` Step F 已完成：已执行 `pnpm build`，构建通过（2026-04-01）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。运行态建议优先手测：① 文本类快捷助手（如中译英）流式返回；② 结果复制；③ 结果发送到当前输入框；④ 图片/文件型快捷助手当前是否正确进入首版降级提示路径。
- `[x]` Step F.1 已完成：已根据用户反馈完成 UI 收口与 payload 自动发送修复后再次执行 `pnpm build`，二次构建通过；当前优先建议运行态手测“quick 复制文本直达 fast_input”与“全局快捷键直达 fastinput 助手自动带入 3 秒内文本/图片/文件”两条链路。
- `[x]` Step F.2 已完成：已完成 fast_input 网络链路、默认定位与拖动能力修复后再次执行 `pnpm build`，三次构建通过；当前优先建议运行态手测 `403/SSL` 是否消失、窗口是否默认出现在鼠标所在屏幕中央下方，以及胶囊条是否可拖动。
- `[x]` Step F.3 已完成：已完成“默认置顶 + 完成后自动复制 + 请求完成后失焦 5 秒自动关闭 + 取消请求安全兜底”修复后再次执行 `pnpm build`，四次构建通过；当前优先建议运行态手测取消请求、空 payload 提示、自动复制与失焦倒计时关闭。
- `[x]` Step F.4 已完成：已完成“fast 单例 destroy 关闭 + 空 payload 稳定性收口 + 重复复制相同内容 freshness 刷新”修复后再次执行 `pnpm build`，五次构建通过；当前优先建议运行态手测重复复制同文本/同文件后 3 秒自动上传是否恢复正常。
- `[x]` Step G 已完成：已做定向 Git 提交存档：`b165a4d feat(fast-input): restore desktop quick input pipeline`。
- `[x]` Step G.1 已完成：已做二次定向 Git 提交存档：`a92b9ab fix(fast-input): simplify ui and forward recent payload`。
- `[x]` Step G.2 已完成：已做三次定向 Git 提交存档：`8c49e39 fix(fast-input): align network, position and drag behavior`。
- `[x]` Step G.3 已完成：已做四次定向 Git 提交存档：`298df31 fix(fast-input): auto-copy and harden close behavior`。

---


### 19. 2026-04-07 本轮进行中（清理遗留 fast_window 中转层） `[x]`

- `[x]` Step A 已完成：已用 `fast_context_search` + 接线核对确认当前真实快捷输入链路为 `main/windowManager.js` → `preload/fast_input_preload.js` → `render/fast_input/*`；`render/fast_window/*` 仅剩旧中转层，已获用户确认删除。
- `[x]` Step B 已完成：已按“构建配置 → preload → 前端遗留”顺序移除 `fast_window` 相关残留（`electron.vite.config.mjs`、`preload/fast_preload.js`、`render/fast_window/*`）。
- `[x]` Step C 已完成：已更新 `README.md` 与本文件中对 `fast_input` / `fast_window` 的当前结论，并修正相关入口描述。
- `[x]` Step D 已完成：已执行 `pnpm build`，当前构建入口已收口为 `main / window / fast_input / quick`；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step E 已完成：已执行 Git 定向提交存档：`0e9ec22 refactor(fast-input): remove legacy fast_window bridge`。

---



### 20. 2026-04-07 本轮进行中（quick 新鲜度 / 选中文本 / 文件前置校验 / window 请求回归） `[x]`

- `[x]` Step A 已完成：修复 quick 的 3 秒 freshness 失效问题；`main/core/system.js` 中 clipboard / explorer / file-drop 时间戳已恢复为“仅在签名变化时刷新”，避免过期很久的复制/选中内容仍被自动上传。
- `[x]` Step B 已完成：补齐网页选中文本的 quick 自动捕获；`render/quick/App.vue` 在打开时会主动触发一次 `refreshFromClipboard(true)`，未复制时也会 best-effort 触发选中文本捕获。
- `[x]` Step C 已完成：把文件不支持提示前移到复制 / 拖拽 / 选择阶段；`main/core/file.js` 的 `isFileTypeSupported()` 负责扩展名层面的快速筛选，同时已新增 `probeFilePathSupport()` 作为“未知扩展走内容探针”的前置校验。`render/quick/App.vue` 与 `render/window/App.vue` 现已改用该探针能力，因此 `.svg` 按文本文件处理，`.tpg` 这类自定义文本扩展也可在接入阶段被识别为可上传文本文件，而真正的二进制未知文件仍会被前置拦截。
- `[x]` Step D 已完成：修复 window 请求回归报错；`main/core/net.js` 已为 preload/window 场景加入 `global fetch` 回退，避免 `net.fetch` 在当前上下文不可用时触发 `Cannot read properties of undefined (reading 'fetch')`；同时已清理 `render/window/App.vue` 中遗留的 `file2fileList` debug log。
- `[x]` Step E 已完成：已执行 `pnpm build` 验证通过，并按用户要求保存当前工作区到 Git 存档。

---



---

### 21. 2026-04-07 本轮进行中（quick 融合追问窗口 / 追问目标栏） `[x]`

- `[x]` Step A 已完成：按“后端 → preload/IPC → 前端”顺序推进；已在 `main/windowManager.js` 为多实例 `window` 建立可展示 metadata（`displayName` / `icon` / `promptCode` 等），并新增主进程 `appendPayloadToWindow()` 能力，用于先激活目标窗口、等待 ready，再定向投递追问 payload。
- `[x]` Step B 已完成：已在 `preload/quick_preload.js` 收口 `listAppendTargets()` / `appendToWindow()` 桥接 API，quick 前端后续可直接获取追问目标列表并向指定窗口发送追问，无需拼装底层事件协议。
- `[x]` Step C 已完成：已调整 `render/quick/App.vue` 与 `main/windowManager.js`，使 quick 窗口高度增至 `452`，并在底部增加“已打开窗口”追问栏；点击目标窗口后会复用 quick 当前 payload，定向发送到对应 `window` 并自动隐藏 quick。
- `[x]` Step D 已完成：已修正 quick 附件场景下的快捷助手排序规则——纯图片优先图片型助手，纯文本优先文本型助手，混合内容/文件内容优先通用型助手，其次再给文件型/图片型兜底排序。
- `[/]` Step E 进行中：每个子步骤完成后执行 `pnpm build` 验证，通过后立即更新本文件并做定向 Git 提交存档。
- `[x]` Step E.1 已完成：后端 Step A 已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step E.2 已完成：preload Step B 已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。
- `[x]` Step E.3 已完成：前端 Step C / D 已执行 `pnpm build`，构建通过（2026-04-07）；保留既有 `main/core/mcp.js` 静态 + 动态引入告警（非本步骤阻塞项）。

## 11. 历史说明

旧版 `进度.md` 中包含大量 2026-03-24 ~ 2026-03-25 的会话级流水记录、热修日志和细粒度修复条目。此次整理后：

- **本文件只保留当前状态、主线计划与必要结论**；
- 更细的单次修复细节以 Git 历史和后续定向提交为准；
- 若后续需要追某个历史修复点，优先看对应 commit，而不是继续把流水账堆回主文档。
