# 🚀 AI Anywhere Desktop 重构进度表

> **核心原则重申：**
> 1. **拒绝展位代码**：严格按顺序开发，底层能力（如数据库、窗口管理）未实现前，绝不进行前端 UI 的硬绑定。
> 2. **彻底脱离 uTools**：将所有 `utools.xxx` API 替换为 Electron 原生或 Node.js 实现。
> 3. **前后端数据转换**：严格管控 IPC 通信前后的数据序列化与反序列化（如 `File` 对象、`Buffer`、`Error` 等）。

原项目地址：E:\Programming\Anywhere
软件项目地址：E:\Programming\Anywhere_desktop

---

## 阶段 0：项目初始化与基础设施 (Phase 0)

```
PS E:\Programming> pnpm create @quick-start/electron Anywhere_desktop
.../19cdd49b4ca-5abc                     |   +8 +
.../19cdd49b4ca-5abc                     | Progress: resolved 8, reused 8, downloaded 0, added 8, done
√ Package name: ... anywhere-desktop
√ Select a framework: » vue
√ Add TypeScript? ... No / Yes
√ Add Electron updater plugin? ... No / Yes
√ Enable Electron download mirror proxy? ... No / Yes

Scaffolding project in E:\Programming\Anywhere_desktop...

Done. Now run:

  cd Anywhere_desktop
  pnpm install
  pnpm dev

```

- [ ] **0.1 基础环境搭建**
  - [ ] 初始化 `pnpm` 工作区，安装 `electron`、`vite`、`vue` 等核心依赖。
  - [ ] 划分目录结构：`main/` (主进程), `preload/` (预加载), `render/` (渲染进程)。
  - [ ] 配置 `vite.config.js`，支持多入口（`main/index.html`, `window/index.html`, `fast_window/index.html`）。
  - [ ] 配置 Electron Builder / Electron Packager 的基础打包脚本。

- [ ] **0.2 核心数据转换器 (`main/dataConverter.js`)**
  - [ ] 实现 `File` 到 `Path` / `Buffer` 的序列化拦截处理。
  - [ ] 实现 Error 对象的标准化提取与传递封装。
  - [ ] 实现 JSON 安全的深拷贝验证工具。

---

## 阶段 A：后端架构与 Preload 桥接 (Phase A)
*复刻原后端逻辑，用 Electron 原生能力完全替代 uTools 底座功能。*

- [ ] **A.1 本地数据库重构 (`main/core/db.js`)**
  - [ ] 选型并接入本地 JSON 数据库（如 `lowdb` 或 `sqlite`）以替代 `utools.db`。
  - [ ] 实现文档 CRUD 操作：`put`, `get`, `remove`, `allDocs`, `bulkDocs`。
  - [ ] 实现附件存储机制：`postAttachment`, `getAttachment`（将 Base64/Buffer 写入本地特定缓存目录，维护元数据映射）。
  - [ ] 实现类 `localStorage` 接口：`dbStorage.setItem / getItem`。

- [ ] **A.2 窗口管理器重构 (`main/windowManager.js`)**
  - [ ] 替代 `utools.createBrowserWindow`，封装带状态的 `BrowserWindow` 创建逻辑。
  - [ ] 实现主控台（Main Window）的单例管理与显隐控制 (`hideMainWindow`, `showMainWindow`)。
  - [ ] 实现独立对话窗口（Agent Window）的多实例管理，维护 `windowMap`，注入唯一 `senderId`。
  - [ ] 实现快捷输入窗口（Fast Window）的无边框、透明、失去焦点自动隐藏处理。
  - [ ] 替代 `utools.redirect`：实现进程内事件总线（EventBus），完成窗口间的指令投递（如“追加消息”）。

- [ ] **A.3 系统级 API 重写 (`main/core/system.js`)**
  - [ ] 剪贴板：基于 `electron.clipboard` 实现 `copyText`、`copyImage` 等。
  - [ ] 文件弹窗：基于 `electron.dialog` 实现 `showOpenDialog`、`showSaveDialog`。
  - [ ] 文件管理器：基于 `electron.shell` 实现 `shellOpenPath`、`shellShowItemInFolder`。
  - [ ] 屏幕拾取：基于 `desktopCapturer` 实现音视频/窗口源获取。

- [ ] **A.4 原后端业务代码迁移与适配**
  - [ ] 重构 `main/core/data.js`：接入新版 DB API，替换所有 `utools.db` 调用。
  - [ ] 重构 `main/core/file.js`：将原 uTools 文件对象逻辑改写为基于 Node.js `fs/promises` 的绝对路径读取。
  - [ ] 迁移 `chat.js`、`mcp.js`、`mcp_builtin.js`、`skill.js`：审查并替换其中可能存在的 uTools API。

- [ ] **A.5 Preload 层隔离实现**
  - [ ] 编写 `preload/main_preload.js`：暴露给主设置界面的 API。
  - [ ] 编写 `preload/window_preload.js`：暴露给对话界面的 API（包含窗口控制、置顶切换等 IPC）。
  - [ ] 编写 `preload/fast_preload.js`：暴露给快捷指令窗的轻量级 API。

---

## 阶段 B：Anywhere_main 主控制台复刻 (Phase B)
*直接复用 Vue 代码，修改接口对接方式，确保界面与设置数据跑通。*

- [ ] **B.1 前端环境与公共资源挂载**
  - [ ] 在 `render/main` 中配置 Vue + Element Plus，引入原项目的 `base.css`, `dark.css` 等样式文件。
  - [ ] 确保原主题切换逻辑（监听媒体查询及 `html.dark` 类名）正常工作。

- [ ] **B.2 设置与状态流转适配**
  - [ ] `Setting.vue`: 接入新版 `window.api.getConfig` 和 `window.api.saveSetting`。
  - [ ] 测试数据导入/导出功能（由于移除了 uTools 底座，需确保文件存取走 `dialog` API）。
  - [ ] 测试 WebDAV 同步功能网络请求是否在 Electron 渲染进程正常执行（或移交主进程处理跨域）。

- [ ] **B.3 其他管理页面复刻**
  - [ ] `Providers.vue` (服务商)：测试 API 连通性，确保模型列表读取正常。
  - [ ] `Mcp.vue` (MCP 管理)：测试子进程拉起能力（Stdio 类型的 MCP 服务能否被主进程正确唤起）。
  - [ ] `Skills.vue` (Skill 管理)：测试本地文件夹读取、Zip 解压（需调用主进程方法）。
  - [ ] `Prompts.vue` (快捷助手)：测试正则匹配、数据关联。
  - [ ] `Tasks.vue` (定时任务)：适配原生定时任务轮询器 (`scheduler.js`)。
  - [ ] `Chats.vue` (历史对话)：测试本地文件列表读取。

---

## 阶段 C：Anywhere_window 独立对话窗口复刻 (Phase C)
*确保聊天体验一致，特别是 Markdown 渲染、多模态处理和长图导出。*

- [ ] **C.1 对话框基础 UI 组装**
  - [ ] 搭建 `render/window/index.html` 及 Vue 实例。
  - [ ] 移植 `TitleBar.vue`：使用 Electron 的 IPC 控制最小化、最大化、关闭、置顶。
  - [ ] 移植 `ChatHeader.vue`、`ChatInput.vue`。

- [ ] **C.2 核心聊天组件适配**
  - [ ] 移植 `ChatMessage.vue`：集成 Markdown、KaTeX、Mermaid 及 DOMPurify 渲染。
  - [ ] 修复代码块一键复制的 API 调用。
  - [ ] **长图导出** (`html2canvas`)：由于前端安全策略限制，需确保在 Electron 中允许加载 `file://` 图片或将图片转 Base64 后再绘制，测试导出并调用原生系统弹窗保存。

- [ ] **C.3 工具审批与协同机制流转测试**
  - [ ] 测试 MCP 工具的自动执行与手动审批弹窗。
  - [ ] 测试多窗口间的协同通信（如调用 `summon_agent` 后，新弹出一个 BrowserWindow 并传递初始数据）。

---

## 阶段 D：Fast_window 快捷唤出面板重构 (Phase D)
*将原生的多个 HTML 重构，使其符合 Electron 最佳实践。*

- [ ] **D.1 Fast Input 悬浮输入条**
  - [ ] 重构 `fast_input.html`（可考虑转为轻量级 Vue3 组件或保持原生 HTML）。
  - [ ] 在 `windowManager.js` 中实现**透明、无边框、置顶**窗口属性。
  - [ ] 优化粒子特效性能（CSS Animation 在透明 Electron 窗口下的性能测试）。
  - [ ] 实现失焦自动关闭（监听 `blur` 事件并通知主进程隐藏窗口）。

- [ ] **D.2 追加选择器 (Append Selector)**
  - [ ] 重构 `append_selector.html`。
  - [ ] 实现跨窗口进程通信：获取当前存活的 `Agent Window` 列表并展示。
  - [ ] 实现点击后，将 Payload 转发至主进程，再由主进程投递给特定的对话窗口。

---

## 阶段 E：桌面端专属体验优化 (Phase E)
*利用纯桌面应用特权，提供原 uTools 版本无法实现的高级功能。*

- [ ] **E.1 全局快捷键与呼出机制**
  - [ ] 主进程注册 `globalShortcut`（如 `Alt+Space` 唤出主控台，`Alt+A` 唤出快捷输入）。
  - [ ] 在设置中心 (`Setting.vue`) 添加“自定义全局快捷键”的 UI 并绑定至主进程。

- [ ] **E.2 划词直接调用功能 (重磅扩展)**
  - [ ] 实现原生选词捕获机制：监听特定组合键（如连续按两下 `Ctrl`），触发主进程模拟发送 `Ctrl+C`。
  - [ ] 抓取系统剪贴板文本/图片，自动填入并直接弹出 Fast Input 或指定的独立对话窗口。

- [ ] **E.3 系统托盘 (System Tray) 与后台运行**
  - [ ] 创建 `Tray` 图标，添加右键菜单（显示主窗口、退出应用等）。
  - [ ] 拦截主窗口的“关闭”事件，改为“隐藏到托盘”（确保定时任务持续运行）。

- [ ] **E.4 打包与分发**
  - [ ] 优化应用启动速度（Vite 预构建、懒加载等）。
  - [ ] 配置 NSIS (Windows) 打包参数（包括应用图标、安装路径、自启动注册表配置）。
  - [ ] 执行端到端测试，检查数据在打包后的 `userData` 路径读写是否正常。

---

> **当前阶段标记**：🔴 **等待启动 Phase 0**
> *（请确认是否开始进行 Phase 0 和 Phase A 的代码编写）*