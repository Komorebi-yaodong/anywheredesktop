# 🚀 AI Anywhere Desktop 重构进度表（整理增强版 / 基准：2026-03-13）

> **整理原则（不删减内容，只重排与去重表达）：**  
> 1) 所有阶段/子任务/验证/根因/修复/提交号/用户反馈 **全部保留**；  
> 2) 将重复出现的“会话对齐/基线通过/下一步计划”合并为“统一总览 + 按时间日志”；  
> 3) 用“✅已完成 / 🟡进行中 / ⏳待开始”统一状态口径，避免同一事项在不同位置出现冲突。

---

## 0. 项目信息（固定锚点）

- 原项目地址：`E:\Programming\Anywhere`  
- 软件项目地址：`E:\Programming\Anywhere_desktop`

---

## 1. 核心原则重申（原文保留）

1. **拒绝展位代码**：严格按顺序开发，底层能力（如数据库、窗口管理）未实现前，绝不进行前端 UI 的硬绑定。  
2. **彻底脱离 uTools**：将所有 `utools.xxx` API 替换为 Electron 原生或 Node.js 实现。  
3. **前后端数据转换**：严格管控 IPC 通信前后的数据序列化与反序列化（如 `File` 对象、`Buffer`、`Error` 等）。

---

## 2. 当前全局状态总览（把分散信息汇总到一处）

### ✅ 已稳定完成并通过构建（多次复核）
- Phase 0.1 多入口基础设施（main/window/fast_window）可用  
- Phase 0.2 `main/dataConverter.js` 核心数据转换器可用  
- Phase A：A.1（DB）/ A.2（WindowManager+EventBus）/ A.3（System API）/ A.4（data/file/chat/mcp/skill 迁移）/ A.5（Preload 隔离 + 窗口控制）均已落地并 `pnpm build` 通过  
- Phase B：
  - B.1（前端环境与公共资源挂载）关键步骤已完成（Element Plus + i18n + base.css/main.css 迁移）
  - B.2（Setting 适配：本地导入导出 + 主题 + memory 同步 + WebDAV 主进程化）已完成
  - B.3（Providers/Mcp/Skills/Prompts/Tasks/Chats）已完成并接入主控台导航
  - B.4（主控台样式深度复刻修复）已完成多轮热修与 CSP 收尾（见日志）

### 🟡 当前进行中（按你最后的要求聚焦）
1. **普通 MCP：残余 `An object could not be cloned` 场景继续收敛**（沿 `Mcp.vue -> preload/ipc -> mcp.js` 参数链继续深挖）  
2. **内置 MCP：不再“最小可用”，必须对齐原项目 `backend/src/mcp_builtin.js` 做“完整复刻”**  
3. **Skill：同样要求按原项目完整复刻运行链路**（不只保留桌面端简化版）

### ⏳ 待开始（后续主线）
- Phase C：Anywhere_window 独立对话窗口复刻  
- Phase D：Fast_window 快捷唤出面板重构  
- Phase E：桌面端专属体验优化（全局快捷键、划词、托盘、打包分发等）

---

## 3. 关键验证结论（集中放置，避免到处重复）

- ✅ `pnpm build` 已通过（`E:\Programming\Anywhere_desktop`）  
- 输出产物包含：
  - `out/main/index.js`
  - `out/preload/main_preload.js` / `window_preload.js` / `fast_preload.js`
  - `out/renderer/main/index.html` / `window/index.html` / `fast_window/index.html`
- 结论：Phase 0.1 多入口基础设施可用。

---

## 4. 阶段 0：项目初始化与基础设施 (Phase 0)

### 0.1 基础环境搭建 ✅
- [X] 初始化 `pnpm` 工作区，安装 `electron`、`vite`、`vue` 等核心依赖。  
- [X] 划分目录结构：`main/` (主进程), `preload/` (预加载), `render/` (渲染进程)。  
- [X] 配置 `vite.config.js`，支持多入口（`main/index.html`, `window/index.html`, `fast_window/index.html`）。  
- [X] 配置 Electron Builder / Electron Packager 的基础打包脚本。  

**初始化脚手架命令记录（原文保留）：**
```powershell
PS E:\Programming> pnpm create @quick-start/electron Anywhere_desktop
.../19cdd49b4ca-5abc                     |   +8 +
.../19cdd49b4ca-5abc                     | Progress: resolved 8, reused 8, downloaded 0, added 8, done
√ Package name: ... anywhere-desktop
√ Select a framework: » vue
√ Add TypeScript? ... No / Yes
√ Add Electron updater plugin? ... No / Yes
√ Enable Electron download mirror proxy? ... No / Yes

Scaffolding project in E:\Programming\Anywhere_desktop...

Done. Now run:

  cd Anywhere_desktop
  pnpm install
  pnpm dev
```

### 会话切换快照（2026-03-11）✅（原信息集中归档）
- ✅ 你已确认：当前无报错，主/窗/快窗口均可打开；界面仅骨架按钮（符合 Phase 0.1 预期）。  
- ✅ 本地 Git 存档已完成（未 push）：
  - `9e463c9 fix(phase0.1): 修复空白窗口渲染报错并稳定三窗口骨架`
- 🔜 下一会话首要任务（Phase 0.2）：
  1. 新建 `main/dataConverter.js`
  2. 实现 `File -> Path/Buffer` 序列化转换
  3. 实现 `Error` 标准化序列化
  4. 实现 `safeClone`（JSON 安全深拷贝）
  5. 在主进程 IPC 层接入 converter 并完成 `pnpm build` 验证

### 验收修复记录（2026-03-11）✅（原文信息保留）
- 🐞 问题：主窗口空白，DevTools 报错：`Property "window" was accessed during render but is not defined on instance`。  
- 根因：在 Vue 模板中直接访问 `window.api`，模板渲染上下文中不允许直接访问全局 `window`。  
- 修复：
  - `render/main|window|fast_window/App.vue` 改为在 `<script setup>` 中声明 `appWindowType`，模板只使用响应式变量。
  - `preload/*_preload.js` 的 `openWindow` 改为 `electronAPI.ipcRenderer.invoke(...)`，避免依赖 `window.electron`。
- ✅ 再验证：`pnpm build` 通过。  

### 验证记录（2026-03-11 / 2026-03-12）✅
- ✅ `pnpm build` 通过（2026-03-12）：dataConverter 初始版本构建成功。  
- 📝 2026-03-12：用户授权 AI 执行 Git 存档；受当前系统 Git 只读策略限制，仍需人工提交。  

---

### 0.2 核心数据转换器 (`main/dataConverter.js`) ✅
- [x] 实现 `File` 到 `Path` / `Buffer` 的序列化拦截处理。  
- [x] 实现 Error 对象的标准化提取与传递封装。  
- [x] 实现 JSON 安全的深拷贝验证工具。  

---

## 5. 阶段 A：后端架构与 Preload 桥接 (Phase A)

> 复刻原后端逻辑，用 Electron 原生能力完全替代 uTools 底座功能。

---

### A.1 本地数据库重构 (`main/core/db.js`) ✅
- [x] 选型并接入本地 JSON 数据库（如 `lowdb` 或 `sqlite`）以替代 `utools.db`。  
- [x] 实现文档 CRUD 操作：`put`, `get`, `remove`, `allDocs`, `bulkDocs`。  
- [x] 实现附件存储机制：`postAttachment`, `getAttachment`（将 Base64/Buffer 写入本地特定缓存目录，维护元数据映射）。  
- [x] 实现类 `localStorage` 接口：`dbStorage.setItem / getItem`。  

**✅ A.1 子任务记录（2026-03-12 / Step A.1.1）**
- 已接入 `lowdb` 并落地 `main/core/db.js`。  
- 已实现：
  - 文档 CRUD：`put/get/remove/allDocs/bulkDocs`
  - 附件：`postAttachment/getAttachment`（落盘到 `userData/attachments`）
  - 存储：`dbStorageSetItem/dbStorageGetItem/dbStorageRemoveItem/dbStorageListKeys`
  - 诊断：`isDbReady/getDbStats`
- IPC 已接入：`db:*` 与 `dbStorage:*` 通道。  
- preload 已暴露 db 接口（main/window/fast 三窗口一致）。  
- ✅ 验证：`pnpm build` 通过。  

---

### A.2 窗口管理器重构 (`main/windowManager.js`) ✅

**目标与产出（原文要点保留、集中整理）：**
- 目标：把 `main/index.js` 中的窗口创建逻辑抽离为独立模块，形成可复用的 `windowManager`；并在 IPC 层形成统一的 invoke-handler 包装，保证返回值可序列化、异常可结构化回传。  
- 产出文件：
  - `main/windowManager.js`：
    - `openWindow(type)`：`main/fast` 走单例复用；`window` 走多实例（用 `randomUUID()` 生成 id 并维护索引）。
    - 提供 `getWindowIds(type)` / `getWindowById(id)` 作为后续多窗口通信/投递的基础能力。
  - `main/ipcHandler.js`：
    - `handleInvoke(channel, handler)`：统一 try/catch；成功路径走 `serializeIpcPayload`；失败路径回 `{ok:false, error: serializeError(err)}`。
    - `registerIpcHandlers(...)`：已从单一 `window:open` 扩展到主窗口显隐、窗口列表与事件投递。
  - `main/index.js`：精简为应用生命周期 + 注册 IPC + `openWindow('main')`。
- 验证：`pnpm build` 通过；`pnpm dev` 可正常启动（renderer dev server 5173 + Electron app）。  
- 约束说明：当前 `window:open` 仍返回基础信息（ok/type/id），后续做“消息投递/窗口间通信”时再引入 senderId 与更精细的路由。  

**完成项（原 checklist 保留）：**
- [x] 替代 `utools.createBrowserWindow`，封装带状态的 `BrowserWindow` 创建逻辑。  
- [x] 实现主控台（Main Window）的单例管理与显隐控制 (`hideMainWindow`, `showMainWindow`)。  
- [x] 实现独立对话窗口（Agent Window）的多实例管理，维护 `windowMap`，注入唯一 `senderId`。  
- [x] 实现快捷输入窗口（Fast Window）的无边框、透明、失去焦点自动隐藏处理。  
- [x] 替代 `utools.redirect`：实现进程内事件总线（EventBus），完成窗口间的指令投递（如“追加消息”）。  

**✅ A.2 子任务记录（按原 Step 顺序归档）**
- ✅ Step A.2.2：主窗口显隐能力  
  - `showMainWindow()` / `hideMainWindow()`（`main/windowManager.js`）  
  - IPC：`window:showMain` / `window:hideMain`（`main/ipcHandler.js`）  
  - 主进程注入：`registerIpcHandlers({ openWindow, showMainWindow, hideMainWindow })`（`main/index.js`）  
  - 三套 preload 暴露：`showMainWindow` / `hideMainWindow`  
  - 骨架页面加入联调入口按钮（main/window/fast）  
  - ✅ 验证：`pnpm build` 通过  

- ✅ Step A.2.5：EventBus 投递链路  
  - 新增 `main/eventBus.js`：`dispatchWindowEvent`  
  - target：
    - `target: 'main' | 'fast' | '<windowId>'`（定向投递）
    - `target: 'type:window'`（按窗口类型投递）
    - `target: 'broadcast' | '*'`（全量广播）
  - IPC：
    - `window:list`
    - `window:event:emit`
  - windowManager 增强：
    - `getWindowByRef / listWindows / getWindowRefByWebContentsId`
    - 为每个窗口绑定 `webContents.id -> windowRef`
  - 三套 preload 暴露：`listWindows / emitWindowEvent / onWindowEvent`
  - 骨架页增加联调按钮与“最近收到事件”展示（追加消息式投递验证）
  - ✅ 验证：`pnpm build` 通过  

- ✅ Step A.2.3：窗口初始化信号 + senderId 稳定注入  
  - 注入初始化信号 `window:init`（did-finish-load 后发送）  
  - 独立对话窗口提供稳定 `senderId`（`windowRef/id`），preload 通过 `getWindowContext()` 暴露  
  - 三套 preload 增加：`onWindowInit` + `getWindowContext`  
  - 三套骨架页展示 `senderId`（用于后续定向追问/追加消息联调）  
  - ✅ 验证：`pnpm build` 通过  

**🛠️ A.2 热修复记录（2026-03-12 / Close Crash，原文保留）**
- 用户反馈：关闭窗口时主进程报错 `TypeError: Object has been destroyed at unbindWindowRef`。  
- 根因：`BrowserWindow` 进入 `closed` 阶段后，访问 `win.webContents` 可能抛出对象已销毁异常。  
- 修复：
  - 新增 `unbindWindowRefByWebContentsId`，在窗口创建时预捕获 `webContentsId`。
  - `closed` 回调改为按预捕获 `webContentsId` 解绑映射，不再触碰已销毁对象。  
- 兼容策略：主窗口显隐逻辑保留为“可选能力”，不将“打开对话前必须隐藏主窗口”作为桌面版强制策略。  
- 体验修正：`hideMainWindow` 语义从“直接隐藏”改为“最小化主窗口”，避免主窗口彻底不可见难以找回。  

**补充说明（原文保留）：**
- A.2 核心子项均已达成（窗口创建复用、主窗口显隐、Fast 失焦隐藏、EventBus、senderId 注入）；后续将把窗口控制能力（最小化/最大化/置顶切换）在 A.5 里以稳定 API 形式补齐。  

---

### A.3 系统级 API 重写 (`main/core/system.js`) ✅
- [x] 剪贴板：基于 `electron.clipboard` 实现 `copyText`、`copyImage` 等。  
- [x] 文件弹窗：基于 `electron.dialog` 实现 `showOpenDialog`、`showSaveDialog`。  
- [x] 文件管理器：基于 `electron.shell` 实现 `shellOpenPath`、`shellShowItemInFolder`。  
- [x] 屏幕拾取：基于 `desktopCapturer` 实现音视频/窗口源获取。  

**✅ A.3 子任务记录（2026-03-12 / Step A.3.1）**
- 新增 `main/core/system.js`：
  - 剪贴板：`copyText / copyImage / readClipboardText`
  - 对话框：`showOpenDialog / showSaveDialog`
  - Shell：`shellOpenPath / shellShowItemInFolder / shellOpenExternal`
  - 桌面源：`getDesktopSources`（返回可序列化缩略图与图标）
- IPC 新增：
  - `system:clipboard:*`
  - `system:dialog:*`
  - `system:shell:*`
  - `system:desktop:getSources`
- preload 已同步暴露 system API（main/window/fast 三窗口一致）。  
- ✅ 验证：`pnpm build` 通过。  

---

### A.4 原后端业务代码迁移与适配 ✅/🟡（整体已落地，但后续仍会随“完整复刻”继续补齐）

#### A.4（总项）说明（原文要点保留）
- [/] **A.4 原后端业务代码迁移与适配**
  - [x] 重构 `main/core/data.js`：接入新版 DB API，替换所有 `utools.db` 调用。  
    - 补充说明：已完成配置分块存储（`config/prompts/providers/mcpServers/tasks/config_local_desktop`）与 memory 导入导出；后续在迁移 `chat/mcp/skill` 时按需补充扩展能力。  
    - [x] Step A.4.1（已完成）：先落地配置与 memory 同步最小闭环（`getConfig/saveSetting/updateConfig/updateConfigWithoutFeatures/exportMemoryData/importMemoryData`），并完成 IPC + preload 暴露。  
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。  
    - [x] Step A.4.3.3（已完成）：迁移 `skill.js` -> `main/core/skill.js`，并打通 skill IPC / preload（list/get/save/delete/export/extract/definition/resolve/pathJoin）。  
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。  

  - [x] 重构 `main/core/file.js`：将原 uTools 文件对象逻辑改写为基于 Node.js `fs/promises` 的绝对路径读取。  
    - [x] Step A.4.2（已完成）：新增 `main/core/file.js` 并接入 IPC（`file:*`）；三套 preload 已暴露文件相关 API（`handleFilePath/sendfileDirect/saveFile/selectDirectory/listJsonFiles/readLocalFile/renameLocalFile/deleteLocalFile/writeLocalFile/setFileMtime/isFileTypeSupported/parseFileObject/copyLocalPath`）。  
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。  

  - [x] 迁移 `chat.js`、`mcp.js`、`mcp_builtin.js`、`skill.js`：审查并替换其中可能存在的 uTools API。  
    - [x] 说明：`mcp_builtin.js` 已在桌面端落地最小可用骨架（内置工具注册与调用入口），后续在 C/D 阶段按 UI/Agent 联调逐步填充具体内置工具实现。  
    - ✅ 本轮本地提交：
      - `a70218e` feat(phase-a4.3.1): 迁移 chat 核心并接入 Electron IPC
      - `db4bbb8` feat(phase-a4.3.2): 迁移 mcp 管理核心并打通 IPC/preload
      - `d347989` feat(phase-a4.3.3): 迁移 skill 核心并接入 IPC/preload  

  - [x] Step A.4.4（已完成，2026-03-12）：同步上游 `5ccb192` 的文件类型判定增强（`file.js`），并评估 `data.js` 差异在桌面端是否存在对应实现。  
    - 结论：桌面端 `main/core/data.js` 当前未实现上游 `expectedMatchFeature/cmds` 那套分支，故本次 `data.js` 无需迁移该 diff。  
    - 已同步到 `main/core/file.js`：  
      - `isFileTypeSupported`：白名单支持 + 二进制黑名单拒绝 + 未知后缀放行待探针  
      - `parseFileObject`：新增 Base64 内容探针（`\0` 检测），未知后缀且非二进制按文本处理  
    - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。  

---

### A.5 Preload 层隔离实现 ✅
- [x] Step A.5.1（已完成）：补齐窗口控制相关 IPC 与 preload API（`window:minimize/maximizeOrRestore/close/toggleAlwaysOnTop`），为后续 `TitleBar.vue` 迁移铺路。  
  - ✅ 验证：`pnpm build` 通过（2026-03-12，本轮）。  
- [x] 编写 `preload/main_preload.js`：暴露给主设置界面的 API。  
- [x] 编写 `preload/window_preload.js`：暴露给对话界面的 API（包含窗口控制、置顶切换等 IPC）。  
- [x] 编写 `preload/fast_preload.js`：暴露给快捷指令窗的轻量级 API。  
- 说明：已统一暴露数据层、文件层、窗口事件总线与窗口控制能力，并保留 `windowControl(action)` 兼容接口以承接旧代码迁移。  
- ✅ 验证：`pnpm build` 通过（2026-03-12，本轮，见 Step A.5.1）。  

---

## 6. 阶段 B：Anywhere_main 主控制台复刻 (Phase B)

> 直接复用 Vue 代码，修改接口对接方式，确保界面与设置数据跑通。

---

### B.1 前端环境与公共资源挂载 ✅
- [x] Step B.1.0：清理脚手架残留目录 `src/`（确认当前构建不引用）  
  - ✅ `pnpm build` 通过（2026-03-12）  
- [ ] 在 `render/main` 中配置 Vue + Element Plus，引入原项目的 `base.css`, `dark.css` 等样式文件。  
- [x] Step B.1.2：同步 `Anywhere_main/src/assets/base.css` 与 `main.css` 到 `render/main/assets/`  
  - ✅ `pnpm build` 通过（2026-03-12）  
- [x] Step B.1.1：迁移插件与 i18n 初始化到 `render/main/main.js`（Element Plus + icons + i18n + locales）  
  - 新增 `render/main/locales/en.json` 与 `render/main/locales/zh.json`（最小语言包，后续扩充）  
  - 安装依赖：`element-plus`、`@element-plus/icons-vue`、`vue-i18n`  
  - ✅ `pnpm build` 通过（2026-03-12）  

---

### B.2 设置与状态流转适配 ✅
- [x] Step B.2.0：补齐 `Setting.vue` 迁移前置依赖（`vuedraggable`、`webdav`）  
  - ✅ `pnpm build` 通过（2026-03-12）  
- [x] Step B.2.1：迁移 `Setting.vue` 并接入桌面端壳（配置 + Memory 联调）  
  - `render/main/App.vue`：config provide + Setting 宿主结构  
  - `vuedraggable` 切换 Vue3 兼容版本 `^4.1.0`  
  - ✅ `pnpm build` 通过（2026-03-12）  
- [ ] 确保原主题切换逻辑（监听媒体查询及 `html.dark` 类名）正常工作。  
- [x] Step B.2.2：最小主题闭环（`themeMode` + `isDarkMode` + 系统主题监听 + `html.dark`）  
  - ✅ `pnpm build` 通过（2026-03-12）  
- [x] Step B.2.2（导入导出适配）：从浏览器下载/文件输入改造为 Electron `dialog + file IPC`（保留 memory 同步）  
  - `showSaveDialog/showOpenDialog/writeLocalFile/readLocalFile`  
  - ✅ `pnpm build` 通过（2026-03-12）  
- [x] Step B.2.3：WebDAV 同步主进程化  
  - 新增 `main/core/webdav.js` + IPC + preload  
  - Setting 的 WebDAV 备份/恢复/列表/删除改为主进程桥接调用  
  - ✅ `pnpm build` 通过（2026-03-12）  
- [x] `Setting.vue`：接入新版 `window.api.getConfig` 和 `window.api.saveSetting`  
- [x] 测试导入/导出功能（文件存取走 `dialog` API）  
- [x] 测试 WebDAV 同步网络请求是否在 Electron 渲染进程正常执行（或移交主进程处理跨域）  

---

### B.3 其他管理页面复刻 ✅
- [x] Step B.3.0：补齐阻塞 API（`coderedirect` / `runTaskNow` / `getRandomItem`）  
  - 主进程 + IPC + preload 最小兼容层  
  - 复用窗口事件总线实现跨窗口投递（`coderedirect` 与 `runTaskNow`）  
  - ✅ `pnpm build` 通过（2026-03-12）  
- [x] Providers.vue（服务商，完成 2026-03-12）：  
  - API：`getConfig/updateConfigWithoutFeatures/saveSetting/getRandomItem`  
  - ✅ `pnpm build` 通过  
- [x] Mcp.vue（MCP 管理，完成 2026-03-12）：  
  - `testMcpConnection/testInvokeMcpTool/getMcpToolCache/saveMcpToolCache`  
  - `copyText` 返回结构兼容  
  - ✅ `pnpm build` 通过  
- [x] Skills.vue（Skill 管理，完成 2026-03-12）：  
  - 本地目录读取、Skill 导入导出、文件操作 API 联调  
  - ✅ `pnpm build` 通过  
- [x] Prompts.vue（快捷助手，完成 2026-03-12）：  
  - 正则匹配/数据关联基础链路 + `coderedirect` 接线  
- [x] Tasks.vue（定时任务，完成 2026-03-12）：  
  - `runTaskNow`、任务配置读写、历史记录文件操作  
- [x] Chats.vue（历史对话，完成 2026-03-12）：  
  - 本地文件列表、重命名/删除、云端同步链路接线  

（保留原子任务记录）
- ✅ Step B.3.3：Skills 导航入口与渲染分支接入，build 通过  
- ✅ Step B.3.4：Prompts 接入，API 对齐确认，build 通过  
- ✅ Step B.3.5：Tasks 接入，API 对齐确认，build 通过  
- ✅ Step B.3.6：Chats 接入，API 对齐确认，build 通过  

---

### B.4 主控台样式深度复刻修复（新增，2026-03-12）✅/🟡（按 H* 子步归档）

#### Step B.4.0 ✅（根因排查结论原文保留）
- 结论1：`render/main/App.vue` 与原项目 `Anywhere_main/src/App.vue` SHA256 一致。  
- 结论2：桌面端 `render/main/index.html` 仅保留最小骨架，缺失原项目 `Anywhere_main/index.html` 的大体量内联主题变量与 Element Plus 全局覆盖样式，是“默认 Element 风格”的直接原因。  
- 结论3：桌面端受严格 CSP（`img-src 'self' data:`）限制，可能影响远程文档图片显示。  
- 下一步：复制原 `Anywhere_main/index.html` 主题样式块到 `render/main/index.html`，仅做 Electron 入口脚本路径适配。  

#### Step B.4.1 ✅（样式入口迁移 + marked 依赖）
- 复制 `Anywhere_main/index.html` 到 `render/main/index.html`  
- 适配入口脚本：`/src/main.js` -> `/main/main.js`  
- 构建阻塞：缺少 `marked`，已 `pnpm add marked` 修复  
- ✅ 本地 Git 存档：`63fa53e fix(main-style): 复刻Anywhere_main主题样式并补齐marked依赖`  

#### Step B.4.H1 ✅（热修：启动崩溃 + CSP meta 恢复）
- 用户反馈：启动报错 `Cannot read properties of undefined (reading 'config')`，并出现 Electron CSP 安全警告。  
- 定位：
  - `render/main/App.vue` 依赖 `window.api.defaultConfig.config`，但 preload 未暴露该字段  
  - `render/main/index.html` 复制后缺失 CSP meta  
- 修复：
  1) `App.vue` 改为安全配置归一化：优先用 `getConfig`，失败走本地 fallback  
  2) `index.html` 恢复严格 CSP（不启用 `unsafe-eval`，保留 `style-src 'unsafe-inline'`）  
- ✅ `pnpm build` 通过（2026-03-12）  

#### Step B.4.H2 ✅（2026-03-13：运行态报错分治）
- 已完成：按“先崩溃、后连通”策略推进（作为 H3 的前置收敛）。  

#### Step B.4.H3 🟡（2026-03-13：多点运行态 bug 收敛，按顺序修）
> 顺序（原文保留）：**设置/备份 → MCP 可见性 → MCP 测试 → CSP 资源**

- H3.1 ✅（已修复，待运行态复测 → 后续又补充多轮并已 build 通过）
  - 修复 `Setting.vue` WebDAV 备份/还原与本地配置导入失败（`[object Object]` / `Unexpected end of JSON input`）  
  - 根因初判：  
    - `readLocalFile` 返回值结构判断错误（字符串被当 `{ content }`）  
    - 对 `webdav/importMemoryData` IPC 错误对象未做 message 解包  
    - `mcp.js` 测试链路仅识别 `transport` 不识别页面 `type`  
    - `mcp_builtin.js` 为空对象导致“内置 MCP 全空”  
  - 修复范围：错误解包 + 本地导入修正、`type -> transport` 兼容、内置 MCP 最小可见性恢复（图片 CSP 另步）  
  - 第二轮补充：  
    - `webdav.js` 兼容 `exists()/HEAD` 405  
    - Setting 备份读写统一走 `data_path`  
    - 修正 `Mcp.vue` 对 `getMcpToolCache/testMcpConnection/testInvokeMcpTool` IPC 解包  
    - ipcHandler 保存 sanitize 工具缓存，避免 `An object could not be cloned` 与 `执行错误: [object Object]`  
  - 第三轮补充：`Mcp.vue#runToolTest` 对测试配置深拷贝，避免响应式对象穿透  
  - ✅ `pnpm build` 通过（2026-03-13）

- H3.2 ✅：恢复内置 MCP 可见性  
  - `main/core/mcp_builtin.js` 补回最小可见内置 MCP 服务与工具定义（Python / Filesystem / Bash / Search / Super-Agent / Tasks / Time / Memory）  
  - ✅ `pnpm build` 通过（2026-03-13）

- H3.3 ✅：修复 `testMcpConnection/testInvokeMcpTool` 联调失败  
  - `Mcp.vue` 正确解包 IPC 返回，失败优先展示 message  
  - 缓存工具默认 `enabled !== false` 视为启用  
  - `ipcHandler#mcp:testConnection` 先保存 sanitize 工具缓存，避免克隆异常  
  - preload 统一 `toPlainPayload(JSON.parse(JSON.stringify(...)))` 入参纯对象化  
  - `main/core/mcp.js#buildServerConfig` 兼容页面 `type` 映射到 `transport`  
  - ✅ `pnpm build` 通过（2026-03-13）

- H3.4 ✅：CSP 收尾（按你要求“不要细粒度白名单，放行远程图片/请求”）
  - 策略：保留 `script-src 'self'`，放开 `img-src/connect-src` 到 `http/https/ws/wss + data/blob/file`  
  - ✅ `pnpm build` 通过（2026-03-13）

- H3.5 🟡：继续补全桌面端内置 MCP/Skill 的完整实现（按原项目完整复刻，不再最小复刻）
  - ✅ H3.5.b（已完成，2026-03-13）：
    - 已修复 `main/core/mcp_builtin.js#get_full_memory` 的残留 `utools` 调用，统一改为桌面端 `dbGet` 读取
    - 已将 `main/core/mcp_builtin.js#getBuiltinTools` 改为异步动态注入 Super-Agent Agent 名单（从 `prompts` 文档 + config prompts 合并）
    - 已在 `main/core/mcp.js` 传递 `config.prompts` 到 `getBuiltinTools`，并移除“持久连接数量限制 + on-demand 并发阈值”逻辑
    - ✅ 验证：`pnpm build` 通过（2026-03-13）
    - 📝 备注：仍存在非阻塞打包告警（`mcp.js` 同时静态/动态引入），不影响构建产物
    - 补充修复：为兼容 `getBuiltinTools` 异步化，已将 `main/core/skill.js#getAllBuiltinToolNames/resolveSkillInvocation` 与 `main/ipcHandler.js#skill:resolveInvocation` 调整为 async 链路


  - ✅ H3.5.a（已完成，2026-03-13）：  
    - “复制为主、少量适配”迁移 `main/core/mcp_builtin.js` 第一批完整迁移  
    - 恢复 Filesystem/Bash/Search/Time/Memory/Tasks 核心 handlers 与 background shell 管理  
    - 你已人工修复字符串换行转义问题（`\n` 误写为实际换行），并清理 bk/bak 文件  
    - 同步更新语言文件：`render/main/locales/en.json`  
    - ✅ `pnpm build` 通过（2026-03-13）  
    - 📝 构建备注：保留一个非阻塞告警（`mcp.js` 同时被静态与动态引入），不影响产物与运行  
  - ⏳ 后续仍需继续：按原项目把内置 MCP handlers 全量补齐 + Skill 完整运行链路对齐

---

## 7. Chats / Providers / 配置写回 等专项修复（原记录不删，集中归档）

### Chats 专项
- ✅ 目标1：修复 `Chats.vue` 部分文件项 `basename` 缺失导致 `endsWith` 崩溃  
  - normalizeChatFile/resolveFileBasename/resolveWebdavDataPath 兜底  
  - `formatFilenameDisplay` 安全字符串处理  
  - ✅ build 通过（2026-03-12）  
  - ✅ commit：`cef592c fix(chats): 兜底历史对话文件名渲染崩溃`

- ✅ 目标2：Chats 云端 WebDAV 访问主进程代理化，规避 CSP `connect-src`  
  - 读链路：复用 `webdav:listBackups` / `webdav:readBackup`  
  - 写链路：rename/delete/sync/clean 继续迁移到 `webdav:*`；补齐 `moveFile` 与 `writeBackup.lastModified`  
  - 时间显示异常：避免回退 Unix epoch；深挖时间源并恢复真实最后修改时间  
  - dataConverter 修复：避免将普通元数据误判为 File-like 导致 `lastmod` 丢失  
  - ✅ build 通过（2026-03-13）  
  - ✅ commit：`f728a0e fix(chats): restore history timestamp display`

### Providers 专项
- ✅ 目标3：Providers 模型拉取改主进程代理，规避 CSP `connect-src`  
  - `main/core/chat.js` 新增 `listProviderModels` + IPC + preload(main)  
  - 前端仅传 `baseUrl/apiKey`，主进程请求 `/models`  
  - Providers 解包 IPC 错误 `{ ok:false, error }`，失败展示 message  
  - ✅ build 通过（2026-03-13）  
  - ✅ commit：`1551f7a fix(providers): proxy model list requests through main`  
  - ✅ commit：`78e9db7 fix(providers): handle ipc model fetch errors`

### 配置写回（克隆防护）
- ✅ 目标4：为 `atomicSave` 增加深拷贝防护，规避 `An object could not be cloned`  
  - Mcp/Prompts/Tasks：`configToSave = { config: JSON.parse(JSON.stringify(latestConfig)) }`  
  - ✅ build 通过（2026-03-13）  
  - ✅ commit：`d2b7016 fix(main-config): harden atomic save payload cloning`

---

## 8. 关键“会话对齐/交接摘要/提交号”集中区（原内容不删）

### 🧾 会话压缩总结（2026-03-12，上下文续航）
- 当前已稳定完成并通过构建：`Phase 0.2`、`A.1`、`A.2`、`A.3`。  
- 关键修复：  
  - 关闭窗口崩溃已修复  
  - 主窗口“隐藏”改为“最小化”  
- 本地 Git 存档（未 push）关键提交：  
  - `4406c10`、`27971e9`、`f50df0a`、`5784454`、`b329a43`、`9c40c31`、`7372b68`。  
- 外部变更同步：原项目 `Anywhere` 拉到 `92fb056`（删除显示通知，新增 memory 同步）  
  - 影响文件：`backend/src/data.js`、`backend/src/preload.js`、`backend/src/file.js`、`Anywhere_main/src/components/Setting.vue`  
  - 阅读确认：  
    - `preload.js` 新增 `exportMemoryData/importMemoryData` 暴露  
    - `Setting.vue` 导入导出新增 memory 同步逻辑  
    - `data.js` 迁移需移除 `showNotification` 残留并补 memory 导入导出接口  
- 图标更新状态：`E:\Programming\Anywhere_desktop\resources\icon.png` 已检测存在；代码已引用资源图标  
- 下一步（小步+验证+本地存档）：  
  1) 落地 `main/core/data.js`（配置 + memory 同步）  
  2) 落地 `main/core/file.js`  
  3) 回填 preload API 对齐 `Setting.vue` 依赖（`getConfig/saveSetting/updateConfig/exportMemoryData/importMemoryData`）  

### 🔄 会话交接摘要（2026-03-12，供新会话续接）
- ✅ 本会话已完成并提交：  
  - `fd4cee1` chore(cleanup): 删除无用 `src` 脚手架目录并验证构建  
  - `a70218e`（A.4.3.1 chat 迁移）  
  - `db4bbb8`（A.4.3.2 mcp 迁移）  
  - `d347989`（A.4.3.3 skill 迁移）  
  - `011fd29`（进度文档归档）  
- ✅ 当前代码状态：`pnpm build` 通过，仓库干净可继续  
- ✅ 目录结论：当前唯一生效渲染目录为 `render/`；`src/` 已无引用并清理  

---

## 9. 阶段 C：Anywhere_window 独立对话窗口复刻 (Phase C) ⏳（待开始）

> 确保聊天体验一致，特别是 Markdown 渲染、多模态处理和长图导出。

- [ ] **C.1 对话框基础 UI 组装**
  - [ ] 搭建 `render/window/index.html` 及 Vue 实例。
  - [ ] 移植 `TitleBar.vue`：使用 Electron 的 IPC 控制最小化、最大化、关闭、置顶。
  - [ ] 移植 `ChatHeader.vue`、`ChatInput.vue`。

- [ ] **C.2 核心聊天组件适配**
  - [ ] 移植 `ChatMessage.vue`：集成 Markdown、KaTeX、Mermaid 及 DOMPurify 渲染。
  - [ ] 修复代码块一键复制的 API 调用。
  - [ ] **长图导出** (`html2canvas`)：由于前端安全策略限制，需确保在 Electron 中允许加载 `file://` 图片或将图片转 Base64 后再绘制，测试导出并调用原生系统弹窗保存。

- [ ] **C.3 工具审批与协同机制流转测试**
  - [ ] 测试 MCP 工具的自动执行与手动审批弹窗。
  - [ ] 测试多窗口间的协同通信（如调用 `summon_agent` 后，新弹出一个 BrowserWindow 并传递初始数据）。

---

## 10. 阶段 D：Fast_window 快捷唤出面板重构 (Phase D) ⏳（待开始）

> 将原生的多个 HTML 重构，使其符合 Electron 最佳实践。

- [ ] **D.1 Fast Input 悬浮输入条**
  - [ ] 重构 `fast_input.html`（可考虑转为轻量级 Vue3 组件或保持原生 HTML）。
  - [ ] 在 `windowManager.js` 中实现**透明、无边框、置顶**窗口属性。
  - [ ] 优化粒子特效性能（CSS Animation 在透明 Electron 窗口下的性能测试）。
  - [ ] 实现失焦自动关闭（监听 `blur` 事件并通知主进程隐藏窗口）。

- [ ] **D.2 追加选择器 (Append Selector)**
  - [ ] 重构 `append_selector.html`。
  - [ ] 实现跨窗口进程通信：获取当前存活的 `Agent Window` 列表并展示。
  - [ ] 实现点击后，将 Payload 转发至主进程，再由主进程投递给特定的对话窗口。

---

## 11. 阶段 E：桌面端专属体验优化 (Phase E) ⏳（待开始）

> 利用纯桌面应用特权，提供原 uTools 版本无法实现的高级功能。

- [ ] **E.1 全局快捷键与呼出机制**
  - [ ] 主进程注册 `globalShortcut`（如 `Alt+Space` 唤出主控台，`Alt+A` 唤出快捷输入）。
  - [ ] 在设置中心 (`Setting.vue`) 添加“自定义全局快捷键”的 UI 并绑定至主进程。

- [ ] **E.2 划词直接调用功能 (重磅扩展)**
  - [ ] 实现原生选词捕获机制：监听特定组合键（如连续按两下 `Ctrl`），触发主进程模拟发送 `Ctrl+C`。
  - [ ] 抓取系统剪贴板文本/图片，自动填入并直接弹出 Fast Input 或指定的独立对话窗口。

- [ ] **E.3 系统托盘 (System Tray) 与后台运行**
  - [ ] 创建 `Tray` 图标，添加右键菜单（显示主窗口、退出应用等）。
  - [ ] 拦截主窗口的“关闭”事件，改为“隐藏到托盘”（确保定时任务持续运行）。

- [ ] **E.4 打包与分发**
  - [ ] 优化应用启动速度（Vite 预构建、懒加载等）。
  - [ ] 配置 NSIS (Windows) 打包参数（包括应用图标、安装路径、自启动注册表配置）。
  - [ ] 执行端到端测试，检查数据在打包后的 `userData` 路径读写是否正常。

---

## 12. 历史“当前阶段标记”（原文保留，不与新总览冲突）

> **当前阶段标记**：🟡 **Phase B.3 准备启动（主控制台其余页面复刻）**  
> *（下一步先补齐 B.3 阻塞 API：`coderedirect` / `runTaskNow` / `getRandomItem`，再逐页迁移 Providers/Mcp/Skills/Prompts/Tasks/Chats）*

> 说明：该标记为当时写入的历史状态；按本整理版“当前全局状态总览”，B.3 已完成，当前重心已推进到 B.4.H3.5（内置 MCP/Skill 完整复刻）并准备进入 Phase C。

---

## 13. 用户要求的“下一步严格顺序”（把散落的“下一会话首要任务”合并成唯一版本）

1) **继续收敛普通 MCP 的 `An object could not be cloned` 残余场景**  
   - 优先检查：`testInvokeMcpTool` 入参与返回值在 preload/ipc 边界是否仍含 Proxy/函数/不可克隆对象  
2) **按原项目完整复刻 `backend/src/mcp_builtin.js` 到桌面端，且无持久连接数量和最大并发连接数限制**（不再最小复刻）  
3) **对照原项目 `backend/src/skill.js` 补全 Skill 完整运行机制**（不再简化版）  
4) **然后进入 Phase C：Anywhere_window 完整复刻**（UI/聊天/工具审批/长图导出/协同联调）